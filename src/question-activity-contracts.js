import { completeAssistanceTrace,createAttempt,createAssistanceTrace,createReceipt,learningContractDigest,validateLearningEnvelope } from './learning-contracts.js';
import { decideEvidence } from './evidence-policy.js';
import { validateIeltsObjectiveInventoryItem } from './ielts-profile-inventory.js';
import { createSourceRevisionRef,validateSourceRevisionForExecution } from './source-revision-ref.js';
import { startTodayRun,recordTodayReceipt } from './today-runner.js';
import * as objectiveTextResponse from './objective-text-response.js';
import * as objectiveMatchingResponse from './objective-matching-response.js';
import { createObjectiveSpatialPrompt } from './objective-spatial-prompt.js';

export const QUESTION_ACTIVITY_SCHEMA='question-activity';
export const QUESTION_ACTIVITY_VERSION=1;
export const QUESTION_REGISTRY_REVISION='qar-00-registry-v1';
export const IELTS_LAB_CHOICE_KIND='ielts-lab-choice';
export const QUESTION_SCORER_VERSION='ielts-lab-choice-v1';
export const LISTENING_MULTIPLE_CHOICE_KIND='listening-multiple-choice';
export const LISTENING_MULTIPLE_CHOICE_REGISTRY_REVISION='qar-listening-mcq-registry-v1';
export const LISTENING_MULTIPLE_CHOICE_INVENTORY_REGISTRY_REVISION='qar-listening-mcq-inventory-registry-v2';
export const LISTENING_MULTIPLE_CHOICE_SCORER_VERSION='listening-multiple-choice-v1';
globalThis.createSourceRevisionRef=createSourceRevisionRef;
globalThis.learningContractDigest=learningContractDigest;
export { createIeltsReadingQuestionOwnerAdapter as createReadingQuestionOwnerAdapter };
export const LISTENING_ASSISTANCE_COLLECTION_MODE='qar-listening';
export const LISTENING_MULTIPLE_CHOICE_MULTIPLE_KIND='listening-multiple-choice-multiple';
export const LISTENING_MULTI_SELECT_REGISTRY_REVISION='qar-listening-multi-select-registry-v1';
export const READING_MULTIPLE_CHOICE_SINGLE_KIND='reading-multiple-choice-single';
export const READING_MULTIPLE_CHOICE_MULTIPLE_KIND='reading-multiple-choice-multiple';
export const READING_TRUE_FALSE_NOT_GIVEN_KIND='reading-true-false-not-given';
export const READING_YES_NO_NOT_GIVEN_KIND='reading-yes-no-not-given';
export const READING_SINGLE_SELECT_REGISTRY_REVISION='qar-reading-single-select-registry-v1';
export const READING_SINGLE_SELECT_SCORER_VERSION='reading-single-select-v1';
export const READING_MULTI_SELECT_REGISTRY_REVISION='qar-reading-multi-select-registry-v1';
export const OBJECTIVE_MULTI_SELECT_SCORER_VERSION='objective-multi-select-v1';
export const READING_ASSISTANCE_COLLECTION_MODE='qar-reading';
export const OBJECTIVE_TEXT_RESPONSE_REGISTRY_REVISION=objectiveTextResponse.OBJECTIVE_TEXT_RESPONSE_REGISTRY_REVISION;
export const OBJECTIVE_TEXT_RESPONSE_KINDS=objectiveTextResponse.OBJECTIVE_TEXT_RESPONSE_KINDS;
export const OBJECTIVE_TEXT_RESPONSE_NORMALIZER=objectiveTextResponse.OBJECTIVE_TEXT_RESPONSE_NORMALIZER;
export const OBJECTIVE_TEXT_RESPONSE_SCORER=objectiveTextResponse.OBJECTIVE_TEXT_RESPONSE_SCORER;
export const createObjectiveTextResponseOwnerAdapter=objectiveTextResponse.createObjectiveTextResponseOwnerAdapter;
export const createObjectiveTextResponseQuestion=objectiveTextResponse.createObjectiveTextResponseQuestion;
export const createObjectiveTextResponseQuestionAsync=objectiveTextResponse.createObjectiveTextResponseQuestionAsync;
export const normalizeObjectiveTextResponse=objectiveTextResponse.normalizeObjectiveTextResponse;
export const countObjectiveTextWords=objectiveTextResponse.countObjectiveTextWords;
export const OBJECTIVE_MATCHING_RESPONSE_REGISTRY_REVISION=objectiveMatchingResponse.OBJECTIVE_MATCHING_RESPONSE_REGISTRY_REVISION;
export const OBJECTIVE_MATCHING_RESPONSE_KINDS=objectiveMatchingResponse.OBJECTIVE_MATCHING_RESPONSE_KINDS;
export const OBJECTIVE_MATCHING_RESPONSE_NORMALIZER=objectiveMatchingResponse.OBJECTIVE_MATCHING_RESPONSE_NORMALIZER;
export const OBJECTIVE_MATCHING_RESPONSE_SCORER=objectiveMatchingResponse.OBJECTIVE_MATCHING_RESPONSE_SCORER;
export const createObjectiveMatchingResponseOwnerAdapter=objectiveMatchingResponse.createObjectiveMatchingResponseOwnerAdapter;
export const createObjectiveMatchingResponseQuestion=objectiveMatchingResponse.createObjectiveMatchingResponseQuestion;
export const createObjectiveMatchingResponseQuestionAsync=objectiveMatchingResponse.createObjectiveMatchingResponseQuestionAsync;
const MAX_JSON_BYTES=80_000;
const OWNER_ADAPTERS=new WeakSet();const LISTENING_OWNER_READERS=new WeakMap();const LISTENING_INVENTORY_OWNER_READERS=new WeakMap();const READING_OWNER_READERS=new WeakMap();const AUTHENTIC_QUESTIONS=new WeakMap();const QUESTION_REGISTRIES=new WeakSet();
const clean=(value,max=240)=>typeof value==='string'?value.trim().replace(/\s+/g,' ').slice(0,max):'';
const clone=value=>value==null?value:structuredClone(value);
const hasOwn=(value,key)=>Object.prototype.hasOwnProperty.call(value,key);
const failure=(code,message)=>Object.assign(new Error(message),{code});
function deepFreeze(value,seen=new Set()){if(!value||typeof value!=='object'||seen.has(value))return value;seen.add(value);for(const key of Reflect.ownKeys(value))deepFreeze(value[key],seen);return Object.freeze(value);}
function plain(value){try{const prototype=value&&typeof value==='object'?Object.getPrototypeOf(value):null;return Boolean(value)&&typeof value==='object'&&!Array.isArray(value)&&(prototype===Object.prototype||prototype===null);}catch{return false;}}
function dataOnly(value,seen=new Set(),depth=0){
  if(value===null||typeof value==='string'||typeof value==='boolean')return true;
  if(typeof value==='number')return Number.isFinite(value);
  if(typeof value!=='object'||depth>50||seen.has(value)||(!Array.isArray(value)&&!plain(value)))return false;
  seen.add(value);let descriptors;try{descriptors=Object.getOwnPropertyDescriptors(value);}catch{return false;}
  for(const [key,descriptor] of Object.entries(descriptors)){const normalized=key.replace(/[^a-z0-9]/gi,'').toLowerCase();if(/(secret(value)?|clientsecret|authorizationheader|credentialvalue|passwordhash|bearertoken|private(key|path)|filepath|path)$/.test(normalized)||!hasOwn(descriptor,'value')||!dataOnly(descriptor.value,seen,depth+1))return false;}
  seen.delete(value);return true;
}
function bounded(value){try{return dataOnly(value)&&new TextEncoder().encode(JSON.stringify(value)).length<=MAX_JSON_BYTES;}catch{return false;}}
const exact=(value,keys)=>plain(value)&&Object.keys(value).length===keys.length&&Object.keys(value).every(key=>keys.includes(key))&&keys.every(key=>hasOwn(value,key));
const stringValue=(value,max=240)=>typeof value==='string'&&value.trim().length>0&&value.trim().length<=max;
function canonicalSpatialPrompt(value,slotIds){try{const canonical=createObjectiveSpatialPrompt(value,{slotIds});return learningContractDigest(canonical)===learningContractDigest(value);}catch{return false;}}
function strictCoreSourceRef(input){
  if(!bounded(input)||!plain(input))return{valid:false,code:'QUESTION_ACTIVITY_SOURCE_INVALID',value:null};
  try{const value=createSourceRevisionRef(input);if(value.kind!=='core-card'||!plain(value.locator)||!stringValue(value.locator.cardId,180)||value.locator.cardId!==value.sourceId.replace(/^core-card:/,''))return{valid:false,code:'QUESTION_ACTIVITY_SOURCE_INVALID',value:null};return{valid:true,code:null,value};}catch{return{valid:false,code:'QUESTION_ACTIVITY_SOURCE_INVALID',value:null};}
}
function normalizeComparableText(value){return clean(value,2000).normalize('NFKC').toLocaleLowerCase();}
function canonicalItem(input){
  if(!bounded(input)||!plain(input))return{valid:false,errors:['IELTS Lab item must be bounded plain JSON data.'],value:null};
  const fields=['id','kind','prompt','context','options','sourceCardIds','status','provenance','createdAt','updatedAt'];
  if(!exact(input,fields)||!stringValue(input.id,180)||!['paraphrase','distractor'].includes(input.kind)||!stringValue(input.prompt,2000)||typeof input.context!=='string'||!Number.isFinite(input.createdAt)||!Number.isFinite(input.updatedAt)||input.updatedAt<input.createdAt)return{valid:false,errors:['IELTS Lab item has invalid immutable fields.'],value:null};
  if(!Array.isArray(input.options)||input.options.length<2||!input.options.every(option=>exact(option,['id','text','correct','rationale'])&&stringValue(option.id,100)&&stringValue(option.text,2000)&&typeof option.correct==='boolean'&&stringValue(option.rationale,2000)))return{valid:false,errors:['IELTS Lab item options must use the exact objective option schema.'],value:null};
  if(new Set(input.options.map(option=>option.id)).size!==input.options.length||new Set(input.options.map(option=>normalizeComparableText(option.text))).size!==input.options.length||input.options.filter(option=>option.correct).length!==1)return{valid:false,errors:['IELTS Lab item options are ambiguous or incomplete.'],value:null};
  if(!Array.isArray(input.sourceCardIds)||input.sourceCardIds.length===0||!input.sourceCardIds.every(value=>stringValue(value,180))||new Set(input.sourceCardIds).size!==input.sourceCardIds.length)return{valid:false,errors:['IELTS Lab item sourceCardIds are invalid.'],value:null};
  if(input.status!=='verified'||!exact(input.provenance,['status','verifiedBy'])||input.provenance.status!=='verified'||!stringValue(input.provenance.verifiedBy,180))return{valid:false,errors:['IELTS Lab item lacks a durable verified owner approval.'],value:null};
  return{valid:true,errors:[],value:Object.freeze(clone(input))};
}
function assertItemSourceBinding(item,sourceRevisionRef){const reference=strictCoreSourceRef(sourceRevisionRef);if(!reference.valid)throw failure(reference.code,'IELTS Lab adapter requires an immutable Core SourceRevisionRef.');if(!item.sourceCardIds.includes(reference.value.locator.cardId))throw failure('QUESTION_ACTIVITY_SOURCE_BINDING_MISMATCH','SourceRevisionRef card is not an exact sourceCardIds member of this IELTS Lab item.');return reference.value;}
function rawItemDigest(item){return learningContractDigest(item);}
function safeProjection(item){return deepFreeze({id:item.id,kind:item.kind,prompt:item.prompt,context:item.context,options:item.options.map(option=>({id:option.id,text:option.text})),sourceCardIds:[...item.sourceCardIds],createdAt:item.createdAt,updatedAt:item.updatedAt});}

export function createIeltsLabOwnerAdapter({readVerifiedItem}={}){
  if(typeof readVerifiedItem!=='function')throw failure('QUESTION_ACTIVITY_OWNER_RESOLVER_REQUIRED','IELTS Lab owner adapter requires a read-only verified-item resolver.');
  const adapter=Object.freeze({readVerifiedItem(id){const item=readVerifiedItem(id);return item&&typeof item.then==='function'?item.then(value=>value==null?null:clone(value)):item==null?null:clone(item);}});OWNER_ADAPTERS.add(adapter);return adapter;
}
function ownerRead(item,adapter){
  if(!OWNER_ADAPTERS.has(adapter))throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','IELTS Lab questions require a module-created owner adapter.');
  const value=adapter.readVerifiedItem(item.id);if(value&&typeof value.then==='function')throw failure('QUESTION_ACTIVITY_OWNER_ASYNC_UNSUPPORTED','Use adaptIeltsLabItemAsync for an asynchronous owner resolver.');
  const owned=canonicalItem(value);if(!owned.valid||rawItemDigest(owned.value)!==rawItemDigest(item))throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Question data must exactly match a durable owner-verified item.');
  return owned.value;
}
export async function adaptIeltsLabItemAsync(item,sourceRevisionRef,{id=null,promptRevision=null,ownerAdapter}={}){
  const validation=canonicalItem(item);if(!validation.valid)throw failure('QUESTION_ACTIVITY_ITEM_INVALID',validation.errors.join(' '));
  if(!OWNER_ADAPTERS.has(ownerAdapter))throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','IELTS Lab questions require a module-created owner adapter.');
  const owned=canonicalItem(await ownerAdapter.readVerifiedItem(validation.value.id));if(!owned.valid||rawItemDigest(owned.value)!==rawItemDigest(validation.value))throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Question data must exactly match a durable owner-verified item.');
  return buildQuestion(owned.value,sourceRevisionRef,{id,promptRevision});
}
export function adaptIeltsLabItem(item,sourceRevisionRef,{id=null,promptRevision=null,ownerAdapter}={}){
  const validation=canonicalItem(item);if(!validation.valid)throw failure('QUESTION_ACTIVITY_ITEM_INVALID',validation.errors.join(' '));
  const owned=ownerRead(validation.value,ownerAdapter);return buildQuestion(owned,sourceRevisionRef,{id,promptRevision});
}
function buildQuestion(owned,sourceRevisionRef,{id=null,promptRevision=null}={}){
  const reference=assertItemSourceBinding(owned,sourceRevisionRef);
  if(id!=null&&!stringValue(id,180))throw failure('QUESTION_ACTIVITY_ID_INVALID','Question activity id must be a string.');if(promptRevision!=null&&!stringValue(promptRevision,240))throw failure('QUESTION_ACTIVITY_PROMPT_INVALID','Question prompt revision must be a string.');
  const keyDigest=learningContractDigest(owned.options.map(row=>({id:row.id,correct:row.correct})));
  const promptDigest=learningContractDigest({id:owned.id,kind:owned.kind,prompt:owned.prompt,context:owned.context,options:owned.options.map(row=>({id:row.id,text:row.text})),sourceCardIds:owned.sourceCardIds,sourceRevisionRef:reference,createdAt:owned.createdAt,updatedAt:owned.updatedAt});
  const rubricDigest=learningContractDigest({rationales:owned.options.map(row=>({id:row.id,rationale:row.rationale})),reviewPolicy:'objective-owner-review-v1'});const question=deepFreeze({schema:QUESTION_ACTIVITY_SCHEMA,version:QUESTION_ACTIVITY_VERSION,registryRevision:QUESTION_REGISTRY_REVISION,kind:IELTS_LAB_CHOICE_KIND,id:clean(id,180)||`qar:${owned.id}`,item:safeProjection(owned),sourceRevisionRef:clone(reference),promptRevision:clean(promptRevision,240)||`ielts-lab:${owned.updatedAt}`,promptDigest,keyRevision:`key:${owned.updatedAt}`,keyDigest,rubricRevision:`rubric:${owned.updatedAt}`,rubricDigest,reviewPolicyRevision:'objective-owner-review-v1',scorer:{id:QUESTION_SCORER_VERSION,version:1},requiredCapabilities:['keyboard','focus','screen-reader'],approval:'owner-verified'});
  AUTHENTIC_QUESTIONS.set(question,Object.freeze({item:deepFreeze(clone(owned)),sourceRevisionRef:deepFreeze(clone(reference)),keyDigest,promptDigest,rubricDigest,registryRevision:QUESTION_REGISTRY_REVISION,publicDigest:learningContractDigest(question)}));return question;
}

function strictTranscriptRef(input){
  if(!bounded(input)||!plain(input))return{valid:false,value:null};
  try{const value=createSourceRevisionRef(input);if(value.kind!=="transcript"||value.authority!=="canonical-transcript-registry"||!exact(value.locator,['revisionId'])||value.locator.revisionId!==value.revisionId)return{valid:false,value:null};return{valid:true,value};}catch{return{valid:false,value:null};}
}
function canonicalListeningItem(input){
  if(!bounded(input)||!plain(input)||!stringValue(input.id,180)||!Number.isFinite(input.createdAt)||!Number.isFinite(input.updatedAt)||input.updatedAt<input.createdAt)return{valid:false,value:null};
  if(input.kind===LISTENING_MULTIPLE_CHOICE_KIND){
    const fields=['id','kind','prompt','options','target','sourceAnchor','status','provenance','createdAt','updatedAt'];
    if(!exact(input,fields)||!stringValue(input.prompt,2000))return{valid:false,value:null};
    if(!Array.isArray(input.options)||input.options.length<2||!input.options.every(row=>exact(row,['id','text','correct','rationale'])&&stringValue(row.id,100)&&stringValue(row.text,2000)&&typeof row.correct==='boolean'&&stringValue(row.rationale,2000))||new Set(input.options.map(row=>row.id)).size!==input.options.length||new Set(input.options.map(row=>normalizeComparableText(row.text))).size!==input.options.length||input.options.filter(row=>row.correct).length!==1)return{valid:false,value:null};
    if(!exact(input.target,['cardId','senseId','skill'])||!stringValue(input.target.cardId,180)||(input.target.senseId!==null&&!stringValue(input.target.senseId,180))||input.target.skill!=='listening')return{valid:false,value:null};
    const anchor=input.sourceAnchor;if(!exact(anchor,['sourceId','revisionId','integrity','segmentIds','startMs','endMs'])||!stringValue(anchor.sourceId,240)||!stringValue(anchor.revisionId,500)||!stringValue(anchor.integrity,240)||!Array.isArray(anchor.segmentIds)||!anchor.segmentIds.length||!anchor.segmentIds.every(id=>stringValue(id,500))||new Set(anchor.segmentIds).size!==anchor.segmentIds.length||!Number.isFinite(anchor.startMs)||!Number.isFinite(anchor.endMs)||anchor.startMs<0||anchor.endMs<=anchor.startMs)return{valid:false,value:null};
    if(input.status!=='verified'||!exact(input.provenance,['status','verifiedBy','rights'])||input.provenance.status!=='verified'||!stringValue(input.provenance.verifiedBy,180)||input.provenance.rights!=='allowed')return{valid:false,value:null};
    return{valid:true,value:deepFreeze(clone(input))};
  }
  if(input.kind===LISTENING_MULTIPLE_CHOICE_MULTIPLE_KIND){
    const fields=['id','kind','skill','prompt','expectedCount','options','correctOptionIds','rationales','target','sourceAnchor','createdAt','updatedAt'];
    if(!exact(input,fields)||input.skill!=='listening'||!stringValue(input.prompt,2000)||!Number.isSafeInteger(input.expectedCount)||input.expectedCount<2)return{valid:false,value:null};
    if(!Array.isArray(input.options)||input.options.length<input.expectedCount||!input.options.every(row=>exact(row,['id','text'])&&stringValue(row.id,100)&&stringValue(row.text,2000))||new Set(input.options.map(row=>row.id)).size!==input.options.length||new Set(input.options.map(row=>normalizeComparableText(row.text))).size!==input.options.length)return{valid:false,value:null};
    if(!Array.isArray(input.correctOptionIds)||input.correctOptionIds.length!==input.expectedCount||new Set(input.correctOptionIds).size!==input.expectedCount||!input.correctOptionIds.every(id=>input.options.some(opt=>opt.id===id)))return{valid:false,value:null};
    if(!plain(input.rationales)||!input.correctOptionIds.every(id=>hasOwn(input.rationales,id)&&stringValue(input.rationales[id],2000)))return{valid:false,value:null};
    if(!exact(input.target,['cardId','senseId','skill'])||!stringValue(input.target.cardId,180)||(input.target.senseId!==null&&!stringValue(input.target.senseId,180))||input.target.skill!=='listening')return{valid:false,value:null};
    const anchor=input.sourceAnchor;if(!exact(anchor,['sourceId','revisionId','integrity','segmentIds','startMs','endMs'])||!stringValue(anchor.sourceId,240)||!stringValue(anchor.revisionId,500)||!stringValue(anchor.integrity,240)||!Array.isArray(anchor.segmentIds)||!anchor.segmentIds.length||!anchor.segmentIds.every(id=>stringValue(id,500))||new Set(anchor.segmentIds).size!==anchor.segmentIds.length||!Number.isFinite(anchor.startMs)||!Number.isFinite(anchor.endMs)||anchor.startMs<0||anchor.endMs<=anchor.startMs)return{valid:false,value:null};
    return{valid:true,value:deepFreeze(clone(input))};
  }
  return{valid:false,value:null};
}
function transcriptOwnerProvenance(aggregate){
  const revision=aggregate.revision,source=aggregate.source,provenance=revision.provenance;
  if(!plain(provenance))return null;
  const verification=revision.status==='unverified'||provenance.verification==='unverified'?'unverified':revision.status==='verified'||provenance.verification==='verified'?'verified':'unknown';
  const rights=[revision.rightsStatus,source.rightsStatus,provenance.rights].includes('blocked')?'blocked':[revision.rightsStatus,source.rightsStatus,provenance.rights].includes('allowed')?'allowed':'unknown';
  const privacy=['private','shared','public'].includes(source.namespace)?source.namespace:['private','shared','public'].includes(provenance.privacy)?provenance.privacy:'private';
  return{origin:stringValue(provenance.origin,240)?provenance.origin:'canonical-transcript-registry',verification,rights,privacy};
}
function listeningAggregate(anchor,reference,aggregate){
  if(!dataOnly(aggregate)||!plain(anchor)||!plain(reference)||!plain(aggregate)||!plain(aggregate.source)||!plain(aggregate.revision)||!Array.isArray(aggregate.segments))return null;
  const source=aggregate.source,revision=aggregate.revision,segments=aggregate.segments,coverage=revision.coverage,expectedProvenance=transcriptOwnerProvenance(aggregate);
  if(source.kind!=='transcript-source'||source.schemaVersion!==1||revision.kind!=='transcript-revision'||revision.schemaVersion!==1||source.id!==anchor.sourceId||revision.sourceId!==source.id||revision.id!==anchor.revisionId||revision.contentDigest!==anchor.integrity||reference.sourceId!==anchor.sourceId||reference.revisionId!==anchor.revisionId||reference.integrity!==anchor.integrity||!expectedProvenance||learningContractDigest(reference.provenance)!==learningContractDigest(expectedProvenance))return null;
  if(revision.status!=='verified'||source.status!=='verified'||source.activeRevisionId!==revision.id||expectedProvenance.verification!=='verified'||expectedProvenance.rights!=='allowed'||revision.provenance.aligned===false||revision.provenance.alignmentStatus==='unaligned'||revision.tombstone||source.tombstone||revision.deleted||source.deleted||!plain(coverage)||coverage.complete!==true||coverage.ratio!==1||coverage.coveredMs!==coverage.endMs-coverage.startMs||!Array.isArray(revision.segmentIds)||revision.segmentIds.length!==segments.length||new Set(revision.segmentIds).size!==revision.segmentIds.length)return null;
  let coveredMs=0;const byId=new Map();for(let index=0;index<segments.length;index+=1){const row=segments[index];if(!plain(row)||!stringValue(row.id,500)||revision.segmentIds[index]!==row.id||byId.has(row.id)||row.sourceId!==source.id||row.revisionId!==revision.id||row.status!=='verified'||row.aligned!==true||!Number.isFinite(row.startMs)||!Number.isFinite(row.endMs)||row.startMs<0||row.endMs<=row.startMs||(index&&row.startMs!==segments[index-1].endMs))return null;coveredMs+=row.endMs-row.startMs;byId.set(row.id,{row,index});}
  if(!segments.length||!Number.isFinite(coverage.startMs)||!Number.isFinite(coverage.endMs)||!Number.isFinite(coverage.coveredMs)||!Number.isFinite(coverage.ratio)||typeof coverage.complete!=='boolean'||coverage.startMs!==segments[0].startMs||coverage.endMs!==segments.at(-1).endMs||coverage.coveredMs!==coveredMs||coverage.ratio!==Math.min(1,coveredMs/(coverage.endMs-coverage.startMs)))return null;
  const selected=[];let previous=null;for(const id of anchor.segmentIds){const value=byId.get(id);if(!value||previous&&(value.index!==previous.index+1||value.row.startMs!==previous.row.endMs))return null;selected.push(value);previous=value;}
  if(!selected.length||selected[0].row.startMs!==anchor.startMs||selected.at(-1).row.endMs!==anchor.endMs||anchor.startMs<coverage.startMs||anchor.endMs>coverage.endMs)return null;return selected.map(value=>value.row);
}
export function createListeningQuestionOwnerAdapter(options={}){
  if(!plain(options))throw failure('QUESTION_ACTIVITY_OWNER_RESOLVER_REQUIRED','Listening owner adapter requires verified item and Transcript readers.');let descriptors;try{descriptors=Object.getOwnPropertyDescriptors(options);}catch{throw failure('QUESTION_ACTIVITY_OWNER_RESOLVER_REQUIRED','Listening owner adapter requires verified item and Transcript readers.');}const itemDescriptor=descriptors.readVerifiedItem,aggregateDescriptor=descriptors.getTranscriptAggregate;if(!exact(options,['readVerifiedItem','getTranscriptAggregate'])||!itemDescriptor||!aggregateDescriptor||!hasOwn(itemDescriptor,'value')||!hasOwn(aggregateDescriptor,'value')||typeof itemDescriptor.value!=='function'||typeof aggregateDescriptor.value!=='function')throw failure('QUESTION_ACTIVITY_OWNER_RESOLVER_REQUIRED','Listening owner adapter requires verified item and Transcript readers.');const readVerifiedItem=itemDescriptor.value,getTranscriptAggregate=aggregateDescriptor.value;
  const adapter=Object.freeze({});OWNER_ADAPTERS.add(adapter);LISTENING_OWNER_READERS.set(adapter,Object.freeze({readVerifiedItem,getTranscriptAggregate}));return adapter;
}
export async function adaptListeningMultipleChoiceItem(item,sourceRevisionRef,options={}){
  if(!plain(options))throw failure('QUESTION_ACTIVITY_ID_INVALID','Listening question options are invalid.');let descriptors;try{descriptors=Object.getOwnPropertyDescriptors(options);}catch{throw failure('QUESTION_ACTIVITY_ID_INVALID','Listening question options are invalid.');}if(Object.keys(descriptors).some(key=>!['id','promptRevision','ownerAdapter'].includes(key))||Object.values(descriptors).some(descriptor=>!hasOwn(descriptor,'value')))throw failure('QUESTION_ACTIVITY_ID_INVALID','Listening question options are invalid.');const id=descriptors.id?.value??null,promptRevision=descriptors.promptRevision?.value??null,ownerAdapter=descriptors.ownerAdapter?.value;
  const validation=canonicalListeningItem(item);if(!validation.valid)throw failure('QUESTION_ACTIVITY_ITEM_INVALID','Listening item is invalid.');const readers=LISTENING_OWNER_READERS.get(ownerAdapter);if(!OWNER_ADAPTERS.has(ownerAdapter)||!readers)throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Listening questions require a module-created owner adapter.');
  const reference=strictTranscriptRef(sourceRevisionRef);if(!reference.valid)throw failure('QUESTION_ACTIVITY_SOURCE_INVALID','Listening questions require an exact Transcript SourceRevisionRef.');
  let ownedRaw,aggregate;try{[ownedRaw,aggregate]=await Promise.all([readers.readVerifiedItem(validation.value.id),readers.getTranscriptAggregate(reference.value.revisionId)]);}catch{throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Listening owner reader is unavailable.');}const owned=canonicalListeningItem(ownedRaw);if(!owned.valid||rawItemDigest(owned.value)!==rawItemDigest(validation.value))throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Listening item must exactly match owner verification.');
  const anchorValid = owned.value.kind === LISTENING_MULTIPLE_CHOICE_MULTIPLE_KIND ? Boolean(aggregate && aggregate.source?.status === 'verified' && reference.value.sourceId === owned.value.sourceAnchor?.sourceId) : Boolean(listeningAggregate(owned.value.sourceAnchor,reference.value,aggregate));
  if(!anchorValid)throw failure('QUESTION_ACTIVITY_SOURCE_BINDING_MISMATCH','Listening transcript anchor is unavailable, unsafe, or mismatched.');
  if(id!=null&&!stringValue(id,180)||promptRevision!=null&&!stringValue(promptRevision,240))throw failure('QUESTION_ACTIVITY_ID_INVALID','Listening question identity is invalid.');
  if(owned.value.kind===LISTENING_MULTIPLE_CHOICE_MULTIPLE_KIND){
    const keyDigest=learningContractDigest(owned.value.correctOptionIds);
    const promptDigest=learningContractDigest({id:owned.value.id,prompt:owned.value.prompt,expectedCount:owned.value.expectedCount,options:owned.value.options.map(row=>({id:row.id,text:row.text})),target:owned.value.target,sourceAnchor:owned.value.sourceAnchor,sourceRevisionRef:reference.value});
    const rubricDigest=learningContractDigest({rationales:owned.value.rationales,reviewPolicy:'objective-listening-review-v1'});
    const projection={id:owned.value.id,kind:owned.value.kind,prompt:owned.value.prompt,expectedCount:owned.value.expectedCount,options:owned.value.options.map(row=>({id:row.id,text:row.text})),target:clone(owned.value.target),sourceAnchor:clone(owned.value.sourceAnchor),createdAt:owned.value.createdAt,updatedAt:owned.value.updatedAt};
    const question=deepFreeze({schema:QUESTION_ACTIVITY_SCHEMA,version:1,registryRevision:LISTENING_MULTI_SELECT_REGISTRY_REVISION,kind:LISTENING_MULTIPLE_CHOICE_MULTIPLE_KIND,id:clean(id,180)||`qar:${owned.value.id}`,item:projection,sourceRevisionRef:clone(reference.value),promptRevision:clean(promptRevision,240)||`listening:${owned.value.updatedAt}`,promptDigest,keyRevision:`key:${owned.value.updatedAt}`,keyDigest,rubricRevision:`rubric:${owned.value.updatedAt}`,rubricDigest,reviewPolicyRevision:'objective-listening-review-v1',scorer:{id:OBJECTIVE_MULTI_SELECT_SCORER_VERSION,version:1},requiredCapabilities:['audio-playback','focus','keyboard','screen-reader'],approval:'owner-verified'});
    AUTHENTIC_QUESTIONS.set(question,Object.freeze({item:owned.value,sourceRevisionRef:deepFreeze(clone(reference.value)),keyDigest,promptDigest,rubricDigest,registryRevision:LISTENING_MULTI_SELECT_REGISTRY_REVISION,publicDigest:learningContractDigest(question),ownerAdapter}));
    return question;
  }
  const keyDigest=learningContractDigest(owned.value.options.map(row=>({id:row.id,correct:row.correct})));const promptDigest=learningContractDigest({id:owned.value.id,prompt:owned.value.prompt,options:owned.value.options.map(row=>({id:row.id,text:row.text})),target:owned.value.target,sourceAnchor:owned.value.sourceAnchor,sourceRevisionRef:reference.value});const rubricDigest=learningContractDigest({rationales:owned.value.options.map(row=>({id:row.id,rationale:row.rationale})),reviewPolicy:'objective-listening-review-v1'});const projection={id:owned.value.id,kind:owned.value.kind,prompt:owned.value.prompt,options:owned.value.options.map(row=>({id:row.id,text:row.text})),target:clone(owned.value.target),sourceAnchor:clone(owned.value.sourceAnchor),createdAt:owned.value.createdAt,updatedAt:owned.value.updatedAt};
  const question=deepFreeze({schema:QUESTION_ACTIVITY_SCHEMA,version:1,registryRevision:LISTENING_MULTIPLE_CHOICE_REGISTRY_REVISION,kind:LISTENING_MULTIPLE_CHOICE_KIND,id:clean(id,180)||`qar:${owned.value.id}`,item:projection,sourceRevisionRef:clone(reference.value),promptRevision:clean(promptRevision,240)||`listening:${owned.value.updatedAt}`,promptDigest,keyRevision:`key:${owned.value.updatedAt}`,keyDigest,rubricRevision:`rubric:${owned.value.updatedAt}`,rubricDigest,reviewPolicyRevision:'objective-listening-review-v1',scorer:{id:LISTENING_MULTIPLE_CHOICE_SCORER_VERSION,version:1},requiredCapabilities:['audio-playback','focus','keyboard','screen-reader'],approval:'owner-verified'});AUTHENTIC_QUESTIONS.set(question,Object.freeze({item:owned.value,sourceRevisionRef:deepFreeze(clone(reference.value)),keyDigest,promptDigest,rubricDigest,registryRevision:LISTENING_MULTIPLE_CHOICE_REGISTRY_REVISION,publicDigest:learningContractDigest(question),ownerAdapter}));return question;
}

function sealedListeningOptions(options){return Array.isArray(options)&&options.length>=2&&options.every(row=>exact(row,['id','text','correct','rationale'])&&stringValue(row.id,100)&&stringValue(row.text,2000)&&typeof row.correct==='boolean'&&stringValue(row.rationale,2000))&&new Set(options.map(row=>row.id)).size===options.length&&new Set(options.map(row=>normalizeComparableText(row.text))).size===options.length&&options.filter(row=>row.correct).length===1;}
function listeningInventory(input){
  const validated=validateIeltsObjectiveInventoryItem(input,{historical:true});if(!validated.valid||learningContractDigest(validated.value)!==learningContractDigest(input))return null;input=validated.value;
  if(!bounded(input)||!plain(input)||input.skill!=='listening'||input.status!=='verified'||!Array.isArray(input.profiles)||input.profiles.length!==2||input.profiles[0]!=='academic'||input.profiles[1]!=='general-training'||!stringValue(input.id,240)||!plain(input.questionBinding)||!plain(input.questionPayload))return null;
  const binding=input.questionBinding,payload=input.questionPayload,reference=strictTranscriptRef(input.sourceRevisionRef),target=payload.target,anchor=payload.sourceAnchor;
  if(!reference.valid||!exact(binding,['kind','schemaVersion','registryRevision','questionId','promptRevision','promptDigest','keyRevision','keyDigest','rubricRevision','rubricDigest','scorer','reviewPolicyRevision','requiredCapabilities'])||binding.kind!==LISTENING_MULTIPLE_CHOICE_KIND||binding.schemaVersion!==2||binding.registryRevision!==LISTENING_MULTIPLE_CHOICE_INVENTORY_REGISTRY_REVISION||!['questionId','promptRevision','promptDigest','keyRevision','keyDigest','rubricRevision','rubricDigest','reviewPolicyRevision'].every(key=>stringValue(binding[key],240))||binding.reviewPolicyRevision!=='objective-listening-review-v1'||!exact(binding.scorer,['id','version'])||binding.scorer.id!==LISTENING_MULTIPLE_CHOICE_SCORER_VERSION||binding.scorer.version!==1||!Array.isArray(binding.requiredCapabilities)||binding.requiredCapabilities.length!==4||new Set(binding.requiredCapabilities).size!==4||!['audio-playback','focus','keyboard','screen-reader'].every(value=>binding.requiredCapabilities.includes(value)))return null;
  if(!exact(payload,['id','kind','prompt','options','sealedOptions','target','sourceAnchor','createdAt','updatedAt'])||payload.id!==binding.questionId||payload.kind!==LISTENING_MULTIPLE_CHOICE_KIND||!stringValue(payload.prompt,2000)||!Array.isArray(payload.options)||payload.options.length<2||!payload.options.every(row=>exact(row,['id','text'])&&stringValue(row.id,100)&&stringValue(row.text,2000))||new Set(payload.options.map(row=>row.id)).size!==payload.options.length||new Set(payload.options.map(row=>normalizeComparableText(row.text))).size!==payload.options.length||!sealedListeningOptions(payload.sealedOptions)||payload.options.length!==payload.sealedOptions.length||payload.options.some((row,index)=>row.id!==payload.sealedOptions[index].id||row.text!==payload.sealedOptions[index].text)||!Number.isFinite(payload.createdAt)||!Number.isFinite(payload.updatedAt)||payload.updatedAt<payload.createdAt)return null;
  if(!exact(target||{},['schemaVersion','targetType','targetId','cardId','senseId','skill','sourceId','sourceRevision'])||target.schemaVersion!==2||target.targetType!=='ielts-objective-item'||target.targetId!==input.id||target.cardId!==null||target.senseId!==null||target.skill!=='listening'||target.sourceId!==reference.value.sourceId||target.sourceRevision!==reference.value.revisionId)return null;
  if(!exact(anchor||{},['sourceId','revisionId','integrity','segmentIds','startMs','endMs'])||anchor.sourceId!==reference.value.sourceId||anchor.revisionId!==reference.value.revisionId||anchor.integrity!==reference.value.integrity||!Array.isArray(anchor.segmentIds)||!anchor.segmentIds.length||!anchor.segmentIds.every(value=>stringValue(value,500))||new Set(anchor.segmentIds).size!==anchor.segmentIds.length||!Number.isFinite(anchor.startMs)||!Number.isFinite(anchor.endMs)||anchor.startMs<0||anchor.endMs<=anchor.startMs)return null;
  const publicPayload={id:payload.id,kind:payload.kind,prompt:payload.prompt,options:payload.options,target:payload.target,sourceAnchor:payload.sourceAnchor,createdAt:payload.createdAt,updatedAt:payload.updatedAt},promptDigest=learningContractDigest({item:publicPayload,sourceRevisionRef:reference.value}),keyDigest=learningContractDigest(payload.sealedOptions.map(row=>({id:row.id,correct:row.correct}))),rubricDigest=learningContractDigest({rationales:payload.sealedOptions.map(row=>({id:row.id,rationale:row.rationale})),reviewPolicy:'objective-listening-review-v1'});
  if(binding.promptDigest!==promptDigest||binding.keyDigest!==keyDigest||binding.rubricDigest!==rubricDigest)return null;return deepFreeze(clone(input));
}
export function createIeltsListeningQuestionOwnerAdapter(options={}){
  if(!plain(options))throw failure('QUESTION_ACTIVITY_OWNER_RESOLVER_REQUIRED','Listening inventory owner adapter requires verified inventory and Transcript readers.');let descriptors;try{descriptors=Object.getOwnPropertyDescriptors(options);}catch{throw failure('QUESTION_ACTIVITY_OWNER_RESOLVER_REQUIRED','Listening inventory owner adapter requires verified inventory and Transcript readers.');}if(!exact(options,['readVerifiedInventory','getTranscriptAggregate'])||!hasOwn(descriptors.readVerifiedInventory||{},'value')||!hasOwn(descriptors.getTranscriptAggregate||{},'value')||typeof descriptors.readVerifiedInventory.value!=='function'||typeof descriptors.getTranscriptAggregate.value!=='function')throw failure('QUESTION_ACTIVITY_OWNER_RESOLVER_REQUIRED','Listening inventory owner adapter requires verified inventory and Transcript readers.');const adapter=Object.freeze({});OWNER_ADAPTERS.add(adapter);LISTENING_INVENTORY_OWNER_READERS.set(adapter,Object.freeze({readVerifiedInventory:descriptors.readVerifiedInventory.value,getTranscriptAggregate:descriptors.getTranscriptAggregate.value}));return adapter;
}
export async function adaptIeltsListeningObjectiveItem(input,sourceRevisionRef,options={}){
  if(!plain(options))throw failure('QUESTION_ACTIVITY_ID_INVALID','Listening inventory question options are invalid.');let descriptors;try{descriptors=Object.getOwnPropertyDescriptors(options);}catch{throw failure('QUESTION_ACTIVITY_ID_INVALID','Listening inventory question options are invalid.');}if(Object.keys(descriptors).some(key=>!['id','promptRevision','ownerAdapter'].includes(key))||Object.values(descriptors).some(descriptor=>!hasOwn(descriptor,'value')))throw failure('QUESTION_ACTIVITY_ID_INVALID','Listening inventory question options are invalid.');const candidate=listeningInventory(input),reference=strictTranscriptRef(sourceRevisionRef),ownerAdapter=descriptors.ownerAdapter?.value,id=descriptors.id?.value??null,promptRevision=descriptors.promptRevision?.value??null,readers=LISTENING_INVENTORY_OWNER_READERS.get(ownerAdapter);if(!candidate)throw failure('QUESTION_ACTIVITY_ITEM_INVALID','Listening inventory item is invalid.');if(!reference.valid)throw failure('QUESTION_ACTIVITY_SOURCE_INVALID','Listening questions require an exact immutable Transcript source.');if(!OWNER_ADAPTERS.has(ownerAdapter)||!readers)throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Listening questions require a module-created inventory owner adapter.');let owned,aggregate;try{[owned,aggregate]=await Promise.all([readers.readVerifiedInventory(candidate.id),readers.getTranscriptAggregate(reference.value.revisionId)]);}catch{throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Listening inventory owner reader is unavailable.');}const canonical=listeningInventory(owned);if(!canonical||learningContractDigest(canonical)!==learningContractDigest(candidate))throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Listening inventory must exactly match its verified owner record.');if(!listeningAggregate(canonical.questionPayload.sourceAnchor,reference.value,aggregate))throw failure('QUESTION_ACTIVITY_SOURCE_BINDING_MISMATCH','Listening Transcript source is unavailable, unsafe, or mismatched.');if(id!=null&&id!==canonical.questionBinding.questionId||promptRevision!=null&&promptRevision!==canonical.questionBinding.promptRevision)throw failure('QUESTION_ACTIVITY_ID_INVALID','Listening inventory question identity must equal its canonical owner binding.');
  const payload=canonical.questionPayload,projection={id:payload.id,kind:payload.kind,prompt:payload.prompt,options:clone(payload.options),target:clone(payload.target),sourceAnchor:clone(payload.sourceAnchor),createdAt:payload.createdAt,updatedAt:payload.updatedAt};const question=deepFreeze({schema:QUESTION_ACTIVITY_SCHEMA,version:2,registryRevision:LISTENING_MULTIPLE_CHOICE_INVENTORY_REGISTRY_REVISION,kind:LISTENING_MULTIPLE_CHOICE_KIND,id:canonical.questionBinding.questionId,item:projection,sourceRevisionRef:clone(reference.value),promptRevision:canonical.questionBinding.promptRevision,promptDigest:canonical.questionBinding.promptDigest,keyRevision:canonical.questionBinding.keyRevision,keyDigest:canonical.questionBinding.keyDigest,rubricRevision:canonical.questionBinding.rubricRevision,rubricDigest:canonical.questionBinding.rubricDigest,reviewPolicyRevision:'objective-listening-review-v1',scorer:{id:LISTENING_MULTIPLE_CHOICE_SCORER_VERSION,version:1},requiredCapabilities:['audio-playback','focus','keyboard','screen-reader'],approval:'owner-verified'});AUTHENTIC_QUESTIONS.set(question,Object.freeze({item:deepFreeze({inventoryId:canonical.id,options:clone(payload.sealedOptions),target:clone(payload.target),sourceAnchor:clone(payload.sourceAnchor)}),sourceRevisionRef:deepFreeze(clone(reference.value)),keyDigest:canonical.questionBinding.keyDigest,promptDigest:canonical.questionBinding.promptDigest,rubricDigest:canonical.questionBinding.rubricDigest,registryRevision:LISTENING_MULTIPLE_CHOICE_INVENTORY_REGISTRY_REVISION,publicDigest:learningContractDigest(question),ownerAdapter,ownerInventoryDigest:learningContractDigest(canonical),ownerTranscriptDigest:learningContractDigest(aggregate),listeningInventory:true}));return question;
}

const READING_KINDS=Object.freeze([READING_MULTIPLE_CHOICE_SINGLE_KIND,READING_TRUE_FALSE_NOT_GIVEN_KIND,READING_YES_NO_NOT_GIVEN_KIND]);
function strictReadingRef(input){
  if(!bounded(input)||!plain(input))return{valid:false,value:null};
  try{const value=createSourceRevisionRef(input);if(value.kind!=='ielts-reading-passage'||!['ielts-reading-owner','ielts-reading-source'].includes(value.authority)||!exact(value.locator,['passageId','revision'])||!stringValue(value.locator.passageId,240))return{valid:false,value:null};return{valid:true,value};}catch{return{valid:false,value:null};}
}
function readingOptions(kind,options,{sealed=false,expectedCount=null}={}){
  if(kind===READING_MULTIPLE_CHOICE_MULTIPLE_KIND){
    if(!Array.isArray(options)||options.length<2||options.length>10)return false;
    if(!options.every(option=>plain(option)&&exact(option,sealed?['id','text','correct','rationale']:['id','text'])&&stringValue(option.id,100)&&stringValue(option.text,2000)&&(sealed?typeof option.correct==='boolean'&&typeof option.rationale==='string':true)))return false;
    if(new Set(options.map(option=>option.id)).size!==options.length||new Set(options.map(option=>normalizeComparableText(option.text))).size!==options.length)return false;
    if(sealed&&expectedCount!=null&&options.filter(option=>option.correct).length!==expectedCount)return false;
    return true;
  }
  if(!Array.isArray(options)||(kind===READING_MULTIPLE_CHOICE_SINGLE_KIND?(options.length<2||options.length>8):(options.length!==3)))return false;
  const fixed=kind===READING_TRUE_FALSE_NOT_GIVEN_KIND?['true','false','not-given']:kind===READING_YES_NO_NOT_GIVEN_KIND?['yes','no','not-given']:null;
  if(!options.every(option=>plain(option)&&exact(option,sealed?['id','text','correct','rationale']:['id','text'])&&stringValue(option.id,100)&&stringValue(option.text,2000)&&(sealed?typeof option.correct==='boolean'&&stringValue(option.rationale,2000):true)))return false;
  if(new Set(options.map(option=>option.id)).size!==options.length||new Set(options.map(option=>normalizeComparableText(option.text))).size!==options.length)return false;
  if(fixed&&!fixed.every((id,index)=>options[index]?.id===id))return false;
  return !sealed||options.filter(option=>option.correct).length===1;
}
function readingInventory(input){
  if(!bounded(input)||!plain(input)||!stringValue(input.id,240)||input.skill!=='reading'||input.status!=='verified'||!Array.isArray(input.profiles)||input.profiles.length!==1||!['academic','general-training'].includes(input.profiles[0])||!plain(input.questionBinding)||!plain(input.questionPayload))return null;
  const binding=input.questionBinding,payload=input.questionPayload,kind=binding.kind;
  if(kind===READING_MULTIPLE_CHOICE_MULTIPLE_KIND){
    if(binding.schemaVersion!==1||binding.registryRevision!==READING_MULTI_SELECT_REGISTRY_REVISION||!exact(binding.scorer||{},['id','version'])||binding.scorer.id!==OBJECTIVE_MULTI_SELECT_SCORER_VERSION||binding.scorer.version!==1||!Array.isArray(binding.requiredCapabilities)||binding.requiredCapabilities.length!==3||!['keyboard','focus','screen-reader'].every(value=>binding.requiredCapabilities.includes(value))||payload.kind!==kind||!Number.isSafeInteger(payload.expectedCount)||payload.expectedCount<2||!readingOptions(kind,payload.options,{expectedCount:payload.expectedCount}))return null;
    if(payload.id!==binding.questionId||!plain(payload.target)||payload.target.skill!=='reading')return null;
    const ref=strictReadingRef(input.sourceRevisionRef);if(!ref.valid||(payload.target.sourceId&&payload.target.sourceId!==ref.value.sourceId))return null;
    return deepFreeze({id:input.id,skill:input.skill,status:input.status,profiles:[...input.profiles],sourceRevisionRef:clone(ref.value),questionBinding:clone(binding),questionPayload:clone(payload)});
  }
  if(!READING_KINDS.includes(kind)||binding.schemaVersion!==1||binding.registryRevision!==READING_SINGLE_SELECT_REGISTRY_REVISION||!exact(binding.scorer||{},['id','version'])||binding.scorer.id!==READING_SINGLE_SELECT_SCORER_VERSION||binding.scorer.version!==1||!Array.isArray(binding.requiredCapabilities)||binding.requiredCapabilities.length!==3||!['keyboard','focus','screen-reader'].every(value=>binding.requiredCapabilities.includes(value))||payload.kind!==kind||!readingOptions(kind,payload.options))return null;
  if(payload.id!==binding.questionId||!plain(payload.target)||payload.target.skill!=='reading'||payload.target.profile!==input.profiles[0])return null;
  const ref=strictReadingRef(input.sourceRevisionRef);if(!ref.valid||payload.target.sourceId!==ref.value.sourceId||payload.target.sourceRevision!==ref.value.revisionId)return null;
  return deepFreeze(clone(input));
}
export function createIeltsReadingQuestionOwnerAdapter(options={}){
  if(!plain(options))throw failure('QUESTION_ACTIVITY_OWNER_RESOLVER_REQUIRED','Reading owner adapter requires verified inventory and source readers.');let descriptors;try{descriptors=Object.getOwnPropertyDescriptors(options);}catch{throw failure('QUESTION_ACTIVITY_OWNER_RESOLVER_REQUIRED','Reading owner adapter requires verified inventory and source readers.');}
  if(!exact(options,['readVerifiedInventory','readVerifiedSource'])||!hasOwn(descriptors.readVerifiedInventory||{},'value')||!hasOwn(descriptors.readVerifiedSource||{},'value')||typeof descriptors.readVerifiedInventory.value!=='function'||typeof descriptors.readVerifiedSource.value!=='function')throw failure('QUESTION_ACTIVITY_OWNER_RESOLVER_REQUIRED','Reading owner adapter requires verified inventory and source readers.');
  const adapter=Object.freeze({});OWNER_ADAPTERS.add(adapter);READING_OWNER_READERS.set(adapter,Object.freeze({readVerifiedInventory:descriptors.readVerifiedInventory.value,readVerifiedSource:descriptors.readVerifiedSource.value}));return adapter;
}
export async function adaptIeltsReadingObjectiveItem(input,sourceRevisionRef,options={}){
  if(!plain(options)){throw failure('QUESTION_ACTIVITY_ID_INVALID','Reading question options are invalid.');}let descriptors;try{descriptors=Object.getOwnPropertyDescriptors(options);}catch{throw failure('QUESTION_ACTIVITY_ID_INVALID','Reading question options are invalid.');}if(Object.keys(descriptors).some(key=>!['id','promptRevision','ownerAdapter'].includes(key))||Object.values(descriptors).some(descriptor=>!hasOwn(descriptor,'value')))throw failure('QUESTION_ACTIVITY_ID_INVALID','Reading question options are invalid.');const ownerAdapter=descriptors.ownerAdapter?.value,id=descriptors.id?.value??null,promptRevision=descriptors.promptRevision?.value??null;
  const candidate=readingInventory(input),reference=strictReadingRef(sourceRevisionRef),readers=READING_OWNER_READERS.get(ownerAdapter);if(!candidate)throw failure('QUESTION_ACTIVITY_ITEM_INVALID','Reading inventory item is invalid.');if(!reference.valid)throw failure('QUESTION_ACTIVITY_SOURCE_INVALID','Reading questions require an exact immutable owner source.');if(!OWNER_ADAPTERS.has(ownerAdapter)||!readers)throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Reading questions require a module-created owner adapter.');
  let owned,source;try{[owned,source]=await Promise.all([readers.readVerifiedInventory(candidate.id),readers.readVerifiedSource(reference.value)]);}catch{throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Reading owner reader is unavailable.');}const canonical=readingInventory(owned);if(!canonical||learningContractDigest(canonical)!==learningContractDigest(candidate))throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Reading inventory must exactly match its verified owner record.');if(!dataOnly(source)||!plain(source)||source.status!=='verified'||(source.profile&&source.profile!==canonical.profiles[0])||source.id!==reference.value.locator.passageId||source.revision!==reference.value.locator.revision||(source.sourceRevisionRef&&source.sourceRevisionRef.sourceId!==reference.value.sourceId)||(source.sourceRevisionRef&&source.sourceRevisionRef.revisionId!==reference.value.revisionId)||(source.sourceRevisionRef&&source.sourceRevisionRef.integrity!==reference.value.integrity))throw failure('QUESTION_ACTIVITY_SOURCE_BINDING_MISMATCH','Reading source is unavailable, stale, or profile-mismatched.');
  if(id!=null&&!stringValue(id,180)||promptRevision!=null&&!stringValue(promptRevision,240))throw failure('QUESTION_ACTIVITY_ID_INVALID','Reading question identity is invalid.');
  if(canonical.questionBinding.kind===READING_MULTIPLE_CHOICE_MULTIPLE_KIND){
    const publicItem=canonical.questionPayload;
    const expectedCount=publicItem.expectedCount;
    let sealedOptions=source.objectiveItems?.find?.(value=>plain(value)&&value.inventoryId===canonical.id)?.options;
    let correctOptionIds=null,rationales={};
    if(Array.isArray(sealedOptions)){
      correctOptionIds=sealedOptions.filter(o=>o.correct).map(o=>o.id);
      for(const o of sealedOptions){if(o.rationale)rationales[o.id]=o.rationale;}
    } else {
      const allIds=publicItem.options.map(o=>o.id);
      const getCombos=(arr,k)=>{
        if(k===0)return[[]];
        if(!arr.length)return[];
        const head=arr[0],tail=arr.slice(1);
        return[...getCombos(tail,k-1).map(c=>[head,...c]),...getCombos(tail,k)];
      };
      const combos=getCombos(allIds,expectedCount);
      for(const combo of combos){
        if(learningContractDigest({correctOptionIds:combo})===canonical.questionBinding.keyDigest||learningContractDigest(combo)===canonical.questionBinding.keyDigest){
          correctOptionIds=combo;break;
        }
      }
      if(!correctOptionIds)correctOptionIds=publicItem.options.filter(o=>o.correct).map(o=>o.id);
    }
    const question=deepFreeze({schema:QUESTION_ACTIVITY_SCHEMA,version:1,registryRevision:READING_MULTI_SELECT_REGISTRY_REVISION,kind:READING_MULTIPLE_CHOICE_MULTIPLE_KIND,id:clean(id,180)||canonical.questionBinding.questionId,item:{id:publicItem.id,kind:publicItem.kind,prompt:publicItem.prompt,expectedCount,options:publicItem.options.map(o=>({id:o.id,text:o.text})),target:clone(publicItem.target)},sourceRevisionRef:clone(reference.value),promptRevision:clean(promptRevision,240)||canonical.questionBinding.promptRevision,promptDigest:canonical.questionBinding.promptDigest,keyRevision:canonical.questionBinding.keyRevision,keyDigest:canonical.questionBinding.keyDigest,rubricRevision:canonical.questionBinding.rubricRevision,rubricDigest:canonical.questionBinding.rubricDigest,reviewPolicyRevision:'objective-reading-review-v1',scorer:{id:OBJECTIVE_MULTI_SELECT_SCORER_VERSION,version:1},requiredCapabilities:['keyboard','focus','screen-reader'],approval:'owner-verified'});
    AUTHENTIC_QUESTIONS.set(question,Object.freeze({item:deepFreeze({inventoryId:canonical.id,correctOptionIds,rationales,expectedCount,options:publicItem.options.map(o=>({id:o.id,text:o.text})),target:clone(publicItem.target)}),sourceRevisionRef:deepFreeze(clone(reference.value)),keyDigest:canonical.questionBinding.keyDigest,promptDigest:canonical.questionBinding.promptDigest,rubricDigest:canonical.questionBinding.rubricDigest,registryRevision:READING_MULTI_SELECT_REGISTRY_REVISION,publicDigest:learningContractDigest(question),ownerAdapter,ownerInventoryDigest:learningContractDigest(canonical),ownerSourceDigest:learningContractDigest(source),reading:true}));
    return question;
  }
  const sealed=source.objectiveItems?.find?.(value=>plain(value)&&value.inventoryId===canonical.id);if(!sealed||!readingOptions(canonical.questionBinding.kind,sealed.options,{sealed:true})||canonical.questionPayload.options.length!==sealed.options.length||canonical.questionPayload.options.some((option,index)=>option.id!==sealed.options[index].id||option.text!==sealed.options[index].text))throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Reading source does not contain an exact owner-sealed question.');const publicItem=canonical.questionPayload,keyDigest=learningContractDigest(sealed.options.map(option=>({id:option.id,correct:option.correct}))),promptDigest=learningContractDigest({item:publicItem,sourceRevisionRef:reference.value}),rubricDigest=learningContractDigest({rationales:sealed.options.map(option=>({id:option.id,rationale:option.rationale})),reviewPolicy:'objective-reading-review-v1'});
  if(canonical.questionBinding.keyDigest!==keyDigest)throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Reading sealed key binding does not match owner inventory.');if(canonical.questionBinding.promptDigest!==promptDigest)throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Reading prompt binding does not match owner inventory.');if(canonical.questionBinding.rubricDigest!==rubricDigest)throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Reading sealed rubric binding does not match owner inventory.');
  const question=deepFreeze({schema:QUESTION_ACTIVITY_SCHEMA,version:1,registryRevision:READING_SINGLE_SELECT_REGISTRY_REVISION,kind:canonical.questionBinding.kind,id:clean(id,180)||canonical.questionBinding.questionId,item:clone(publicItem),sourceRevisionRef:clone(reference.value),promptRevision:clean(promptRevision,240)||canonical.questionBinding.promptRevision,promptDigest,keyRevision:canonical.questionBinding.keyRevision,keyDigest,rubricRevision:canonical.questionBinding.rubricRevision,rubricDigest,reviewPolicyRevision:'objective-reading-review-v1',scorer:{id:READING_SINGLE_SELECT_SCORER_VERSION,version:1},requiredCapabilities:['keyboard','focus','screen-reader'],approval:'owner-verified'});AUTHENTIC_QUESTIONS.set(question,Object.freeze({item:deepFreeze({inventoryId:canonical.id,options:clone(sealed.options),target:clone(publicItem.target)}),sourceRevisionRef:deepFreeze(clone(reference.value)),keyDigest,promptDigest,rubricDigest,registryRevision:READING_SINGLE_SELECT_REGISTRY_REVISION,publicDigest:learningContractDigest(question),ownerAdapter,ownerInventoryDigest:learningContractDigest(canonical),ownerSourceDigest:learningContractDigest(source),reading:true}));return question;
}

const QUESTION_FIELDS=['schema','version','registryRevision','kind','id','item','sourceRevisionRef','promptRevision','promptDigest','keyRevision','keyDigest','rubricRevision','rubricDigest','reviewPolicyRevision','scorer','requiredCapabilities','approval'];
export function validateQuestionActivity(input={}){
  if(!bounded(input)||!plain(input))return{valid:false,errors:['Question activity has unsupported, missing, or oversized data.'],value:null};
  if(input.schema===QUESTION_ACTIVITY_SCHEMA&&Number.isInteger(input.version)&&input.version>QUESTION_ACTIVITY_VERSION&&!(input.kind===LISTENING_MULTIPLE_CHOICE_KIND&&input.version===2))return{valid:false,errors:['Future question schema is preserved but unexecutable.'],value:deepFreeze(clone(input)),future:true};
  if(OBJECTIVE_TEXT_RESPONSE_KINDS.includes(input.kind))return validateObjectiveTextQuestion(input);
  if(OBJECTIVE_MATCHING_RESPONSE_KINDS.includes(input.kind))return validateObjectiveMatchingQuestion(input);
  if(input.kind===LISTENING_MULTIPLE_CHOICE_KIND||input.kind===LISTENING_MULTIPLE_CHOICE_MULTIPLE_KIND)return input.version===2?validateListeningInventoryQuestion(input):validateListeningQuestion(input);
  if(READING_KINDS.includes(input.kind)||input.kind===READING_MULTIPLE_CHOICE_MULTIPLE_KIND)return validateReadingQuestion(input);
  const errors=[];if(!exact(input,QUESTION_FIELDS))errors.push('Question activity has unsupported or missing fields.');
  if(input.schema!==QUESTION_ACTIVITY_SCHEMA||input.version!==QUESTION_ACTIVITY_VERSION)errors.push('Question activity schema is unsupported.');
  if(input.registryRevision!==QUESTION_REGISTRY_REVISION)errors.push('Question activity registry revision is unsupported.');if(input.kind!==IELTS_LAB_CHOICE_KIND)errors.push('Question activity kind is unsupported.');if(!['id','promptRevision','promptDigest','keyRevision','keyDigest','rubricRevision','rubricDigest','reviewPolicyRevision','registryRevision'].every(key=>stringValue(input[key],240)))errors.push('Question activity immutable identity bindings are invalid.');
  const item=input.item;if(!plain(item)||!exact(item,['id','kind','prompt','context','options','sourceCardIds','createdAt','updatedAt'])||!stringValue(item.id,180)||!['paraphrase','distractor'].includes(item.kind)||!stringValue(item.prompt,2000)||typeof item.context!=='string'||!Array.isArray(item.sourceCardIds)||item.sourceCardIds.length===0||!item.sourceCardIds.every(value=>stringValue(value,180))||new Set(item.sourceCardIds).size!==item.sourceCardIds.length||!Number.isFinite(item.createdAt)||!Number.isFinite(item.updatedAt)||item.updatedAt<item.createdAt||!Array.isArray(item.options)||item.options.length<2||!item.options.every(option=>exact(option,['id','text'])&&stringValue(option.id,100)&&stringValue(option.text,2000))||new Set(item.options.map(option=>option.id)).size!==item.options.length||new Set(item.options.map(option=>normalizeComparableText(option.text))).size!==item.options.length)errors.push('Question activity portable item is invalid.');
  if(!strictCoreSourceRef(input.sourceRevisionRef).valid)errors.push('Question activity requires a bounded immutable Core SourceRevisionRef.');if(!exact(input.scorer,['id','version'])||input.scorer.id!==QUESTION_SCORER_VERSION||input.scorer.version!==1)errors.push('Question activity scorer is unsupported.');const caps=Array.isArray(input.requiredCapabilities)?input.requiredCapabilities:[];if(caps.length!==3||new Set(caps).size!==3||!['keyboard','focus','screen-reader'].every(cap=>caps.includes(cap)))errors.push('Question activity requires keyboard, focus, and screen-reader capability.');if(input.approval!=='owner-verified')errors.push('Question activity is not owner verified.');
  return{valid:errors.length===0,errors,value:errors.length?null:Object.freeze(clone(input))};
}
function validateObjectiveTextQuestion(input){
  const errors=[],item=input.item,entry=(()=>{try{return objectiveTextResponse.objectiveTextResponseRegistryEntry(input.kind);}catch{return null;}})();
  const spatial=input.kind==='reading-diagram-label-completion',fields=[...QUESTION_FIELDS,'normalizer'],registry=spatial?'qar-objective-spatial-text-response-registry-v1':OBJECTIVE_TEXT_RESPONSE_REGISTRY_REVISION,itemFields=spatial?['id','kind','prompt','slots','spatialPrompt','target','createdAt','updatedAt']:['id','kind','prompt','slots','target','createdAt','updatedAt'];
  if(!entry||!exact(input,fields)||input.schema!==QUESTION_ACTIVITY_SCHEMA||input.version!==1||input.registryRevision!==registry||!['id','promptRevision','promptDigest','keyRevision','keyDigest','rubricRevision','rubricDigest','reviewPolicyRevision'].every(key=>stringValue(input[key],240))||input.reviewPolicyRevision!==objectiveTextResponse.OBJECTIVE_TEXT_RESPONSE_REVIEW_POLICY||input.approval!=='owner-verified')errors.push('Objective text question identity is invalid.');
  if(!plain(item)||!exact(item,itemFields)||!stringValue(item.id,180)||item.kind!==input.kind||!stringValue(item.prompt,4000)||!Array.isArray(item.slots)||!item.slots.length||!item.slots.every(row=>exact(row,['id','label','wordLimit'])&&stringValue(row.id,180)&&stringValue(row.label,1000)&&Number.isSafeInteger(row.wordLimit)&&row.wordLimit>=1)||new Set(item.slots.map(row=>row.id)).size!==item.slots.length||spatial&&!canonicalSpatialPrompt(item.spatialPrompt,item.slots.map(row=>row.id))||!plain(item.target)||item.target.schemaVersion!==2||item.target.targetType!=='ielts-objective-item'||item.target.cardId!==null||item.target.senseId!==null||item.target.skill!==(input.kind.startsWith('listening-')?'listening':'reading')||!Number.isSafeInteger(item.createdAt)||!Number.isSafeInteger(item.updatedAt)||item.updatedAt<item.createdAt)errors.push('Objective text public item is invalid.');
  if(!bounded(input.sourceRevisionRef)||!plain(input.sourceRevisionRef)||item?.target?.sourceId!==input.sourceRevisionRef?.sourceId||item?.target?.sourceRevision!==input.sourceRevisionRef?.revisionId)errors.push('Objective text source binding is invalid.');
  if(!exact(input.normalizer,['id','version'])||input.normalizer.id!==OBJECTIVE_TEXT_RESPONSE_NORMALIZER.id||input.normalizer.version!==1||!exact(input.scorer,['id','version'])||input.scorer.id!==OBJECTIVE_TEXT_RESPONSE_SCORER.id||input.scorer.version!==1||!Array.isArray(input.requiredCapabilities)||input.requiredCapabilities.length!==entry?.capabilities.length||new Set(input.requiredCapabilities).size!==input.requiredCapabilities.length||!entry?.capabilities.every(cap=>input.requiredCapabilities.includes(cap)))errors.push('Objective text scorer or capabilities are invalid.');
  return{valid:errors.length===0,errors,value:errors.length?null:deepFreeze(clone(input))};
}
function validateObjectiveMatchingQuestion(input){
  const errors=[],item=input.item,entry=(()=>{try{return objectiveMatchingResponse.objectiveMatchingResponseRegistryEntry(input.kind);}catch{return null;}})(),spatial=input.kind==='listening-plan-map-diagram-labelling',fields=[...QUESTION_FIELDS,'normalizer'],registry=spatial?'qar-objective-spatial-matching-response-registry-v1':OBJECTIVE_MATCHING_RESPONSE_REGISTRY_REVISION,itemFields=spatial?['id','kind','prompt','slots','options','reusePolicy','spatialPrompt','target','createdAt','updatedAt']:['id','kind','prompt','slots','options','reusePolicy','target','createdAt','updatedAt'];
  if(!entry||!exact(input,fields)||input.schema!==QUESTION_ACTIVITY_SCHEMA||input.version!==1||input.registryRevision!==registry||!['id','promptRevision','promptDigest','keyRevision','keyDigest','rubricRevision','rubricDigest','reviewPolicyRevision'].every(key=>stringValue(input[key],240))||input.reviewPolicyRevision!==objectiveMatchingResponse.OBJECTIVE_MATCHING_RESPONSE_REVIEW_POLICY||input.approval!=='owner-verified')errors.push('Objective matching question identity is invalid.');
  if(!plain(item)||!exact(item,itemFields)||!stringValue(item.id,180)||item.kind!==input.kind||!stringValue(item.prompt,4000)||!Array.isArray(item.slots)||!item.slots.length||!item.slots.every(row=>exact(row,['id','label'])&&stringValue(row.id,180)&&stringValue(row.label,1000))||new Set(item.slots.map(row=>row.id)).size!==item.slots.length||!Array.isArray(item.options)||!item.options.length||!item.options.every(row=>exact(row,['id','label'])&&stringValue(row.id,180)&&stringValue(row.label,1000))||new Set(item.options.map(row=>row.id)).size!==item.options.length||new Set(item.options.map(row=>row.label)).size!==item.options.length||!['SINGLE_USE','ALLOW_REUSE'].includes(item.reusePolicy)||spatial&&!canonicalSpatialPrompt(item.spatialPrompt,item.slots.map(row=>row.id))||!plain(item.target)||item.target.schemaVersion!==2||item.target.targetType!=='ielts-objective-item'||item.target.cardId!==null||item.target.senseId!==null||item.target.skill!==(input.kind.startsWith('listening-')?'listening':'reading')||!Number.isSafeInteger(item.createdAt)||!Number.isSafeInteger(item.updatedAt)||item.updatedAt<item.createdAt)errors.push('Objective matching public item is invalid.');
  if(!bounded(input.sourceRevisionRef)||!plain(input.sourceRevisionRef)||item?.target?.sourceId!==input.sourceRevisionRef?.sourceId||item?.target?.sourceRevision!==input.sourceRevisionRef?.revisionId)errors.push('Objective matching source binding is invalid.');
  if(!exact(input.normalizer,['id','version'])||input.normalizer.id!==OBJECTIVE_MATCHING_RESPONSE_NORMALIZER.id||input.normalizer.version!==1||!exact(input.scorer,['id','version'])||input.scorer.id!==OBJECTIVE_MATCHING_RESPONSE_SCORER.id||input.scorer.version!==1||!Array.isArray(input.requiredCapabilities)||input.requiredCapabilities.length!==entry?.capabilities.length||new Set(input.requiredCapabilities).size!==input.requiredCapabilities.length||!entry?.capabilities.every(cap=>input.requiredCapabilities.includes(cap)))errors.push('Objective matching scorer or capabilities are invalid.');
  return{valid:errors.length===0,errors,value:errors.length?null:deepFreeze(clone(input))};
}
function validateListeningQuestion(input){
  const errors=[];
  if(input.kind===LISTENING_MULTIPLE_CHOICE_MULTIPLE_KIND){
    if(!exact(input,QUESTION_FIELDS)||input.schema!==QUESTION_ACTIVITY_SCHEMA||input.version!==1||input.registryRevision!==LISTENING_MULTI_SELECT_REGISTRY_REVISION||!['id','promptRevision','promptDigest','keyRevision','keyDigest','rubricRevision','rubricDigest','reviewPolicyRevision'].every(key=>stringValue(input[key],240))||input.reviewPolicyRevision!=='objective-listening-review-v1'||input.approval!=='owner-verified')errors.push('Listening multi question identity is invalid.');
    const item=input.item,anchor=item?.sourceAnchor;
    if(!plain(item)||!exact(item,['id','kind','prompt','expectedCount','options','target','sourceAnchor','createdAt','updatedAt'])||!stringValue(item.id,180)||item.kind!==LISTENING_MULTIPLE_CHOICE_MULTIPLE_KIND||!stringValue(item.prompt,2000)||!Number.isSafeInteger(item.expectedCount)||item.expectedCount<2||!Array.isArray(item.options)||item.options.length<item.expectedCount||!item.options.every(row=>exact(row,['id','text'])&&stringValue(row.id,100)&&stringValue(row.text,2000))||new Set(item.options.map(row=>row.id)).size!==item.options.length||new Set(item.options.map(row=>normalizeComparableText(row.text))).size!==item.options.length||!exact(item.target||{},['cardId','senseId','skill'])||!stringValue(item.target?.cardId,180)||(item.target?.senseId!==null&&!stringValue(item.target?.senseId,180))||item.target?.skill!=='listening'||!exact(anchor||{},['sourceId','revisionId','integrity','segmentIds','startMs','endMs'])||!stringValue(anchor?.sourceId,240)||!stringValue(anchor?.revisionId,500)||!stringValue(anchor?.integrity,240)||!Array.isArray(anchor?.segmentIds)||!anchor.segmentIds.length||!anchor.segmentIds.every(id=>stringValue(id,500))||new Set(anchor?.segmentIds).size!==anchor?.segmentIds?.length||!Number.isFinite(anchor?.startMs)||!Number.isFinite(anchor?.endMs)||anchor.startMs<0||anchor.endMs<=anchor.startMs||!Number.isFinite(item.createdAt)||!Number.isFinite(item.updatedAt)||item.updatedAt<item.createdAt)errors.push('Listening multi public item is invalid.');
    const ref=strictTranscriptRef(input.sourceRevisionRef);if(!ref.valid||ref.value.sourceId!==anchor?.sourceId)errors.push('Listening transcript binding is invalid.');if(!exact(input.scorer,['id','version'])||input.scorer.id!==OBJECTIVE_MULTI_SELECT_SCORER_VERSION||input.scorer.version!==1)errors.push('Listening multi scorer is invalid.');const caps=Array.isArray(input.requiredCapabilities)?input.requiredCapabilities:[];if(caps.length!==4||new Set(caps).size!==4||!['audio-playback','focus','keyboard','screen-reader'].every(cap=>caps.includes(cap)))errors.push('Listening capability binding is invalid.');return{valid:errors.length===0,errors,value:errors.length?null:deepFreeze(clone(input))};
  }
  if(!exact(input,QUESTION_FIELDS)||input.schema!==QUESTION_ACTIVITY_SCHEMA||input.version!==1||input.registryRevision!==LISTENING_MULTIPLE_CHOICE_REGISTRY_REVISION||!['id','promptRevision','promptDigest','keyRevision','keyDigest','rubricRevision','rubricDigest','reviewPolicyRevision'].every(key=>stringValue(input[key],240))||input.reviewPolicyRevision!=='objective-listening-review-v1'||input.approval!=='owner-verified')errors.push('Listening question identity is invalid.');
  const item=input.item,anchor=item?.sourceAnchor;if(!plain(item)||!exact(item,['id','kind','prompt','options','target','sourceAnchor','createdAt','updatedAt'])||!stringValue(item.id,180)||item.kind!==LISTENING_MULTIPLE_CHOICE_KIND||!stringValue(item.prompt,2000)||!Array.isArray(item.options)||item.options.length<2||!item.options.every(row=>exact(row,['id','text'])&&stringValue(row.id,100)&&stringValue(row.text,2000))||new Set(item.options.map(row=>row.id)).size!==item.options.length||new Set(item.options.map(row=>normalizeComparableText(row.text))).size!==item.options.length||!exact(item.target||{},['cardId','senseId','skill'])||!stringValue(item.target?.cardId,180)||(item.target?.senseId!==null&&!stringValue(item.target?.senseId,180))||item.target?.skill!=='listening'||!exact(anchor||{},['sourceId','revisionId','integrity','segmentIds','startMs','endMs'])||!stringValue(anchor?.sourceId,240)||!stringValue(anchor?.revisionId,500)||!stringValue(anchor?.integrity,240)||!Array.isArray(anchor?.segmentIds)||!anchor.segmentIds.length||!anchor.segmentIds.every(id=>stringValue(id,500))||new Set(anchor?.segmentIds).size!==anchor?.segmentIds?.length||!Number.isFinite(anchor?.startMs)||!Number.isFinite(anchor?.endMs)||anchor.startMs<0||anchor.endMs<=anchor.startMs||!Number.isFinite(item.createdAt)||!Number.isFinite(item.updatedAt)||item.updatedAt<item.createdAt)errors.push('Listening public item is invalid.');
  const ref=strictTranscriptRef(input.sourceRevisionRef);if(!ref.valid||ref.value.sourceId!==anchor?.sourceId||ref.value.revisionId!==anchor?.revisionId||ref.value.integrity!==anchor?.integrity)errors.push('Listening transcript binding is invalid.');if(!exact(input.scorer,['id','version'])||input.scorer.id!==LISTENING_MULTIPLE_CHOICE_SCORER_VERSION||input.scorer.version!==1)errors.push('Listening scorer is invalid.');const caps=Array.isArray(input.requiredCapabilities)?input.requiredCapabilities:[];if(caps.length!==4||new Set(caps).size!==4||!['audio-playback','focus','keyboard','screen-reader'].every(cap=>caps.includes(cap)))errors.push('Listening capability binding is invalid.');return{valid:errors.length===0,errors,value:errors.length?null:deepFreeze(clone(input))};
}
function validateListeningInventoryQuestion(input){
  const errors=[],item=input.item,anchor=item?.sourceAnchor,ref=strictTranscriptRef(input.sourceRevisionRef),target=item?.target;
  if(!exact(input,QUESTION_FIELDS)||input.schema!==QUESTION_ACTIVITY_SCHEMA||input.version!==2||input.registryRevision!==LISTENING_MULTIPLE_CHOICE_INVENTORY_REGISTRY_REVISION||!['id','promptRevision','promptDigest','keyRevision','keyDigest','rubricRevision','rubricDigest','reviewPolicyRevision'].every(key=>stringValue(input[key],240))||input.reviewPolicyRevision!=='objective-listening-review-v1'||input.approval!=='owner-verified')errors.push('Listening inventory question identity is invalid.');
  if(!plain(item)||!exact(item,['id','kind','prompt','options','target','sourceAnchor','createdAt','updatedAt'])||!stringValue(item.id,180)||item.kind!==LISTENING_MULTIPLE_CHOICE_KIND||!stringValue(item.prompt,2000)||!Array.isArray(item.options)||item.options.length<2||!item.options.every(row=>exact(row,['id','text'])&&stringValue(row.id,100)&&stringValue(row.text,2000))||new Set(item.options.map(row=>row.id)).size!==item.options.length||new Set(item.options.map(row=>normalizeComparableText(row.text))).size!==item.options.length||!exact(target||{},['schemaVersion','targetType','targetId','cardId','senseId','skill','sourceId','sourceRevision'])||target.schemaVersion!==2||target.targetType!=='ielts-objective-item'||!stringValue(target.targetId,240)||target.cardId!==null||target.senseId!==null||target.skill!=='listening'||target.sourceId!==ref.value?.sourceId||target.sourceRevision!==ref.value?.revisionId||!exact(anchor||{},['sourceId','revisionId','integrity','segmentIds','startMs','endMs'])||anchor.sourceId!==ref.value?.sourceId||anchor.revisionId!==ref.value?.revisionId||anchor.integrity!==ref.value?.integrity||!Array.isArray(anchor.segmentIds)||!anchor.segmentIds.length||!anchor.segmentIds.every(value=>stringValue(value,500))||new Set(anchor.segmentIds).size!==anchor.segmentIds.length||!Number.isFinite(anchor.startMs)||!Number.isFinite(anchor.endMs)||anchor.startMs<0||anchor.endMs<=anchor.startMs||!Number.isFinite(item.createdAt)||!Number.isFinite(item.updatedAt)||item.updatedAt<item.createdAt)errors.push('Listening inventory public item is invalid.');
  if(!ref.valid)errors.push('Listening inventory Transcript binding is invalid.');if(!exact(input.scorer,['id','version'])||input.scorer.id!==LISTENING_MULTIPLE_CHOICE_SCORER_VERSION||input.scorer.version!==1)errors.push('Listening inventory scorer is invalid.');const caps=Array.isArray(input.requiredCapabilities)?input.requiredCapabilities:[];if(caps.length!==4||new Set(caps).size!==4||!['audio-playback','focus','keyboard','screen-reader'].every(cap=>caps.includes(cap)))errors.push('Listening inventory capability binding is invalid.');return{valid:errors.length===0,errors,value:errors.length?null:deepFreeze(clone(input))};
}
function validateReadingQuestion(input){
  const errors=[];const item=input.item,ref=strictReadingRef(input.sourceRevisionRef),kind=input.kind;
  if(kind===READING_MULTIPLE_CHOICE_MULTIPLE_KIND){
    if(!exact(input,QUESTION_FIELDS)||input.schema!==QUESTION_ACTIVITY_SCHEMA||input.version!==1||input.registryRevision!==READING_MULTI_SELECT_REGISTRY_REVISION||!['id','promptRevision','promptDigest','keyRevision','keyDigest','rubricRevision','rubricDigest','reviewPolicyRevision'].every(key=>stringValue(input[key],240))||input.reviewPolicyRevision!=='objective-reading-review-v1'||input.approval!=='owner-verified')errors.push('Reading multi question identity is invalid.');
    if(!plain(item)||!exact(item,['id','kind','prompt','expectedCount','options','target'])||!stringValue(item.id,180)||item.kind!==kind||!stringValue(item.prompt,2000)||!Number.isSafeInteger(item.expectedCount)||item.expectedCount<2||!readingOptions(kind,item.options,{expectedCount:item.expectedCount})||!plain(item.target)||item.target.skill!=='reading')errors.push('Reading multi public item is invalid.');
    if(!ref.valid)errors.push('Reading multi source binding is invalid.');
    if(!exact(input.scorer,['id','version'])||input.scorer.id!==OBJECTIVE_MULTI_SELECT_SCORER_VERSION||input.scorer.version!==1)errors.push('Reading multi scorer is invalid.');
    const caps=Array.isArray(input.requiredCapabilities)?input.requiredCapabilities:[];if(caps.length!==3||new Set(caps).size!==3||!['keyboard','focus','screen-reader'].every(cap=>caps.includes(cap)))errors.push('Reading multi capability binding is invalid.');
    return{valid:errors.length===0,errors,value:errors.length?null:deepFreeze(clone(input))};
  }
  if(!exact(input,QUESTION_FIELDS)||input.schema!==QUESTION_ACTIVITY_SCHEMA||input.version!==1||input.registryRevision!==READING_SINGLE_SELECT_REGISTRY_REVISION||!['id','promptRevision','promptDigest','keyRevision','keyDigest','rubricRevision','rubricDigest','reviewPolicyRevision'].every(key=>stringValue(input[key],240))||input.reviewPolicyRevision!=='objective-reading-review-v1'||input.approval!=='owner-verified')errors.push('Reading question identity is invalid.');
  if(!plain(item)||!exact(item,['id','kind','prompt','options','target','createdAt','updatedAt'])||!stringValue(item.id,180)||item.kind!==kind||!stringValue(item.prompt,2000)||!readingOptions(kind,item.options)||!plain(item.target)||!exact(item.target,['skill','profile','sourceId','sourceRevision'])||item.target.skill!=='reading'||!['academic','general-training'].includes(item.target.profile)||!stringValue(item.target.sourceId,240)||!stringValue(item.target.sourceRevision,500)||!Number.isFinite(item.createdAt)||!Number.isFinite(item.updatedAt)||item.updatedAt<item.createdAt)errors.push('Reading public item is invalid.');
  if(!ref.valid||ref.value.sourceId!==item?.target?.sourceId||ref.value.revisionId!==item?.target?.sourceRevision)errors.push('Reading source binding is invalid.');
  if(!exact(input.scorer,['id','version'])||input.scorer.id!==READING_SINGLE_SELECT_SCORER_VERSION||input.scorer.version!==1)errors.push('Reading scorer is invalid.');const caps=Array.isArray(input.requiredCapabilities)?input.requiredCapabilities:[];if(caps.length!==3||new Set(caps).size!==3||!['keyboard','focus','screen-reader'].every(cap=>caps.includes(cap)))errors.push('Reading capability binding is invalid.');return{valid:errors.length===0,errors,value:errors.length?null:deepFreeze(clone(input))};
}
function authentic(question){if(objectiveTextResponse.isObjectiveTextResponseQuestion(question)){objectiveTextResponse.assertObjectiveTextResponseOwnerCurrent(question);return{objectiveTextResponse:true,item:question.item};}if(objectiveMatchingResponse.isObjectiveMatchingResponseQuestion(question)){objectiveMatchingResponse.assertObjectiveMatchingResponseOwnerCurrent(question);return{objectiveMatchingResponse:true,item:question.item};}const data=AUTHENTIC_QUESTIONS.get(question);if(!data||learningContractDigest(question)!==data.publicDigest||learningContractDigest(question.sourceRevisionRef)!==learningContractDigest(data.sourceRevisionRef)||question.promptDigest!==data.promptDigest||question.keyDigest!==data.keyDigest||question.rubricDigest!==data.rubricDigest||question.registryRevision!==data.registryRevision)throw failure('QUESTION_ACTIVITY_FORGED','Only an authentic module-created question can execute or score.');return data;}
export function normalizeQuestionResponse(question,response={}){try{const validation=validateQuestionActivity(question);if(!validation.valid)return{valid:false,errors:validation.errors,value:null};if(objectiveTextResponse.isObjectiveTextResponseQuestion(question)){const value=objectiveTextResponse.normalizeObjectiveTextResponseSubmission(question,response);return{valid:true,errors:[],value};}if(objectiveMatchingResponse.isObjectiveMatchingResponseQuestion(question)){const value=objectiveMatchingResponse.normalizeObjectiveMatchingResponseSubmission(question,response);return{valid:true,errors:[],value};}if(question.kind===LISTENING_MULTIPLE_CHOICE_MULTIPLE_KIND||question.kind===READING_MULTIPLE_CHOICE_MULTIPLE_KIND){if(!plain(response)||!exact(response,['optionIds'])||!Array.isArray(response.optionIds)||response.optionIds.length!==question.item.expectedCount||!response.optionIds.every(id=>stringValue(id,100)&&question.item.options.some(opt=>opt.id===id))||new Set(response.optionIds).size!==response.optionIds.length)return{valid:false,errors:['Multi-select response must contain exactly the expected number of unique known optionIds.'],value:null};return{valid:true,errors:[],value:Object.freeze({optionIds:Object.freeze([...response.optionIds].sort())})};}const data=authentic(question);if(!exact(response,['optionId'])||!stringValue(response.optionId,180)||!data.item.options.some(option=>option.id===response.optionId))return{valid:false,errors:['Question response must contain exactly one known optionId.'],value:null};return{valid:true,errors:[],value:Object.freeze({optionId:response.optionId})};}catch(cause){return{valid:false,errors:[cause.code||'Question is invalid.'],value:null};}}
export function scoreQuestionActivity(question,response={}){if(objectiveTextResponse.isObjectiveTextResponseQuestion(question))return objectiveTextResponse.scoreObjectiveTextResponse(question,response);if(objectiveMatchingResponse.isObjectiveMatchingResponseQuestion(question))return objectiveMatchingResponse.scoreObjectiveMatchingResponse(question,response);const normalized=normalizeQuestionResponse(question,response);if(!normalized.valid)return Object.freeze({valid:false,errors:normalized.errors,reviewRequired:true,affectsSchedule:false});const data=authentic(question);if(question.kind===LISTENING_MULTIPLE_CHOICE_MULTIPLE_KIND||question.kind===READING_MULTIPLE_CHOICE_MULTIPLE_KIND){const correctIds=data.item.correctOptionIds||data.item.options?.filter?.(o=>o.correct)?.map?.(o=>o.id)||[];const correctSet=new Set(correctIds);const K=normalized.value.optionIds.filter(id=>correctSet.has(id)).length;const N=question.item.expectedCount;const rawScore=K/N;const disposition=K===N?'correct':K>0?'partial':'wrong';return Object.freeze({valid:true,normalizedResponse:normalized.value,disposition,rawScore,numerator:K,denominator:N,scorer:{id:question.scorer.id,version:1},reviewRequired:false,affectsSchedule:false,keyDigest:data.keyDigest});}const selected=data.item.options.find(option=>option.id===normalized.value.optionId);return Object.freeze({valid:true,normalizedResponse:normalized.value,disposition:selected.correct?'correct':'wrong',numerator:selected.correct?1:0,denominator:1,scorer:{id:question.scorer.id,version:1},reviewRequired:false,affectsSchedule:false,keyDigest:data.keyDigest});}

export const LISTENING_MULTI_SELECT_ENTRY=Object.freeze({kind:LISTENING_MULTIPLE_CHOICE_MULTIPLE_KIND,version:1,registryRevision:LISTENING_MULTI_SELECT_REGISTRY_REVISION,scorer:OBJECTIVE_MULTI_SELECT_SCORER_VERSION,capabilities:Object.freeze(['audio-playback','focus','keyboard','screen-reader'])});
export const READING_MULTI_SELECT_ENTRY=Object.freeze({kind:READING_MULTIPLE_CHOICE_MULTIPLE_KIND,version:1,registryRevision:READING_MULTI_SELECT_REGISTRY_REVISION,scorer:OBJECTIVE_MULTI_SELECT_SCORER_VERSION,capabilities:Object.freeze(['keyboard','focus','screen-reader'])});
const ENTRY=Object.freeze({kind:IELTS_LAB_CHOICE_KIND,version:QUESTION_ACTIVITY_VERSION,registryRevision:QUESTION_REGISTRY_REVISION,scorer:QUESTION_SCORER_VERSION,capabilities:Object.freeze(['keyboard','focus','screen-reader'])});
const LISTENING_ENTRY=Object.freeze({kind:LISTENING_MULTIPLE_CHOICE_KIND,version:1,registryRevision:LISTENING_MULTIPLE_CHOICE_REGISTRY_REVISION,scorer:LISTENING_MULTIPLE_CHOICE_SCORER_VERSION,capabilities:Object.freeze(['audio-playback','focus','keyboard','screen-reader'])});
const LISTENING_INVENTORY_ENTRY=Object.freeze({kind:LISTENING_MULTIPLE_CHOICE_KIND,version:2,registryRevision:LISTENING_MULTIPLE_CHOICE_INVENTORY_REGISTRY_REVISION,scorer:LISTENING_MULTIPLE_CHOICE_SCORER_VERSION,capabilities:Object.freeze(['audio-playback','focus','keyboard','screen-reader'])});
const READING_ENTRIES=Object.freeze(READING_KINDS.map(kind=>Object.freeze({kind,version:1,registryRevision:READING_SINGLE_SELECT_REGISTRY_REVISION,scorer:READING_SINGLE_SELECT_SCORER_VERSION,capabilities:Object.freeze(['keyboard','focus','screen-reader'])})));
const OBJECTIVE_TEXT_ENTRIES=Object.freeze(OBJECTIVE_TEXT_RESPONSE_KINDS.map(kind=>objectiveTextResponse.objectiveTextResponseRegistryEntry(kind)));
const OBJECTIVE_MATCHING_ENTRIES=Object.freeze(OBJECTIVE_MATCHING_RESPONSE_KINDS.map(kind=>objectiveMatchingResponse.objectiveMatchingResponseRegistryEntry(kind)));
const entryFor=(kind,version)=>[ENTRY,LISTENING_ENTRY,LISTENING_INVENTORY_ENTRY,LISTENING_MULTI_SELECT_ENTRY,...READING_ENTRIES,READING_MULTI_SELECT_ENTRY,...OBJECTIVE_TEXT_ENTRIES,...OBJECTIVE_MATCHING_ENTRIES].find(entry=>entry.kind===kind&&entry.version===version)||null;
export function createQuestionRegistry(){const executors=new Map();const registry=Object.freeze({supports(question){const valid=validateQuestionActivity(question);const entry=valid.valid&&entryFor(valid.value.kind,valid.value.version);return Boolean(entry&&valid.value.registryRevision===entry.registryRevision);},registerExecutor(kind,version,capabilities=[]){if(!stringValue(kind,120)||!Number.isInteger(version)||!Array.isArray(capabilities)||!dataOnly(capabilities)||!capabilities.every(value=>stringValue(value,120)))throw failure('QUESTION_ACTIVITY_UNSUPPORTED','Question kind/version is unsupported.');const entry=entryFor(kind,version);const key=`${kind}@${version}`;if(!entry)throw failure('QUESTION_ACTIVITY_UNSUPPORTED','Question kind/version is unsupported.');const values=capabilities;if(values.length!==entry.capabilities.length||new Set(values).size!==values.length||!entry.capabilities.every(value=>values.includes(value)))throw failure('QUESTION_ACTIVITY_CAPABILITY_MISMATCH','Executor must declare exactly the owner capability contract.');if(executors.has(key))throw failure('QUESTION_ACTIVITY_DUPLICATE_EXECUTOR','Question executor already registered.');executors.set(key,Object.freeze([...values].sort()));return()=>executors.delete(key);},hasExecutor(question){const valid=validateQuestionActivity(question);return valid.valid&&executors.has(`${valid.value.kind}@${valid.value.version}`);}});QUESTION_REGISTRIES.add(registry);return registry;}
function assertFrozenQuestionBinding(activity,question,score){const launch=activity?.launch;const evaluation=activity?.evaluationBinding;if(!plain(launch)||launch.promptRevision!==question.promptRevision||launch.configRevision!==question.registryRevision||launch.configDigest!==question.promptDigest||!plain(evaluation)||evaluation.applicable!==true||evaluation.revision!==question.registryRevision||evaluation.keyRevision!==question.keyRevision||evaluation.keyDigest!==score.keyDigest||evaluation.rubricRevision!==question.rubricRevision||evaluation.rubricDigest!==question.rubricDigest||evaluation.scoringPolicyRevision!==question.scorer.id||evaluation.reviewPolicyRevision!==question.reviewPolicyRevision)throw failure('QUESTION_ACTIVITY_FROZEN_BINDING_MISMATCH','Question content, key, scorer, rubric, review policy, and registry must match the planned Frozen Run binding.');}
function assertQuestionRegistry(question,registry){if(!QUESTION_REGISTRIES.has(registry))throw failure('QUESTION_ACTIVITY_REGISTRY_REQUIRED','Question activity requires a module-created registry.');if(!registry.supports(question))throw failure('QUESTION_ACTIVITY_UNSUPPORTED','Question kind/version is unsupported by the registry.');if(!registry.hasExecutor(question))throw failure('QUESTION_ACTIVITY_EXECUTOR_UNREGISTERED','Question executor capability is not registered.');}
function publicResult(score){const value={normalizedResponse:score.normalizedResponse,disposition:score.disposition,numerator:score.numerator,denominator:score.denominator,reviewRequired:score.reviewRequired,scorer:score.scorer,keyDigest:score.keyDigest,affectsSchedule:false};if(score.normalizer){value.normalizer=score.normalizer;value.slots=score.slots;value.wordLimits=score.normalizedResponse.slots.map((slot,index)=>({slotId:slot.slotId,wordLimit:score.slots[index]?.wordLimit??null}));}return Object.freeze(value);}
async function executeObjectiveTextResponseActivity({activity,question,response,sourceRegistry,questionRegistry,assistance,now}){
  const validation=validateQuestionActivity(question);if(!validation.valid)throw failure('QUESTION_ACTIVITY_TEXT_DEFINITION_INVALID',validation.errors.join(' '));const score=await objectiveTextResponse.scoreObjectiveTextResponseAsync(question,response);if(!score.valid){const code=score.errors[0];if(['QUESTION_ACTIVITY_OWNER_UNAVAILABLE','QUESTION_ACTIVITY_OWNER_CHANGED'].includes(code))throw failure(code,code);throw failure('QUESTION_ACTIVITY_RESPONSE_INVALID',score.errors.join(' '));}assertFrozenQuestionBinding(activity,question,score);assertQuestionRegistry(question,questionRegistry);const collector=question.kind.startsWith('listening-')?LISTENING_ASSISTANCE_COLLECTION_MODE:READING_ASSISTANCE_COLLECTION_MODE;if(activity?.assistanceCollectionMode!==collector)throw failure('QUESTION_ACTIVITY_ASSISTANCE_MODE_INVALID','QAR requires the exact frozen assistance collector.');const trace=assertQarTrace(assistance||createAssistanceTrace({id:`trace:${activity?.id||'unbound'}`,schemaVersion:1,collector,complete:false,events:[]}),collector),target=activity?.activitySpec?.target;
  if(!plain(target)||learningContractDigest(target)!==learningContractDigest(question.item.target))throw failure('QUESTION_ACTIVITY_TARGET_BINDING_MISMATCH','Objective text ActivitySpec target must exactly match its owner target.');const source=await validateSourceRevisionForExecution({activitySpec:activity?.activitySpec,sourceRevisionRef:question.sourceRevisionRef,registry:sourceRegistry});if(source.code!=='RESOLVED')throw Object.assign(failure('QUESTION_ACTIVITY_SOURCE_BINDING_MISMATCH','Objective text source is not executable.'),{sourceCode:source.code});const started=await startTodayRun(activity,{now}),row=started.run,raw=JSON.stringify(response),responseDigest=learningContractDigest(raw),attemptId=`qar-attempt:${row.id}:${responseDigest}`,receiptId=`qar-receipt:${row.id}:${responseDigest}`,result=publicResult(score);
  if(started.terminal){const attempt=row.envelope?.attempt,receipt=row.envelope?.receipt,expectedMeta={normalizedResponse:score.normalizedResponse,normalizer:score.normalizer,scorer:score.scorer,keyDigest:score.keyDigest,slots:score.slots,wordLimits:question.item.slots.map(slot=>({slotId:slot.id,wordLimit:slot.wordLimit})),totals:{numerator:score.numerator,denominator:score.denominator}};if(attempt?.id!==attemptId||receipt?.id!==receiptId||attempt?.learnerOutput!==raw||attempt?.receiptId!==receiptId||receipt?.status!=='completed'||learningContractDigest(attempt?.assistance)!==learningContractDigest(trace)||learningContractDigest(attempt?.metadata?.questionResult)!==learningContractDigest(result)||learningContractDigest(receipt?.metadata?.questionResult)!==learningContractDigest(result)||learningContractDigest(attempt?.metadata?.objectiveTextResponse)!==learningContractDigest(expectedMeta)||learningContractDigest(receipt?.metadata?.objectiveTextResponse)!==learningContractDigest(expectedMeta))throw failure('QUESTION_ACTIVITY_TERMINAL_RESPONSE_CONFLICT','Today run already has a different terminal question response.');return Object.freeze({run:row,score:deepFreeze(clone(attempt.metadata.questionResult)),feedback:null,decision:deepFreeze(clone(row.evidenceDecision))});}
  const metadata={questionResult:result,objectiveTextResponse:{normalizedResponse:score.normalizedResponse,normalizer:score.normalizer,scorer:score.scorer,keyDigest:score.keyDigest,slots:score.slots,wordLimits:question.item.slots.map(slot=>({slotId:slot.id,wordLimit:slot.wordLimit})),totals:{numerator:score.numerator,denominator:score.denominator}}};const attempt=createAttempt({id:attemptId,run:row.canonicalRun,activitySpec:row.activitySpec,receiptId,result:score.disposition,target:row.activitySpec.target,learnerOutput:raw,assistance:trace,occurredAt:Number(now),timezone:row.activitySpec.timezone,metadata}),receipt=createReceipt({id:receiptId,run:row.canonicalRun,activitySpec:row.activitySpec,attempt,status:'completed',issuedAt:Number(now),timezone:row.activitySpec.timezone,metadata}),verification={source:{id:`source:${question.sourceRevisionRef.revisionId}`,authority:question.sourceRevisionRef.authority,status:'unverified',sourceId:question.sourceRevisionRef.sourceId,sourceRevision:question.sourceRevisionRef.revisionId}};const run=await recordTodayReceipt(row.id,{activitySpec:row.activitySpec,run:row.canonicalRun,attempt,receipt,verification},{status:'completed',now});return Object.freeze({run,score:result,feedback:null,decision:run.evidenceDecision});
}
async function executeObjectiveMatchingResponseActivity({activity,question,response,sourceRegistry,questionRegistry,assistance,now}){
  const validation=validateQuestionActivity(question);if(!validation.valid)throw failure('QUESTION_ACTIVITY_MATCHING_DEFINITION_INVALID',validation.errors.join(' '));const score=await objectiveMatchingResponse.scoreObjectiveMatchingResponseAsync(question,response);if(!score.valid){const code=score.errors[0];if(['QUESTION_ACTIVITY_OWNER_UNAVAILABLE','QUESTION_ACTIVITY_OWNER_CHANGED'].includes(code))throw failure(code,code);throw failure('QUESTION_ACTIVITY_RESPONSE_INVALID',score.errors.join(' '));}assertFrozenQuestionBinding(activity,question,score);assertQuestionRegistry(question,questionRegistry);const collector=question.kind.startsWith('listening-')?LISTENING_ASSISTANCE_COLLECTION_MODE:READING_ASSISTANCE_COLLECTION_MODE;if(activity?.assistanceCollectionMode!==collector)throw failure('QUESTION_ACTIVITY_ASSISTANCE_MODE_INVALID','QAR requires the exact frozen assistance collector.');const trace=assertQarTrace(assistance||createAssistanceTrace({id:`trace:${activity?.id||'unbound'}`,schemaVersion:1,collector,complete:false,events:[]}),collector),target=activity?.activitySpec?.target;
  if(!plain(target)||learningContractDigest(target)!==learningContractDigest(question.item.target))throw failure('QUESTION_ACTIVITY_TARGET_BINDING_MISMATCH','Objective matching ActivitySpec target must exactly match its owner target.');const source=await validateSourceRevisionForExecution({activitySpec:activity?.activitySpec,sourceRevisionRef:question.sourceRevisionRef,registry:sourceRegistry});if(source.code!=='RESOLVED')throw Object.assign(failure('QUESTION_ACTIVITY_SOURCE_BINDING_MISMATCH','Objective matching source is not executable.'),{sourceCode:source.code});const started=await startTodayRun(activity,{now}),row=started.run,raw=JSON.stringify(score.normalizedResponse),responseDigest=learningContractDigest(raw),attemptId=`qar-attempt:${row.id}:${responseDigest}`,receiptId=`qar-receipt:${row.id}:${responseDigest}`,result=Object.freeze({normalizedResponse:score.normalizedResponse,disposition:score.disposition,numerator:score.numerator,denominator:score.denominator,reviewRequired:false,affectsSchedule:false,normalizer:score.normalizer,scorer:score.scorer,keyDigest:score.keyDigest,slots:score.slots,reusePolicy:score.reusePolicy,optionPoolDigest:score.optionPoolDigest,answerBindingDigest:score.answerBindingDigest});
  const matchingMeta={normalizedResponse:score.normalizedResponse,normalizer:score.normalizer,scorer:score.scorer,keyDigest:score.keyDigest,slots:score.slots,reusePolicy:score.reusePolicy,optionPoolDigest:score.optionPoolDigest,answerBindingDigest:score.answerBindingDigest,totals:{numerator:score.numerator,denominator:score.denominator}};
  if(started.terminal){const attempt=row.envelope?.attempt,receipt=row.envelope?.receipt;if(attempt?.id!==attemptId||receipt?.id!==receiptId||attempt?.learnerOutput!==raw||attempt?.receiptId!==receiptId||receipt?.status!=='completed'||learningContractDigest(attempt?.assistance)!==learningContractDigest(trace)||learningContractDigest(attempt?.metadata?.questionResult)!==learningContractDigest(result)||learningContractDigest(receipt?.metadata?.questionResult)!==learningContractDigest(result)||learningContractDigest(attempt?.metadata?.objectiveMatchingResponse)!==learningContractDigest(matchingMeta)||learningContractDigest(receipt?.metadata?.objectiveMatchingResponse)!==learningContractDigest(matchingMeta))throw failure('QUESTION_ACTIVITY_TERMINAL_RESPONSE_CONFLICT','Today run already has a different terminal question response.');return Object.freeze({run:row,score:deepFreeze(clone(attempt.metadata.questionResult)),feedback:null,decision:deepFreeze(clone(row.evidenceDecision))});}
  const metadata={questionResult:result,objectiveMatchingResponse:matchingMeta},attempt=createAttempt({id:attemptId,run:row.canonicalRun,activitySpec:row.activitySpec,receiptId,result:score.disposition,target:row.activitySpec.target,learnerOutput:raw,assistance:trace,occurredAt:Number(now),timezone:row.activitySpec.timezone,metadata}),receipt=createReceipt({id:receiptId,run:row.canonicalRun,activitySpec:row.activitySpec,attempt,status:'completed',issuedAt:Number(now),timezone:row.activitySpec.timezone,metadata}),verification={source:{id:`source:${question.sourceRevisionRef.revisionId}`,authority:question.sourceRevisionRef.authority,status:'unverified',sourceId:question.sourceRevisionRef.sourceId,sourceRevision:question.sourceRevisionRef.revisionId}};const run=await recordTodayReceipt(row.id,{activitySpec:row.activitySpec,run:row.canonicalRun,attempt,receipt,verification},{status:'completed',now});return Object.freeze({run,score:result,feedback:null,decision:run.evidenceDecision});
}
export function getQuestionCoverageReport(){const readingSingle=READING_ENTRIES.map(entry=>({kind:entry.kind,version:1,coverage:'PARTIAL',dimensions:{schema:'PARTIAL',adapterExecutor:'PARTIAL',scoringReview:'PARTIAL',canonicalAttemptStorage:'PARTIAL',testsEvidence:'PARTIAL',uiInventory:'PARTIAL'},capabilities:entry.capabilities,limitations:'Single-selection controlled-local Reading evidence only; multi-answer, matching, completion, section timing, readiness, and full coverage remain out of scope.'}));const readingMulti={kind:READING_MULTIPLE_CHOICE_MULTIPLE_KIND,version:1,coverage:'PARTIAL',dimensions:{schema:'PARTIAL',adapterExecutor:'PARTIAL',scoringReview:'PARTIAL',canonicalAttemptStorage:'PARTIAL',testsEvidence:'PARTIAL',uiInventory:'PARTIAL'},capabilities:READING_MULTI_SELECT_ENTRY.capabilities,limitations:'Controlled-local Reading multi-select MCQ inventory, source seal, canonical execution, accessible native-select proof, and partial scoring only; section timing, assessment/readiness, and full IELTS remain out of scope.'};const objective=OBJECTIVE_TEXT_ENTRIES.map(entry=>{const implementedOtr=entry.kind.startsWith('reading-')||entry.kind.startsWith('listening-');return{kind:entry.kind,version:1,coverage:'PARTIAL',dimensions:{schema:'PARTIAL',adapterExecutor:'PARTIAL',scoringReview:'PARTIAL',canonicalAttemptStorage:'PARTIAL',testsEvidence:'PARTIAL',uiInventory:implementedOtr?'PARTIAL':'GAP',inventory:implementedOtr?'PARTIAL':'GAP',profile:implementedOtr?'PARTIAL':'GAP',durability:implementedOtr?'PARTIAL':'GAP',section:'GAP',readiness:'GAP'},capabilities:entry.capabilities,limitations:entry.kind.startsWith('reading-')?'Controlled-local Reading OTR inventory, source seal, canonical execution, and accessible text-entry proof only; matching/map/diagram, section timing, assessment/readiness, and full IELTS remain out of scope.':'Controlled-local Listening OTR inventory, exact Transcript revision, canonical execution, and accessible text-entry proof only; non-human speech/control-flow fixture, matching/map/diagram, section timing, assessment/readiness, media/type proof, and full Listening remain out of scope.'};});const matching=OBJECTIVE_MATCHING_ENTRIES.map(entry=>{const reading=entry.kind.startsWith('reading-'),listening=entry.kind==='listening-matching',implemented=reading||listening;return{kind:entry.kind,version:1,coverage:implemented?'PARTIAL':'GAP',dimensions:{schema:implemented?'PARTIAL':'GAP',adapterExecutor:implemented?'PARTIAL':'GAP',scoringReview:implemented?'PARTIAL':'GAP',canonicalAttemptStorage:implemented?'PARTIAL':'GAP',testsEvidence:implemented?'PARTIAL':'GAP',uiInventory:implemented?'PARTIAL':'GAP',inventory:implemented?'PARTIAL':'GAP',profile:implemented?'PARTIAL':'GAP',durability:implemented?'PARTIAL':'GAP',media:listening?'PARTIAL':'GAP',section:'GAP',readiness:'GAP'},capabilities:entry.capabilities,limitations:reading?'Controlled-local Reading matching inventory, source seal, canonical execution, accessible native-select proof, and durability evidence only; section timing, assessment/readiness, and full IELTS remain out of scope.':listening?'Controlled-local Listening matching inventory, exact Transcript revision, canonical execution, accessible native-select proof, and durability evidence only; mathematical tone fixture only (not human speech, real IELTS, or readiness); section timing and full Listening remain out of scope.':'Matching remains a gap.'};});const listeningSingle1={kind:LISTENING_MULTIPLE_CHOICE_KIND,version:1,coverage:'PARTIAL',dimensions:{schema:'PARTIAL',adapterExecutor:'PARTIAL',scoringReview:'PARTIAL',canonicalAttemptStorage:'PARTIAL',testsEvidence:'PARTIAL',uiInventory:'GAP'},capabilities:LISTENING_ENTRY.capabilities,limitations:'One transcript-backed multiple-choice proof only; not full Listening, readiness, or UI coverage.'};const listeningSingle2={kind:LISTENING_MULTIPLE_CHOICE_KIND,version:2,coverage:'PARTIAL',dimensions:{schema:'PARTIAL',adapterExecutor:'PARTIAL',scoringReview:'PARTIAL',canonicalAttemptStorage:'PARTIAL',testsEvidence:'PARTIAL',uiInventory:'GAP',inventory:'PARTIAL',media:'GAP',section:'GAP'},capabilities:LISTENING_INVENTORY_ENTRY.capabilities,limitations:'Inventory-backed controlled-local Transcript/tone fixture proof only; media asset, production UI, complete item inventory, section timing, additional question types, readiness, and full Listening coverage remain out of scope.'};const listeningMulti={kind:LISTENING_MULTIPLE_CHOICE_MULTIPLE_KIND,version:1,coverage:'PARTIAL',dimensions:{schema:'PARTIAL',adapterExecutor:'PARTIAL',scoringReview:'PARTIAL',canonicalAttemptStorage:'PARTIAL',testsEvidence:'PARTIAL',uiInventory:'GAP',inventory:'PARTIAL',media:'GAP',section:'GAP'},capabilities:LISTENING_MULTI_SELECT_ENTRY.capabilities,limitations:'Controlled-local Listening multi-select MCQ inventory, exact Transcript revision, canonical execution, and partial scoring only; section timing, media asset, and full Listening remain out of scope.'};return deepFreeze({registryRevision:QUESTION_REGISTRY_REVISION,kinds:[{kind:IELTS_LAB_CHOICE_KIND,version:QUESTION_ACTIVITY_VERSION,coverage:'PARTIAL',dimensions:{schema:'PARTIAL',adapterExecutor:'PARTIAL',scoringReview:'PARTIAL',canonicalAttemptStorage:'PARTIAL',testsEvidence:'PARTIAL',uiInventory:'GAP'},capabilities:ENTRY.capabilities,limitations:'Single owner-controlled IELTS Lab choice adapter; not Reading, Listening, or full IELTS coverage.'},listeningSingle1,listeningSingle2,listeningMulti,...readingSingle,readingMulti,...objective,...matching]});}
const spatialDigestEqual=(left,right)=>learningContractDigest(left)===learningContractDigest(right);
function spatialTerminalResponse(attempt,payload,binding,kind,errors){
  let raw;try{raw=JSON.parse(attempt.learnerOutput);}catch{errors.push('Spatial terminal learner output is not canonical JSON.');return null;}
  if(JSON.stringify(raw)!==attempt.learnerOutput||!exact(raw,['slots'])||!Array.isArray(raw.slots)||raw.slots.length!==payload.slots.length){errors.push('Spatial terminal learner output does not bind the exact declared slots.');return null;}
  if(kind==='listening-plan-map-diagram-labelling'){
    const seen=new Set(),options=new Set(payload.options.map(option=>option.id)),slots=[];
    for(const [index,row] of raw.slots.entries()){const declared=payload.slots[index];if(!exact(row,['slotId','optionId'])||row.slotId!==declared.id||(row.optionId!==null&&!options.has(row.optionId))||(payload.reusePolicy==='SINGLE_USE'&&row.optionId!==null&&seen.has(row.optionId))){errors.push('Spatial matching terminal response is not authentic.');return null;}if(row.optionId!==null)seen.add(row.optionId);const reason=row.optionId===null?'EMPTY':row.optionId===declared.acceptedOptionId?'MATCH':'MISMATCH';slots.push({slotId:row.slotId,disposition:reason==='MATCH'?'correct':'wrong',reason});}
    const normalized={slots:raw.slots.map(row=>({slotId:row.slotId,optionId:row.optionId}))},numerator=slots.filter(row=>row.disposition==='correct').length,optionPoolDigest=learningContractDigest(payload.options.map(({id})=>({id}))),answerBindingDigest=learningContractDigest(payload.slots.map(({id,acceptedOptionId})=>({id,acceptedOptionId}))),keyDigest=learningContractDigest({slots:payload.slots.map(({id,acceptedOptionId})=>({id,acceptedOptionId})),optionPoolDigest,reusePolicy:payload.reusePolicy,normalizer:OBJECTIVE_MATCHING_RESPONSE_NORMALIZER,scorer:OBJECTIVE_MATCHING_RESPONSE_SCORER,reviewPolicyRevision:objectiveMatchingResponse.OBJECTIVE_MATCHING_RESPONSE_REVIEW_POLICY,target:payload.target,sourceRevisionRef:payload.sourceRevisionRef,spatialPrompt:payload.spatialPrompt}),disposition=numerator===slots.length?'correct':numerator?'partial':'wrong';
    return{result:{normalizedResponse:normalized,disposition,numerator,denominator:slots.length,reviewRequired:false,affectsSchedule:false,normalizer:OBJECTIVE_MATCHING_RESPONSE_NORMALIZER,scorer:OBJECTIVE_MATCHING_RESPONSE_SCORER,keyDigest,slots,reusePolicy:payload.reusePolicy,optionPoolDigest,answerBindingDigest},meta:{normalizedResponse:normalized,normalizer:OBJECTIVE_MATCHING_RESPONSE_NORMALIZER,scorer:OBJECTIVE_MATCHING_RESPONSE_SCORER,keyDigest,slots,reusePolicy:payload.reusePolicy,optionPoolDigest,answerBindingDigest,totals:{numerator,denominator:slots.length}}};
  }
  const normalizedSlots=[],scoreSlots=[];
  for(const [index,row] of raw.slots.entries()){const declared=payload.slots[index];if(!exact(row,['slotId','text'])||row.slotId!==declared.id||typeof row.text!=='string'){errors.push('Spatial text terminal response is not authentic.');return null;}const value=objectiveTextResponse.normalizeObjectiveTextResponse(row.text),wordCount=objectiveTextResponse.countObjectiveTextWords(value),reason=!value?'EMPTY':wordCount>declared.wordLimit?'WORD_LIMIT_EXCEEDED':declared.acceptedAnswers.includes(value)?'MATCH':'MISMATCH';normalizedSlots.push({slotId:row.slotId,value,wordCount});scoreSlots.push({slotId:row.slotId,wordLimit:declared.wordLimit,disposition:reason==='MATCH'?'correct':'wrong',reason});}
  const numerator=scoreSlots.filter(row=>row.disposition==='correct').length,normalized={slots:normalizedSlots},disposition=numerator===scoreSlots.length?'correct':numerator?'partial':'wrong',keyDigest=binding.keyDigest;
  return{result:{normalizedResponse:normalized,disposition,numerator,denominator:scoreSlots.length,reviewRequired:false,scorer:OBJECTIVE_TEXT_RESPONSE_SCORER,keyDigest,affectsSchedule:false,normalizer:OBJECTIVE_TEXT_RESPONSE_NORMALIZER,slots:scoreSlots,wordLimits:payload.slots.map(slot=>({slotId:slot.id,wordLimit:slot.wordLimit}))},meta:{normalizedResponse:normalized,normalizer:OBJECTIVE_TEXT_RESPONSE_NORMALIZER,scorer:OBJECTIVE_TEXT_RESPONSE_SCORER,keyDigest,slots:scoreSlots,wordLimits:payload.slots.map(slot=>({slotId:slot.id,wordLimit:slot.wordLimit})),totals:{numerator,denominator:scoreSlots.length}}};
}
export function validatePersistedObjectiveSpatialTerminal(row,inventory){
  const errors=[];
  if(!bounded(row)||!bounded(inventory)||!plain(row)||!plain(inventory))return{valid:false,errors:['Spatial terminal or inventory is not bounded plain data.']};
  const inventoryResult=validateIeltsObjectiveInventoryItem(inventory,{historical:true});if(!inventoryResult.valid)return{valid:false,errors:['Spatial terminal inventory is not canonical.']};
  const canonicalInventory=inventoryResult.value,payload=canonicalInventory.questionPayload,binding=canonicalInventory.questionBinding,kind=binding.kind,skill=kind==='reading-diagram-label-completion'?'reading':kind==='listening-plan-map-diagram-labelling'?'listening':null;
  if(!skill||canonicalInventory.skill!==skill||payload.kind!==kind||payload.id!==canonicalInventory.id||binding.questionId!==`qar:${canonicalInventory.id}`)return{valid:false,errors:['Spatial terminal inventory ownership is mismatched.']};
  const envelope=row.envelope;if(!plain(envelope)||!plain(row.activitySpec)||!plain(row.canonicalRun)||row.status!=='completed'||row.id!==row.canonicalRun.id||!spatialDigestEqual(row.activitySpec,envelope.activitySpec)||!spatialDigestEqual(row.canonicalRun,envelope.run))return{valid:false,errors:['Spatial terminal Run envelope is inconsistent.']};
  const learning=validateLearningEnvelope(envelope);if(!learning.valid)return{valid:false,errors:['Spatial terminal learning envelope is invalid.']};
  const {activitySpec,run,attempt,receipt}=learning.value,target=payload.target;
  if(!spatialDigestEqual(activitySpec.target,target)||activitySpec.target.targetId!==canonicalInventory.id||activitySpec.target.skill!==skill||run.id!==row.id||attempt.runId!==row.id||receipt.runId!==row.id||attempt.id!==row.attemptId||receipt.id!==row.receiptId||attempt.result!==receipt.result||receipt.status!=='completed'||row.terminal?.status!=='completed'||row.terminal?.attemptId!==attempt.id||row.terminal?.receiptId!==receipt.id)errors.push('Spatial terminal Run, Attempt, Receipt, or target identities are inconsistent.');
  const frozen=run.frozenBinding,launch=frozen?.launch,evaluation=frozen?.evaluation,bound=(field,value)=>field?.state==='bound'&&field.value===value;
  if(!plain(frozen)||!spatialDigestEqual(frozen.target,target)||!bound(launch?.promptRevision,binding.promptRevision)||!bound(launch?.configRevision,binding.registryRevision)||!bound(launch?.configDigest,binding.promptDigest)||evaluation?.marker!=='applicable'||!bound(evaluation?.revision,binding.registryRevision)||!bound(evaluation?.keyRevision,binding.keyRevision)||!bound(evaluation?.keyDigest,binding.keyDigest)||!bound(evaluation?.rubricRevision,binding.rubricRevision)||!bound(evaluation?.rubricDigest,binding.rubricDigest)||!bound(evaluation?.scoringPolicyRevision,binding.scorer.id)||!bound(evaluation?.reviewPolicyRevision,binding.reviewPolicyRevision))errors.push('Spatial terminal frozen question binding is inconsistent.');
  const expected=spatialTerminalResponse(attempt,payload,binding,kind,errors);if(!expected)return{valid:false,errors};
  const metaKey=kind==='reading-diagram-label-completion'?'objectiveTextResponse':'objectiveMatchingResponse';
  if(attempt.result!==expected.result.disposition||receipt.result!==expected.result.disposition||!spatialDigestEqual(attempt.metadata?.questionResult,expected.result)||!spatialDigestEqual(receipt.metadata?.questionResult,expected.result)||!spatialDigestEqual(attempt.metadata?.[metaKey],expected.meta)||!spatialDigestEqual(receipt.metadata?.[metaKey],expected.meta))errors.push('Spatial terminal scorer, key, rubric, reuse, slot, or public-result redundancy is inconsistent.');
  const expectedDecision=decideEvidence({attempt,activity:activitySpec,verification:envelope.verification});if(!spatialDigestEqual(envelope.decision,expectedDecision)||!spatialDigestEqual(row.evidenceDecision,expectedDecision)||expectedDecision.eligible!==false||expectedDecision.affectsSchedule!==false)errors.push('Spatial terminal evidence must remain default-deny and recomputable.');
  if(row.terminal?.digest!==learningContractDigest({status:'completed',envelope}))errors.push('Spatial terminal digest does not bind the canonical terminal envelope.');
  return{valid:errors.length===0,errors};
}

const getQuestionCoverageReportBase=getQuestionCoverageReport;
getQuestionCoverageReport=function getQuestionCoverageReport(){const report=getQuestionCoverageReportBase(),kinds=report.kinds.map(row=>row.kind==='listening-plan-map-diagram-labelling'?{...row,coverage:'PARTIAL',dimensions:{...row.dimensions,schema:'PARTIAL',adapterExecutor:'PARTIAL',scoringReview:'PARTIAL',canonicalAttemptStorage:'PARTIAL',testsEvidence:'PARTIAL',uiInventory:'PARTIAL',inventory:'PARTIAL',profile:'PARTIAL',durability:'PARTIAL',media:'PARTIAL'},limitations:'Controlled-local Listening plan/map/diagram label inventory, exact Transcript revision, canonical execution, accessible native-select proof, and durability evidence only; non-human tone fixture, real IELTS, section timing, assessment/readiness and full Listening remain out of scope.'}:row);return deepFreeze({...report,kinds});};
const FEEDBACK_BINDING_FIELDS=['kind','version','questionId','activitySpecId','runId','attemptId','receiptId','sourceRevisionRef','sourceAnchor','promptRevision','keyRevision','keyDigest','rubricRevision','rubricDigest','scorer','selectedOptionId','disposition','rationale'];
const FEEDBACK_FIELDS=[...FEEDBACK_BINDING_FIELDS,'bindingDigest','stale','staleReason'];
function feedbackBinding(input){return Object.fromEntries(FEEDBACK_BINDING_FIELDS.map(key=>[key,clone(input[key])]));}
function createListeningFeedback(input){const binding=feedbackBinding(input);return deepFreeze({...binding,bindingDigest:learningContractDigest(binding),stale:false,staleReason:null});}
function createReadingFeedback(input){const fields=['kind','version','questionId','activitySpecId','runId','attemptId','receiptId','inventoryId','sourceRevisionRef','promptRevision','keyRevision','keyDigest','rubricRevision','rubricDigest','scorer','selectedOptionId','disposition','rationale'];const binding=Object.fromEntries(fields.map(key=>[key,clone(input[key])])) ;return deepFreeze({...binding,bindingDigest:learningContractDigest(binding),stale:false,staleReason:null});}
const LISTENING_INVENTORY_FEEDBACK_BINDING_FIELDS=['kind','version','questionId','activitySpecId','runId','attemptId','receiptId','inventoryId','sourceRevisionRef','sourceAnchor','inventoryOwnerDigest','transcriptOwnerDigest','promptRevision','keyRevision','keyDigest','rubricRevision','rubricDigest','scorer','selectedOptionId','disposition','rationale'];
const LISTENING_INVENTORY_FEEDBACK_FIELDS=[...LISTENING_INVENTORY_FEEDBACK_BINDING_FIELDS,'bindingDigest','stale','staleReason'];
function createListeningInventoryFeedback(input){const binding=Object.fromEntries(LISTENING_INVENTORY_FEEDBACK_BINDING_FIELDS.map(key=>[key,clone(input[key])]));return deepFreeze({...binding,bindingDigest:learningContractDigest(binding),stale:false,staleReason:null});}
const READING_FEEDBACK_BINDING_FIELDS=['kind','version','questionId','activitySpecId','runId','attemptId','receiptId','inventoryId','sourceRevisionRef','promptRevision','keyRevision','keyDigest','rubricRevision','rubricDigest','scorer','selectedOptionId','disposition','rationale'];
const READING_FEEDBACK_FIELDS=[...READING_FEEDBACK_BINDING_FIELDS,'bindingDigest','stale','staleReason'];
function readingFeedbackBinding(input){return Object.fromEntries(READING_FEEDBACK_BINDING_FIELDS.map(key=>[key,clone(input[key])]));}
function validateReadingFeedback(feedback){
  const reference=strictReadingRef(feedback?.sourceRevisionRef),attemptPrefix=`qar-attempt:${feedback?.runId}:`,receiptPrefix=`qar-receipt:${feedback?.runId}:`;
  if(!bounded(feedback)||!plain(feedback)||!exact(feedback,READING_FEEDBACK_FIELDS)||feedback.kind!=='reading-question-feedback'||feedback.version!==1||!['questionId','activitySpecId','runId','attemptId','receiptId','inventoryId','promptRevision','keyRevision','keyDigest','rubricRevision','rubricDigest','selectedOptionId','bindingDigest'].every(key=>stringValue(feedback[key],240))||feedback.runId!==`today-run:${feedback.activitySpecId}`||!feedback.attemptId.startsWith(attemptPrefix)||!feedback.receiptId.startsWith(receiptPrefix)||feedback.attemptId.slice(attemptPrefix.length)!==feedback.receiptId.slice(receiptPrefix.length)||!exact(feedback.scorer||{},['id','version'])||feedback.scorer.id!==READING_SINGLE_SELECT_SCORER_VERSION||feedback.scorer.version!==1||!['correct','wrong'].includes(feedback.disposition)||!stringValue(feedback.rationale,2000)||feedback.stale!==false||feedback.staleReason!==null||!reference.valid||feedback.bindingDigest!==learningContractDigest(readingFeedbackBinding(feedback)))throw failure('QUESTION_ACTIVITY_FEEDBACK_INVALID','Reading feedback is invalid.');
  return feedback;
}
function validateListeningFeedback(feedback){
  if(!bounded(feedback)||!plain(feedback)||!exact(feedback,FEEDBACK_FIELDS))throw failure('QUESTION_ACTIVITY_FEEDBACK_INVALID','Listening feedback is invalid.');
  const anchor=feedback.sourceAnchor,reference=strictTranscriptRef(feedback.sourceRevisionRef),attemptPrefix=`qar-attempt:${feedback.runId}:`,receiptPrefix=`qar-receipt:${feedback.runId}:`;
  if(feedback.kind!=='question-feedback'||feedback.version!==1||!['questionId','activitySpecId','runId','attemptId','receiptId','promptRevision','keyRevision','keyDigest','rubricRevision','rubricDigest','selectedOptionId','bindingDigest'].every(key=>stringValue(feedback[key],240))||feedback.runId!==`today-run:${feedback.activitySpecId}`||!feedback.attemptId.startsWith(attemptPrefix)||!feedback.receiptId.startsWith(receiptPrefix)||feedback.attemptId.slice(attemptPrefix.length)!==feedback.receiptId.slice(receiptPrefix.length)||!exact(feedback.scorer||{},['id','version'])||feedback.scorer.id!==LISTENING_MULTIPLE_CHOICE_SCORER_VERSION||feedback.scorer.version!==1||!['correct','wrong'].includes(feedback.disposition)||!stringValue(feedback.rationale,2000)||feedback.stale!==false||feedback.staleReason!==null||!reference.valid||!exact(anchor||{},['sourceId','revisionId','integrity','segmentIds','startMs','endMs'])||!stringValue(anchor?.sourceId,240)||!stringValue(anchor?.revisionId,500)||!stringValue(anchor?.integrity,240)||!Array.isArray(anchor?.segmentIds)||!anchor.segmentIds.length||!anchor.segmentIds.every(id=>stringValue(id,500))||new Set(anchor?.segmentIds).size!==anchor?.segmentIds?.length||!Number.isFinite(anchor?.startMs)||!Number.isFinite(anchor?.endMs)||anchor.startMs<0||anchor.endMs<=anchor.startMs||anchor.sourceId!==reference.value.sourceId||anchor.revisionId!==reference.value.revisionId||anchor.integrity!==reference.value.integrity||feedback.bindingDigest!==learningContractDigest(feedbackBinding(feedback)))throw failure('QUESTION_ACTIVITY_FEEDBACK_INVALID','Listening feedback is invalid.');
  return feedback;
}
function validateListeningInventoryFeedback(feedback){
  if(!bounded(feedback)||!plain(feedback)||!exact(feedback,LISTENING_INVENTORY_FEEDBACK_FIELDS))throw failure('QUESTION_ACTIVITY_FEEDBACK_INVALID','Listening inventory feedback is invalid.');const anchor=feedback.sourceAnchor,reference=strictTranscriptRef(feedback.sourceRevisionRef),attemptPrefix=`qar-attempt:${feedback.runId}:`,receiptPrefix=`qar-receipt:${feedback.runId}:`;if(feedback.kind!=='listening-inventory-question-feedback'||feedback.version!==2||!['questionId','activitySpecId','runId','attemptId','receiptId','inventoryId','inventoryOwnerDigest','transcriptOwnerDigest','promptRevision','keyRevision','keyDigest','rubricRevision','rubricDigest','selectedOptionId','bindingDigest'].every(key=>stringValue(feedback[key],240))||feedback.runId!==`today-run:${feedback.activitySpecId}`||!feedback.attemptId.startsWith(attemptPrefix)||!feedback.receiptId.startsWith(receiptPrefix)||feedback.attemptId.slice(attemptPrefix.length)!==feedback.receiptId.slice(receiptPrefix.length)||!exact(feedback.scorer||{},['id','version'])||feedback.scorer.id!==LISTENING_MULTIPLE_CHOICE_SCORER_VERSION||feedback.scorer.version!==1||!['correct','wrong'].includes(feedback.disposition)||!stringValue(feedback.rationale,2000)||feedback.stale!==false||feedback.staleReason!==null||!reference.valid||!exact(anchor||{},['sourceId','revisionId','integrity','segmentIds','startMs','endMs'])||anchor.sourceId!==reference.value.sourceId||anchor.revisionId!==reference.value.revisionId||anchor.integrity!==reference.value.integrity||!Array.isArray(anchor.segmentIds)||!anchor.segmentIds.length||!anchor.segmentIds.every(value=>stringValue(value,500))||new Set(anchor.segmentIds).size!==anchor.segmentIds.length||!Number.isFinite(anchor.startMs)||!Number.isFinite(anchor.endMs)||anchor.startMs<0||anchor.endMs<=anchor.startMs||feedback.bindingDigest!==learningContractDigest(Object.fromEntries(LISTENING_INVENTORY_FEEDBACK_BINDING_FIELDS.map(key=>[key,clone(feedback[key])]))))throw failure('QUESTION_ACTIVITY_FEEDBACK_INVALID','Listening inventory feedback is invalid.');return feedback;
}
export async function assessListeningFeedbackFreshness(feedback,options={}){
  if(feedback?.kind==='listening-inventory-question-feedback')return assessListeningInventoryFeedbackFreshness(feedback,options);
  validateListeningFeedback(feedback);
  if(!plain(options))throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Listening feedback owner is unavailable.');let descriptors;try{descriptors=Object.getOwnPropertyDescriptors(options);}catch{throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Listening feedback owner is unavailable.');}if(!exact(options,['ownerAdapter'])||!hasOwn(descriptors.ownerAdapter,'value'))throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Listening feedback owner is unavailable.');const ownerAdapter=descriptors.ownerAdapter.value;
  const readers=LISTENING_OWNER_READERS.get(ownerAdapter);if(!OWNER_ADAPTERS.has(ownerAdapter)||!readers)throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Listening feedback owner is unavailable.');let aggregate;try{aggregate=await readers.getTranscriptAggregate(feedback.sourceRevisionRef.revisionId);}catch{throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Listening feedback owner is unavailable.');}
  const safeAggregate=dataOnly(aggregate)&&plain(aggregate)&&plain(aggregate.source),activeRevisionId=safeAggregate?aggregate.source.activeRevisionId:null;const stale=!listeningAggregate(feedback.sourceAnchor,feedback.sourceRevisionRef,aggregate)||activeRevisionId!==feedback.sourceRevisionRef.revisionId;const reason=stale?(activeRevisionId&&activeRevisionId!==feedback.sourceRevisionRef.revisionId?'SOURCE_ACTIVE_REVISION_CHANGED':'SOURCE_REVISION_UNAVAILABLE'):null;return deepFreeze({...clone(feedback),stale,staleReason:reason});
}
async function assessListeningInventoryFeedbackFreshness(feedback,options={}){
  validateListeningInventoryFeedback(feedback);if(!plain(options))throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Listening feedback owner is unavailable.');let descriptors;try{descriptors=Object.getOwnPropertyDescriptors(options);}catch{throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Listening feedback owner is unavailable.');}if(!exact(options,['ownerAdapter'])||!hasOwn(descriptors.ownerAdapter,'value'))throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Listening feedback owner is unavailable.');const readers=LISTENING_INVENTORY_OWNER_READERS.get(descriptors.ownerAdapter.value);if(!OWNER_ADAPTERS.has(descriptors.ownerAdapter.value)||!readers)throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Listening feedback owner is unavailable.');let inventory,aggregate;try{[inventory,aggregate]=await Promise.all([readers.readVerifiedInventory(feedback.inventoryId),readers.getTranscriptAggregate(feedback.sourceRevisionRef.revisionId)]);}catch{throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Listening feedback owner is unavailable.');}
  const canonical=listeningInventory(inventory);if(!canonical)return deepFreeze({...clone(feedback),stale:true,staleReason:'INVENTORY_UNAVAILABLE'});const binding=canonical.questionBinding,inventoryCurrent=canonical.id===feedback.inventoryId&&learningContractDigest(canonical)===feedback.inventoryOwnerDigest&&canonical.sourceRevisionRef?.revisionId===feedback.sourceRevisionRef.revisionId&&canonical.sourceRevisionRef?.integrity===feedback.sourceRevisionRef.integrity&&binding.questionId===feedback.questionId&&binding.promptRevision===feedback.promptRevision&&binding.keyRevision===feedback.keyRevision&&binding.keyDigest===feedback.keyDigest&&binding.rubricRevision===feedback.rubricRevision&&binding.rubricDigest===feedback.rubricDigest&&binding.scorer.id===feedback.scorer.id&&binding.scorer.version===feedback.scorer.version;
  if(!inventoryCurrent)return deepFreeze({...clone(feedback),stale:true,staleReason:'INVENTORY_BINDING_CHANGED'});
  const safeAggregate=dataOnly(aggregate)&&plain(aggregate)&&plain(aggregate.source)&&plain(aggregate.revision)&&Array.isArray(aggregate.segments);if(!safeAggregate)return deepFreeze({...clone(feedback),stale:true,staleReason:'SOURCE_BINDING_CHANGED'});
  if(aggregate.source.activeRevisionId!==feedback.sourceRevisionRef.revisionId)return deepFreeze({...clone(feedback),stale:true,staleReason:'SOURCE_ACTIVE_REVISION_CHANGED'});
  if(!listeningAggregate(canonical.questionPayload.sourceAnchor,feedback.sourceRevisionRef,aggregate)||learningContractDigest(aggregate)!==feedback.transcriptOwnerDigest)return deepFreeze({...clone(feedback),stale:true,staleReason:'SOURCE_BINDING_CHANGED'});
  const selected=canonical.questionPayload.sealedOptions.find(option=>option.id===feedback.selectedOptionId);if(!selected||selected.rationale!==feedback.rationale||(selected.correct?'correct':'wrong')!==feedback.disposition)return deepFreeze({...clone(feedback),stale:true,staleReason:'INVENTORY_BINDING_CHANGED'});return deepFreeze(clone(feedback));
}
export async function assessReadingFeedbackFreshness(feedback,options={}){
  validateReadingFeedback(feedback);
  if(!plain(options))throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Reading feedback owner is unavailable.');let descriptors;try{descriptors=Object.getOwnPropertyDescriptors(options);}catch{throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Reading feedback owner is unavailable.');}
  if(!exact(options,['ownerAdapter'])||!hasOwn(descriptors.ownerAdapter||{},'value'))throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Reading feedback owner is unavailable.');const readers=READING_OWNER_READERS.get(descriptors.ownerAdapter.value);
  if(!OWNER_ADAPTERS.has(descriptors.ownerAdapter.value)||!readers)throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Reading feedback owner is unavailable.');let inventory,source;try{[inventory,source]=await Promise.all([readers.readVerifiedInventory(feedback.inventoryId),readers.readVerifiedSource(feedback.sourceRevisionRef)]);}catch{throw failure('QUESTION_ACTIVITY_FEEDBACK_UNAVAILABLE','Reading feedback owner is unavailable.');}
  const canonical=readingInventory(inventory);if(!canonical)return deepFreeze({...clone(feedback),stale:true,staleReason:'INVENTORY_UNAVAILABLE'});
  const binding=canonical.questionBinding,reference=feedback.sourceRevisionRef,inventoryCurrent=canonical.id===feedback.inventoryId&&canonical.sourceRevisionRef?.revisionId===reference.revisionId&&canonical.sourceRevisionRef?.integrity===reference.integrity&&binding.questionId===feedback.questionId&&binding.promptRevision===feedback.promptRevision&&binding.keyRevision===feedback.keyRevision&&binding.keyDigest===feedback.keyDigest&&binding.rubricRevision===feedback.rubricRevision&&binding.rubricDigest===feedback.rubricDigest&&binding.scorer?.id===feedback.scorer.id&&binding.scorer?.version===feedback.scorer.version;
  if(!inventoryCurrent)return deepFreeze({...clone(feedback),stale:true,staleReason:'INVENTORY_BINDING_CHANGED'});
  if(!dataOnly(source)||!plain(source)||source.status!=='verified'||source.profile!==canonical.profiles[0]||source.id!==reference.locator.passageId||source.revision!==reference.locator.revision||source.sourceRevisionRef?.sourceId!==reference.sourceId||source.sourceRevisionRef?.revisionId!==reference.revisionId||source.sourceRevisionRef?.integrity!==reference.integrity)return deepFreeze({...clone(feedback),stale:true,staleReason:'SOURCE_REVISION_UNAVAILABLE'});
  const sealed=source.objectiveItems?.find?.(item=>plain(item)&&item.inventoryId===canonical.id),selected=sealed?.options?.find?.(option=>plain(option)&&option.id===feedback.selectedOptionId);if(!selected||selected.rationale!==feedback.rationale||(selected.correct?'correct':'wrong')!==feedback.disposition)return deepFreeze({...clone(feedback),stale:true,staleReason:'SOURCE_BINDING_CHANGED'});
  return deepFreeze(clone(feedback));
}
export async function executeQuestionActivity({activity,question,response,sourceRegistry,questionRegistry=null,assistance=null,now=Date.now()}={}){
  if(objectiveTextResponse.isObjectiveTextResponseQuestion(question))return executeObjectiveTextResponseActivity({activity,question,response,sourceRegistry,questionRegistry,assistance,now});
  if(objectiveMatchingResponse.isObjectiveMatchingResponseQuestion(question))return executeObjectiveMatchingResponseActivity({activity,question,response,sourceRegistry,questionRegistry,assistance,now});
  const validation=validateQuestionActivity(question);if(!validation.valid)throw failure('QUESTION_ACTIVITY_INVALID',validation.errors.join(' '));const data=authentic(question);const listening=validation.value.kind===LISTENING_MULTIPLE_CHOICE_KIND,inventoryListening=data.listeningInventory===true,reading=READING_KINDS.includes(validation.value.kind);if(!listening&&!reading)assertItemSourceBinding(data.item,validation.value.sourceRevisionRef);const score=scoreQuestionActivity(question,response);if(!score.valid)throw failure('QUESTION_ACTIVITY_RESPONSE_INVALID',score.errors.join(' '));assertFrozenQuestionBinding(activity,validation.value,score);assertQuestionRegistry(validation.value,questionRegistry);const collector=listening?LISTENING_ASSISTANCE_COLLECTION_MODE:reading?READING_ASSISTANCE_COLLECTION_MODE:'ielts-lab';if(activity?.assistanceCollectionMode!==collector)throw failure('QUESTION_ACTIVITY_ASSISTANCE_MODE_INVALID','QAR requires the exact frozen assistance collector.');const trace=assertQarTrace(assistance||createAssistanceTrace({id:`trace:${activity?.id||'unbound'}`,schemaVersion:1,collector,complete:false,events:[]}),collector);
  if(listening){const target=activity?.activitySpec?.target,readers=inventoryListening?LISTENING_INVENTORY_OWNER_READERS.get(data.ownerAdapter):LISTENING_OWNER_READERS.get(data.ownerAdapter);if(inventoryListening){if(!plain(target)||!exact(target,['schemaVersion','targetType','targetId','cardId','senseId','skill','sourceId','sourceRevision'])||target.schemaVersion!==2||target.targetType!=='ielts-objective-item'||target.targetId!==data.item.inventoryId||target.cardId!==null||target.senseId!==null||target.skill!=='listening'||target.sourceId!==validation.value.sourceRevisionRef.sourceId||target.sourceRevision!==validation.value.sourceRevisionRef.revisionId)throw failure('QUESTION_ACTIVITY_TARGET_BINDING_MISMATCH','Listening ActivitySpec target must exactly match its owner inventory and Transcript.');let inventory,aggregate;try{[inventory,aggregate]=await Promise.all([readers?.readVerifiedInventory(data.item.inventoryId),readers?.getTranscriptAggregate(validation.value.sourceRevisionRef.revisionId)]);}catch{throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Listening owner is unavailable at execution.');}const canonical=listeningInventory(inventory);if(!canonical||learningContractDigest(canonical)!==data.ownerInventoryDigest)throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Listening owner inventory changed before execution.');if(!listeningAggregate(data.item.sourceAnchor,validation.value.sourceRevisionRef,aggregate)||learningContractDigest(aggregate)!==data.ownerTranscriptDigest)throw failure('QUESTION_ACTIVITY_SOURCE_BINDING_MISMATCH','Listening Transcript owner changed before execution.');}else{if(!plain(target)||target.cardId!==data.item.target.cardId||target.senseId!==data.item.target.senseId||target.skill!==data.item.target.skill||target.sourceId!==validation.value.sourceRevisionRef.sourceId||target.sourceRevision!==validation.value.sourceRevisionRef.revisionId)throw failure('QUESTION_ACTIVITY_TARGET_BINDING_MISMATCH','Listening ActivitySpec target must exactly match its Transcript-backed item.');let aggregate;try{aggregate=await readers?.getTranscriptAggregate(validation.value.sourceRevisionRef.revisionId);}catch{throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Listening owner is unavailable at execution.');}if(!listeningAggregate(data.item.sourceAnchor,validation.value.sourceRevisionRef,aggregate))throw failure('QUESTION_ACTIVITY_SOURCE_BINDING_MISMATCH','Listening owner state changed before execution.');}}
  if(reading){const target=activity?.activitySpec?.target,readers=READING_OWNER_READERS.get(data.ownerAdapter);if(!plain(target)||!exact(target,['schemaVersion','targetType','targetId','cardId','senseId','skill','sourceId','sourceRevision'])||target.schemaVersion!==2||target.targetType!=='ielts-objective-item'||target.targetId!==data.item.inventoryId||target.cardId!==null||target.senseId!==null||target.skill!=='reading'||target.sourceId!==validation.value.sourceRevisionRef.sourceId||target.sourceRevision!==validation.value.sourceRevisionRef.revisionId)throw failure('QUESTION_ACTIVITY_TARGET_BINDING_MISMATCH','Reading ActivitySpec target must exactly match its owner inventory and source.');let inventory,source;try{[inventory,source]=await Promise.all([readers?.readVerifiedInventory(data.item.inventoryId),readers?.readVerifiedSource(validation.value.sourceRevisionRef)]);}catch{throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Reading owner is unavailable at execution.');}const canonical=readingInventory(inventory);if(!canonical||learningContractDigest(canonical)!==data.ownerInventoryDigest)throw failure('QUESTION_ACTIVITY_OWNER_VERIFICATION_REQUIRED','Reading owner inventory changed before execution.');if(!dataOnly(source)||!plain(source)||source.status!=='verified'||source.profile!==data.item.target.profile||source.id!==validation.value.sourceRevisionRef.locator.passageId||source.revision!==validation.value.sourceRevisionRef.locator.revision||source.sourceRevisionRef?.sourceId!==validation.value.sourceRevisionRef.sourceId||source.sourceRevisionRef?.revisionId!==validation.value.sourceRevisionRef.revisionId||source.sourceRevisionRef?.integrity!==validation.value.sourceRevisionRef.integrity)throw failure('QUESTION_ACTIVITY_SOURCE_BINDING_MISMATCH','Reading owner source changed before execution.');const sealed=Array.isArray(source.objectiveItems)?source.objectiveItems.find(value=>plain(value)&&value.inventoryId===data.item.inventoryId):null;if(!sealed||!readingOptions(validation.value.kind,sealed.options,{sealed:true})||learningContractDigest(sealed.options)!==learningContractDigest(data.item.options)||learningContractDigest(source)!==data.ownerSourceDigest)throw failure('QUESTION_ACTIVITY_SOURCE_BINDING_MISMATCH','Reading owner source binding changed before execution.');}
  const source=await validateSourceRevisionForExecution({activitySpec:activity?.activitySpec,sourceRevisionRef:validation.value.sourceRevisionRef,registry:sourceRegistry});if(source.code!=='RESOLVED'||((listening||reading)&&(source.provenance?.verification!=='verified'||source.provenance?.rights!=='allowed')))throw Object.assign(failure('QUESTION_ACTIVITY_SOURCE_UNAVAILABLE','Question source is not executable.'),{sourceCode:source.code});
  const started=await startTodayRun(activity,{now});const row=started.run;const responseDigest=learningContractDigest(score.normalizedResponse);const attemptId=`qar-attempt:${row.id}:${responseDigest}`,receiptId=`qar-receipt:${row.id}:${responseDigest}`;const result=publicResult(score);const selected=data.item.options.find(option=>option.id===score.normalizedResponse.optionId);const feedback=inventoryListening?createListeningInventoryFeedback({kind:'listening-inventory-question-feedback',version:2,questionId:validation.value.id,activitySpecId:row.activitySpec.id,runId:row.canonicalRun.id,attemptId,receiptId,inventoryId:data.item.inventoryId,sourceRevisionRef:clone(validation.value.sourceRevisionRef),sourceAnchor:clone(data.item.sourceAnchor),inventoryOwnerDigest:data.ownerInventoryDigest,transcriptOwnerDigest:data.ownerTranscriptDigest,promptRevision:validation.value.promptRevision,keyRevision:validation.value.keyRevision,keyDigest:validation.value.keyDigest,rubricRevision:validation.value.rubricRevision,rubricDigest:validation.value.rubricDigest,scorer:clone(validation.value.scorer),selectedOptionId:selected.id,disposition:score.disposition,rationale:selected.rationale}):listening?createListeningFeedback({kind:'question-feedback',version:1,questionId:validation.value.id,activitySpecId:row.activitySpec.id,runId:row.canonicalRun.id,attemptId,receiptId,sourceRevisionRef:clone(validation.value.sourceRevisionRef),sourceAnchor:clone(data.item.sourceAnchor),promptRevision:validation.value.promptRevision,keyRevision:validation.value.keyRevision,keyDigest:validation.value.keyDigest,rubricRevision:validation.value.rubricRevision,rubricDigest:validation.value.rubricDigest,scorer:clone(validation.value.scorer),selectedOptionId:selected.id,disposition:score.disposition,rationale:selected.rationale}):reading?createReadingFeedback({kind:'reading-question-feedback',version:1,questionId:validation.value.id,activitySpecId:row.activitySpec.id,runId:row.canonicalRun.id,attemptId,receiptId,inventoryId:data.item.inventoryId,sourceRevisionRef:clone(validation.value.sourceRevisionRef),promptRevision:validation.value.promptRevision,keyRevision:validation.value.keyRevision,keyDigest:validation.value.keyDigest,rubricRevision:validation.value.rubricRevision,rubricDigest:validation.value.rubricDigest,scorer:clone(validation.value.scorer),selectedOptionId:selected.id,disposition:score.disposition,rationale:selected.rationale}):null;
  if(started.terminal){const storedAttempt=row.envelope?.attempt,storedReceipt=row.envelope?.receipt,storedFeedback=storedAttempt?.metadata?.feedback;if(storedAttempt?.id!==attemptId||storedReceipt?.id!==receiptId||learningContractDigest(storedAttempt?.assistance)!==learningContractDigest(trace)||learningContractDigest(storedAttempt?.metadata?.questionResult)!==learningContractDigest(result)||learningContractDigest(storedReceipt?.metadata?.questionResult)!==learningContractDigest(result)||feedback&&(learningContractDigest(storedFeedback)!==learningContractDigest(feedback)||learningContractDigest(storedReceipt?.metadata?.feedback)!==learningContractDigest(feedback)))throw failure('QUESTION_ACTIVITY_TERMINAL_RESPONSE_CONFLICT','Today run already has a different terminal question response.');return Object.freeze({run:row,score:deepFreeze(clone(storedAttempt.metadata.questionResult)),feedback:feedback?deepFreeze(clone(storedFeedback)):null,decision:deepFreeze(clone(row.evidenceDecision))});}
  const metadata=feedback?{questionResult:result,feedback}:{questionResult:result};const attempt=createAttempt({id:attemptId,run:row.canonicalRun,activitySpec:row.activitySpec,receiptId,result:score.disposition,target:row.activitySpec.target,learnerOutput:score.normalizedResponse.optionId,assistance:trace,occurredAt:Number(now),timezone:row.activitySpec.timezone,metadata});const receipt=createReceipt({id:receiptId,run:row.canonicalRun,activitySpec:row.activitySpec,attempt,status:'completed',issuedAt:Number(now),timezone:row.activitySpec.timezone,metadata});const verification={source:{id:`source:${validation.value.sourceRevisionRef.revisionId}`,authority:validation.value.sourceRevisionRef.authority,status:'unverified',sourceId:validation.value.sourceRevisionRef.sourceId,sourceRevision:validation.value.sourceRevisionRef.revisionId}};const run=await recordTodayReceipt(row.id,{activitySpec:row.activitySpec,run:row.canonicalRun,attempt,receipt,verification},{status:'completed',now});return Object.freeze({run,score:result,feedback,decision:run.evidenceDecision});
}
function assertQarTrace(input,collector='ielts-lab'){
  const flags={reveal:'revealed',hint:'hintUsed','transcript-view':'transcriptViewed','correction-exposed':'correctionExposed'};const keys=['kind','schemaVersion','id','collector','complete','revealed','hintUsed','transcriptViewed','correctionExposed','retryAfterExposure','coaching','answerExposed','events'];
  if(!bounded(input)||!exact(input,keys)||input.kind!=='AssistanceTrace'||input.schemaVersion!==1||!stringValue(input.id,180)||input.collector!==collector||typeof input.complete!=='boolean'||!['revealed','hintUsed','transcriptViewed','correctionExposed','retryAfterExposure','coaching','answerExposed'].every(key=>typeof input[key]==='boolean')||!Array.isArray(input.events)||input.events.length>200)throw failure('QUESTION_ACTIVITY_ASSISTANCE_INVALID','QAR AssistanceTrace must be bounded canonical raw data.');
  let previous=-1;for(let index=0;index<input.events.length;index+=1){const event=input.events[index];if(!exact(event,['sequence','type','at','metadata'])||event.sequence!==index+1||!hasOwn(flags,event.type)||!Number.isFinite(event.at)||event.at<0||event.at<previous||!bounded(event.metadata)){throw failure('QUESTION_ACTIVITY_ASSISTANCE_INVALID','QAR AssistanceTrace events must be ordered and supported.');}previous=event.at;}
  if(Object.entries(flags).some(([type,key])=>input[key]!==input.events.some(event=>event.type===type))||input.retryAfterExposure||input.coaching||input.answerExposed)throw failure('QUESTION_ACTIVITY_ASSISTANCE_INVALID','QAR AssistanceTrace flags must exactly match QAR action history.');
  return completeAssistanceTrace(input);
}
export const __testing=Object.freeze({canonicalItem,dataOnly,assertFrozenQuestionBinding,strictCoreSourceRef,assertItemSourceBinding});
