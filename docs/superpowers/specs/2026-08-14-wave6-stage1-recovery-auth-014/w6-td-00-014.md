# Package Record — W6-TD-00-014

Manifest: `W6-STAGE1-RECOVERY-AUTH-014`
Protocol: `BOUNDED_EXECUTION_CAPSULE_PROTOCOL_V1`
Status: `CONDITIONAL / INACTIVE UNTIL EXACT MANIFEST ACCEPT AND R3-C4 SUCCESS`
Owner: `TD-00 Targeted Diagnostic adapter`
Writer: `W6-STAGE1-EXECUTOR-014 / ONE_WRITER_EXCLUSIVE`

## Predecessor and baseline

Exact predecessor selector: the unique immutable R3-C4 SHA produced under AUTH-014 and read back with natural exact-head SUCCESS plus artifact digests before R4-A4. No other SHA is eligible.

Dependencies: technical R1 WeaknessProfile output and technical R3 Frozen Assessment/QAR output on the same replacement branch. Dependency chaining grants no independent or package acceptance. ASM-00 remains the only durable assessment owner.

Same `codex/w6-stage1-recovery-exec-014` Draft PR. R4-A4 parent exact R3-C4; R4-B4 parent exact R4-A4; R4-C4 parent exact R4-B4. Append-only only; no second writer/reset/rebase/amend/squash/force/merge-from-main.

Baseline is exact R3-C4 natural `pull_request`/`synchronize` SUCCESS with exact run/attempt/job/head and all available verification/browser artifacts/digests read back before A4. Missing, ambiguous or contradictory baseline => STOP.

## R4-A4 — test-only allowlist

Allowed test path only:
- `tests/wave6-targeted-diagnostic.test.mjs`

No production source change. A4 must statically use only dependency modules present at R3-C4. For the new TD module, a controlled dynamic import may catch only expected `ERR_MODULE_NOT_FOUND` and convert that absence into the named product-capability assertion `Targeted Diagnostic adapter capability is absent`.

Before that capability assertion, A4 must prove healthy prerequisites:
- branded canonical WeaknessProfile v1 can be built/validated from technical R1 state;
- authentic supported QAR questions can be resolved through technical R3 state;
- Frozen Assessment runtime/owner can create/read its bounded prerequisite fixture without mutation ambiguity.

Eligible RED: all prerequisite checks are healthy, then the explicit assertion that `createTargetedDiagnosticAdapter` capability is absent fails for product capability absence.

Invalid RED: source mutation; unexpected path; unhealthy WeaknessProfile/QAR/Frozen prerequisite; uncaught module/syntax/dependency/infrastructure failure; fixture failure; wrong parent; predecessor already providing the capability; ambiguous first cause. Invalid => STOP before B4.

Immutable A4 contract guarantees (post-GREEN):
- only branded canonical WeaknessProfile v1 is accepted;
- deterministic weakness-biased selection independent of caller ordering;
- at least two sufficiently observed weak skills and at least two authentic supported QAR items per selected skill;
- forged/duplicate/hostile/unsupported question input fails before Frozen-owner mutation;
- ASM-00 remains sole durable assessment owner;
- blueprint/run/replay/backup compose through Frozen Assessment, never a second store/runtime;
- final diagnostic explicitly preserves:
  - `representative=false`
  - `bandScore=null`
  - `readiness=null`
  - `mastery=null`
  - `affectsSchedule=false`
  - `evidenceEligible=false`

## R4-B4 — source-only allowlist

Allowed source path only:
- `src/targeted-diagnostic.js`

A4 blob immutable. WeaknessProfile, Frozen Assessment, QAR, Today, FSRS and EvidencePolicy modules are read-only dependencies and may not be modified.

Minimal GREEN: one deterministic weakness-biased adapter over technical dependency shapes; no second store/runtime/scheduler; no schedule/evidence authority; no representative score/readiness/band/mastery claim.

Focused verification command:
```bash
node --test tests/wave6-targeted-diagnostic.test.mjs tests/progress.test.mjs tests/wave6-frozen-assessment.test.mjs
```
plus every applicable shared PR GREEN gate from `.github/workflows/ci.yml`.

Natural B4 exact-head workflow MUST conclude SUCCESS. Any failure, including infrastructure failure, => STOP / new authority.

## R4-C4 — evidence-only allowlist

Allowed evidence path only:
- `docs/superpowers/specs/2026-08-14-wave6-stage1-exec-014/w6-td-00-014-evidence-v1.md`

A4/B4 blobs immutable. C4 natural exact-head workflow MUST conclude SUCCESS. Evidence binds accepted AUTH-014 exact head/comment; R3-C4 predecessor; A/B/C topology/blobs; fresh RED/GREEN run/job/head/artifacts; dependency identities; overlap checks; limitations; `IMPLEMENTER_EVIDENCE / NOT_ACCEPTANCE`; package and merge authority NOT_GRANTED.

## Migration and rollback

Migration: `NONE`. TD creates no durable store/schema/version and writes only through ASM-00.

Rollback before future integration: discard/revert R4 chain. After accepted integration: remove/disable TD adapter only; Frozen Assessment rows remain valid and unrelated owner state untouched.

## STOP and acceptance

STOP on unresolved R3-C4 baseline, open overlap, second writer, wrong parent/path, source-before-valid-RED, invalid RED, A mutation, hostile/unsupported input mutating Frozen state, second store/runtime/scheduler, readiness/band/mastery/evidence/schedule authority, any B/C GREEN failure, artifact mismatch or acceptance inference.

No Ready/merge/status reconciliation. Independent final batch auditor must verdict exact R4 technical subject separately. Package acceptance and merge authority: `NOT_GRANTED_BY_IMPLEMENTER`.
`MECHANICAL_INTEGRATION_PREAUTHORIZATION: NOT_GRANTED`.
