# Package Record — W6-ASM-00-014

Manifest: `W6-STAGE1-RECOVERY-AUTH-014`
Protocol: `BOUNDED_EXECUTION_CAPSULE_PROTOCOL_V1`
Status: `CONDITIONAL / INACTIVE UNTIL EXACT MANIFEST ACCEPT`
Owner: `ASM-00 Frozen Assessment runtime & durability`
Writer: `W6-STAGE1-EXECUTOR-014 / ONE_WRITER_EXCLUSIVE`

## Predecessor and baseline

Exact technical predecessor: R2-C2 `2dc10d86a6440efe1c4d8c3dc925923a27977248`.
Baseline: natural exact-head CI #391 / run `31771748853` (SUCCESS), verification artifact `9208382868` (`sha256:970262b3abaa5b301df55669a10eb0e05fe477afbb4194fc95d897adab4f8a20`).

Branch: `codex/w6-stage1-recovery-exec-014` (Draft PR to `main`).

## R3-A4 — test-only allowlist

Allowed test path only:
- `tests/wave6-frozen-assessment.test.mjs`

No production source change.

### Test blob content reuse and commit rules
- Historical commit SHA `bd6cf73857d94d32afeaadb66a54cfa4e480fee5` MUST NOT be reused as commit identity.
- Historical CI #394 MUST NOT be reused as new execution RED acceptance evidence.
- Prospective reuse of exact test/blob **CONTENT** `1f0cf13f24f38e24247c389aad038aa708ebffb5` is explicitly permitted for R3-A4 creation, as historical CI #394 verified it correctly implements the healthy prerequisite sequence and capability absence check.
- R3-A4 must be created as a fresh test-only commit directly on top of `2dc10d86a6440efe1c4d8c3dc925923a27977248`.
- A fresh natural exact-head CI run for R3-A4 is strictly mandatory.

### Behavioral RED predicate
R3-A4 must verify healthy existing prerequisites before failing on Frozen Assessment capability absence:
1. build two authentic owner-verified QAR questions from existing canonical adapters;
2. construct canonical question registry;
3. `registerExecutor(question.kind, question.version, question.requiredCapabilities)`;
4. assert `registry.supports(question) === true`;
5. assert `registry.hasExecutor(question) === true`;
6. verify existing IELTS persistence and backup are healthy;
7. controlled-dynamic-import of new ASM modules, catching only expected `ERR_MODULE_NOT_FOUND`;
8. explicit assertion that dedicated durable Frozen Assessment owner capability is absent fails (`false !== true`).

### RED invalidation
Any failure at steps 1–6, uncaught syntax or import errors, fixture/IndexedDB/timeout/infrastructure failure, unrelated test failure, already-passing capability, wrong parent or source delta invalidates RED and requires STOP before B4.

### Immutable contract guarantees (post-GREEN)
- Dedicated `AssessmentBlueprint` and `AssessmentRun` schemas with immutable snapshots;
- exact question/order/key/scorer/source bindings from canonical QAR authority;
- atomic terminal completion and deterministic replay/conflict resolution;
- incomplete/duplicate/unknown/tampered inputs fail closed and mutate nothing;
- additive durable persistence in IELTS DB plus standalone and combined backup/restore/reopen;
- hostile accessors, prototype pollution, cycles and forged references fail closed without accessor execution;
- public output contains raw item and aggregate facts only:
  - `representative=false`
  - `bandScore=null`
  - `readiness=null`
  - `mastery=null`
  - `affectsSchedule=false`
  - `evidenceEligible=false`

## R3-B4 — source-only allowlist

Allowed source paths only:
- `src/frozen-assessment-contracts.js`
- `src/frozen-assessment-runtime.js`
- `src/question-activity-contracts.js`
- `src/ielts-domain.js`
- `src/ielts-persistence.js`
- `src/backup-registry.js`

All A4 test blobs immutable.
Minimal GREEN: single Frozen Assessment owner consuming existing QAR authority with additive persistence/backup support required by A4.

Excluded: second scheduler, second QAR registry/owner, AI key/scoring authority, FSRS/EvidencePolicy write authority, representative/full-mock/band/readiness/mastery claims.

Focused verification command:
```bash
node --test tests/wave6-frozen-assessment.test.mjs tests/qar-00-question-activity-contracts.test.mjs tests/ielts-persistence.test.mjs tests/backup-registry.test.mjs tests/migration-ledger.test.mjs tests/restore-safety.test.mjs
```
plus all shared natural PR GREEN gates from `.github/workflows/ci.yml`. Any B4 failure => STOP / new authority.

## R3-C4 — evidence-only allowlist

Allowed evidence path only:
- `docs/superpowers/specs/2026-08-14-wave6-stage1-exec-014/w6-asm-00-014-evidence-v1.md`

All A4 test and B4 source blobs immutable.
Evidence must bind: accepted AUTH-014 head and comment, R2-C2 baseline, A/B/C SHAs, parents, blobs, paths, raw RED run/job/head/artifact, exact B/C GREEN run/job/head/artifact IDs and digests, overlap preflight, migration outputs, immutable A/B verification, and authority boundaries.
Evidence authority: `IMPLEMENTER_EVIDENCE / NOT_ACCEPTANCE`.
Natural exact-head CI on R3-C4 must conclude SUCCESS before R4-A4 may proceed.

## Migration and rollback

Forward-only additive migration:
- IELTS IndexedDB `3 -> 4`;
- exactly one `frozenAssessments` store with only required indexes;
- preserve all existing stores and records;
- IELTS backup `3 -> 4`;
- combined backup schema and registry `5 -> 6`;
- legacy v5 and older envelopes upgrade with empty Frozen state where historical schema lacked it;
- restore/reopen preserves Frozen rows and unrelated owner data.
No downgrade, deletion, destructive reset or history rewrite.

Rollback before integration: discard/revert R3 chain. After accepted integration: disable producers while retaining compatible durable readers, store and backup; never downgrade DB or delete rows.

## STOP and acceptance

STOP on overlap, second writer, wrong predecessor/path, source-before-valid-RED, invalid RED, A mutation, migration ambiguity, second store/runtime/registry, unauthorized scoring/claims, B/C non-success or artifact mismatch.

Independent final batch audit required. Package acceptance and merge authority: `NOT_GRANTED_BY_IMPLEMENTER`.
`MECHANICAL_INTEGRATION_PREAUTHORIZATION: NOT_GRANTED`.
