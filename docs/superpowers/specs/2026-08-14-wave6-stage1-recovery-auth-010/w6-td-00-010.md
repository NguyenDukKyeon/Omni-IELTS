# Package Record — W6-TD-00-010

Manifest: `W6-STAGE1-RECOVERY-AUTH-010`
Protocol: `BOUNDED_EXECUTION_CAPSULE_PROTOCOL_V1`
Record status: `CONDITIONAL / INACTIVE UNTIL EXACT MANIFEST ACCEPT`
Canonical owner: `TD-00 Targeted Diagnostic` adapter, independently ratified and merged through PR #69.
Writer: `W6-STAGE1-EXECUTOR-010` exclusively.

## Exact predecessor selector / dependency / topology

Exact predecessor selector: exact immutable R3-C SHA generated under this manifest and read back before R4-A; no other SHA is eligible. Its natural exact-head SUCCESS CI/run/job/artifact identities must be frozen before A.

Dependencies: technical R1 WeaknessProfile output and R3 Frozen Assessment/QAR output on the same branch. Neither dependency grants TD independent acceptance. ASM remains the only durable assessment owner.

Branch/PR: same `codex/w6-stage1-recovery-exec-010` single Draft PR. R4-A parent = exact R3-C; R4-B parent = exact R4-A; R4-C parent = exact R4-B. No second writer/reset/rebase/amend/squash/force/merge-from-main.

## Record baseline CI / verification

Baseline is exact R3-C natural `pull_request/synchronize` CI SUCCESS on the same PR, with exact run/attempt/job/head/artifact identities read back before A. Missing/ambiguous baseline blocks R4.

## Commit A — exact allowlist

Only:
- `tests/wave6-targeted-diagnostic.test.mjs`

No production source may change.

## Natural behavioral RED predicate

A statically uses only dependency modules present at R3-C. For the new TD owner module itself, a controlled dynamic import may catch only expected absence and convert it into the named assertion `Targeted Diagnostic adapter capability is absent`. Uncaught raw module-resolution failure is invalid RED.

Eligible first RED: explicit product-capability assertion that `createTargetedDiagnosticAdapter` is unavailable while R1/R3 dependency fixtures are healthy.

Immutable A must prove after GREEN:
- only branded canonical WeaknessProfile v1 is accepted;
- deterministic weakness-biased selection independent of caller ordering;
- at least two sufficiently observed weak skills and at least two authentic supported QAR items per selected skill;
- forged/duplicate/hostile/unsupported question input fails before Frozen-owner mutation;
- ASM-00 is the only durable assessment owner;
- blueprint/run/replay/backup behavior composes through Frozen Assessment rather than a second store/runtime;
- final diagnostic remains explicitly non-representative: `representative=false`, `bandScore=null`, `readiness=null`, `mastery=null`, `affectsSchedule=false`, `evidenceEligible=false`.

RED invalidation: source mutation in A; unexpected path; uncaught module/syntax/infrastructure/dependency failure; unhealthy prerequisite fixture; wrong parent; predecessor already providing capability; ambiguous first cause. Any invalidation => STOP before B.

## Commit B — exact allowlist / minimal GREEN

Only:
- `src/targeted-diagnostic.js`

A blob unchanged. R4 may read but not modify WeaknessProfile, Frozen Assessment, QAR, Today, FSRS or EvidencePolicy modules.

Minimal GREEN: one deterministic weakness-biased adapter over technical dependency shapes; no second store/runtime/scheduler; no schedule/evidence authority; no representative score/readiness/band/mastery claim.

Focused verification:
`node --test tests/wave6-targeted-diagnostic.test.mjs tests/progress.test.mjs tests/wave6-frozen-assessment.test.mjs`
plus every shared PR GREEN gate.

## Commit C / evidence

C changes exactly:
`docs/superpowers/specs/2026-08-14-wave6-stage1-exec-010/w6-td-00-010-evidence-v1.md`

A/B blobs immutable. Evidence uses Manifest Evidence V1 and authority `IMPLEMENTER_EVIDENCE / NOT_ACCEPTANCE`.

## Migration / rollback

Migration: `NONE`. TD creates no durable store/schema/version and writes only through ASM-00.

Rollback before merge: discard/revert R4 source/test/evidence chain. After future accepted integration, remove/disable TD adapter only; Frozen Assessment rows remain valid and unrelated owner state is untouched.

## Record-specific STOP / integration

STOP on unresolved R3-C baseline, second writer, wrong parent/path, source-before-RED, invalid RED, A mutation, unsupported/hostile input mutating Frozen state, second store/runtime/scheduler, readiness/band/mastery/evidence/schedule authority, GREEN failure, artifact/digest/evidence mismatch.

No merge/Ready/status reconciliation. Independent batch auditor must issue a distinct R4 verdict. Package acceptance/merge authority are not granted by executor, prerequisite technical completion or CI.

Canonical acceptance criteria: canonical governance docs, ADR-046, merged PR #69 TD boundary, exact accepted manifest root and this record.