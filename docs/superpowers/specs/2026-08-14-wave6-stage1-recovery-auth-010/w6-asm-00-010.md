# Package Record — W6-ASM-00-010

Manifest: `W6-STAGE1-RECOVERY-AUTH-010`
Protocol: `BOUNDED_EXECUTION_CAPSULE_PROTOCOL_V1`
Record status: `CONDITIONAL / INACTIVE UNTIL EXACT MANIFEST ACCEPT`
Canonical owner: `ASM-00 Frozen Assessment`, independently ratified and merged through PR #69.
Writer: `W6-STAGE1-EXECUTOR-010` exclusively.

## Exact predecessor selector / dependency / topology

Exact predecessor selector: exact immutable R2-C SHA generated under this manifest and read back before R3-A; no other SHA is eligible. Its natural exact-head SUCCESS CI/run/job/artifact identities must be frozen before R3-A.

Dependencies: accepted canonical LI-00 and QAR-00 semantics already in the branch predecessor; R2 technical state does not grant ASM acceptance. QAR retains question normalization/scoring authority; LI retains its existing execution boundary; ASM owns only the ratified frozen multi-item aggregate/runtime/durability boundary.

Branch/PR: same `codex/w6-stage1-recovery-exec-010` single Draft PR. R3-A parent = exact R2-C; R3-B parent = exact R3-A; R3-C parent = exact R3-B. No second writer/reset/rebase/amend/squash/force/merge-from-main.

## Record baseline CI / verification

Baseline is exact R2-C natural `pull_request/synchronize` CI SUCCESS on the same PR, with exact run/attempt/job/head and artifact digests read back before A. Missing or ambiguous baseline blocks R3.

## Commit A — exact allowlist

Only:
- `tests/wave6-frozen-assessment.test.mjs`
- `tests/qar-00-question-activity-contracts.test.mjs`
- `tests/ielts-persistence.test.mjs`
- `tests/backup-registry.test.mjs`
- `tests/migration-ledger.test.mjs`
- `tests/restore-safety.test.mjs`

No production source may change.

## Natural behavioral RED predicate

Eligible first RED is a named Frozen Assessment owner assertion through modules already present at R2-C: canonical IELTS/QAR seams do not expose the dedicated Frozen Assessment durable owner plus authenticated frozen scoring snapshot required by ASM-00. The predecessor must fail by explicit capability assertion before any conditional import of new ASM modules.

Raw `ERR_MODULE_NOT_FOUND`, syntax error, IndexedDB harness failure, unrelated backup/restore failure, missing external dependency, wrong parent or source mutation is invalid RED.

Immutable A must prove after GREEN:
- immutable authenticated multi-item AssessmentBlueprint and RunSnapshot;
- exact item/order/question/key/scorer/source bindings sourced from QAR authority;
- atomic terminal completion and deterministic replay/collision handling;
- incomplete/invalid/tampered input mutates nothing;
- additive durable storage survives reopen, combined backup and restore;
- hostile accessor/cycle/symbol/sparse input fails closed without unsafe accessor execution;
- public results expose raw item/aggregate facts only;
- no EvidencePolicy/schedule/readiness/band/mastery/representativeness/AI-key authority.

RED invalidation: any source delta in A; unexpected path; raw module/syntax/infra/IndexedDB harness/unrelated baseline failure; wrong parent; assertion that already passes predecessor; ambiguous cause. Any invalidation => STOP before B.

## Commit B — exact allowlist / minimal GREEN

Only:
- `src/frozen-assessment-contracts.js`
- `src/frozen-assessment-runtime.js`
- `src/question-activity-contracts.js`
- `src/ielts-domain.js`
- `src/ielts-persistence.js`
- `src/backup-registry.js`

All A blobs unchanged.

Minimal GREEN: one Frozen Assessment owner consuming existing QAR authority, with the minimum additive persistence/backup support required. Explicitly excluded: second assessment scheduler, second QAR registry, model-generated executable keys/scoring, EvidencePolicy writes, representative/full-mock/band/readiness/mastery claims.

Focused verification:
`node --test tests/wave6-frozen-assessment.test.mjs tests/qar-00-question-activity-contracts.test.mjs tests/ielts-persistence.test.mjs tests/backup-registry.test.mjs tests/migration-ledger.test.mjs tests/restore-safety.test.mjs`
plus every shared PR GREEN gate.

## Commit C / evidence

C changes exactly:
`docs/superpowers/specs/2026-08-14-wave6-stage1-exec-010/w6-asm-00-010-evidence-v1.md`

A/B blobs remain immutable. Evidence uses Manifest Evidence V1 and authority `IMPLEMENTER_EVIDENCE / NOT_ACCEPTANCE`.

## Migration obligations

Forward-only exact additive migration:
- IELTS IndexedDB version `3 -> 4`;
- add exactly one `frozenAssessments` store and only indexes required by immutable accepted tests;
- preserve every pre-existing IELTS store/record;
- IELTS backup version `3 -> 4` where required by owner module;
- full backup schema/registry `5 -> 6`;
- supported legacy v5/older upgrade paths produce valid v6 envelope, adding empty Frozen Assessment state only where historical schema could not contain it;
- restore/reopen preserves Frozen Assessment rows and unrelated owner data.

Migration proof must appear in raw tests/CI and C evidence. No destructive reset, IndexedDB downgrade, store deletion or backup-history rewrite.

## Rollback

Before merge: discard/revert R3 chain. After future accepted integration: disable producers while keeping compatible readers/store/backup support for existing v4/v6 data. Never downgrade database versions or delete durable rows.

## Record-specific STOP / integration

STOP on unresolved R2-C baseline, second writer, wrong parent/path, source-before-RED, invalid RED, A mutation, migration/backup/restore ambiguity, GREEN failure, second runtime/store/registry, AI scoring authority, artifact/digest/evidence mismatch.

No merge/Ready/status reconciliation. Independent batch auditor must issue distinct R3 verdict. Package acceptance/merge authority are not granted by executor or CI.

Canonical acceptance criteria: canonical governance docs, ADR-046, merged PR #69 ASM boundary, exact accepted manifest root and this record.