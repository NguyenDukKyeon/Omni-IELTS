# Package Record — W6-FCS-00-01-010

Manifest: `W6-STAGE1-RECOVERY-AUTH-010`
Protocol: `BOUNDED_EXECUTION_CAPSULE_PROTOCOL_V1`
Record status: `CONDITIONAL / INACTIVE UNTIL EXACT MANIFEST ACCEPT`
Canonical owner: `P1-07 Today Composer` bounded Wave 6 Focus seam.
Writer: `W6-STAGE1-EXECUTOR-010` exclusively.

## Exact predecessor selector / dependency / topology

Exact predecessor selector: the exact immutable R1-C SHA generated under this manifest and read back before R2-A. No other SHA is eligible. The R1-C SHA, its successful natural exact-head CI and artifact identities must be frozen in R2 evidence before A.

Dependencies: technical R1 WeaknessProfile/projection output; canonical P1-07/P1-08; merged PR #69 owner authority. `P7-04` remains unactivated. R1 technical completion does not grant R2 independent acceptance.

Branch/PR: same `codex/w6-stage1-recovery-exec-010` single Draft PR. R2-A parent = exact R1-C; R2-B parent = exact R2-A; R2-C parent = exact R2-B. No second writer/reset/rebase/amend/squash/force/merge-from-main.

## Record baseline CI / verification

Baseline is the exact R1-C natural `pull_request/synchronize` CI on the same Draft PR, conclusion SUCCESS, with exact run/attempt/job/head and `verification-output` digest read back before R2-A. Missing/ambiguous baseline => R2 unauthorized.

## Commit A — exact allowlist

Only:
- `tests/wave6-focus-today.test.mjs`
- `tests/today-composer.test.mjs`
- `tests/today-containment.test.mjs`
- `scripts/browser-smoke.mjs` (test harness only)

No production source may change.

## Natural behavioral RED predicate

A must exercise Today modules already present at R1-C. Eligible first failure is a named Focus/Today assertion showing the existing `composeTodayPlan`/Today path does not yet bind exactly one authenticated Focus selection with reason `observed-weakness-focus` after all due work while preserving plan budget.

The expected predecessor result may be no Focus row or an ordinary repair/content reason; it must fail by assertion, not missing module/browser launch/network/provider/timeout/syntax/fixture failure.

Immutable A must prove after GREEN:
- deterministic candidate ranking from authentic WeaknessProfile provenance;
- at most one Focus slot;
- every due item precedes Focus and Focus cannot displace due work;
- Focus fits remaining budget;
- exact target/reason/provenance/executor/candidate-set binding is reproducible and durable through existing Today owner;
- accepted executor registry only;
- tampered binding fails before executor side effects;
- no realtime provider/AI call during Today composition;
- no second scheduler, Focus-side FSRS/evidence mutation, `P7-04` workload optimization, or `FCS-02` advisory lane.

RED invalidation: any production-source mutation in A, path outside A allowlist, module/syntax/browser-infrastructure/network/provider/timeout/unrelated baseline first failure, wrong parent, assertion passing predecessor, or ambiguous cause. Any invalidation => STOP before B.

## Commit B — exact allowlist / minimal GREEN

Only:
- `src/focus-selector.js`
- `src/today-composer.js`
- `src/today-planner-v2.js`

All A blobs unchanged.

Minimal GREEN: one deterministic evidence-backed Focus contribution to existing Today Composer; no new scheduler/store/FSRS/EvidencePolicy owner; no provider call in composition; no P7-04 activation.

Focused verification:
`node --test tests/wave6-focus-today.test.mjs tests/today-composer.test.mjs tests/today-containment.test.mjs`
and `npm run test:browser`, plus every shared PR GREEN gate.

## Commit C / evidence

C changes exactly:
`docs/superpowers/specs/2026-08-14-wave6-stage1-exec-010/w6-fcs-00-01-010-evidence-v1.md`

A/B test/source blobs unchanged. Evidence uses Manifest Evidence V1 and `IMPLEMENTER_EVIDENCE / NOT_ACCEPTANCE`.

## Migration / rollback

Migration: `NONE`. No database/store/version change. Focus payload may only use additive fields inside existing Today activity ownership.

Rollback before merge: discard/revert R2 source/test/evidence chain. After future accepted integration, rollback must leave due work, FSRS, evidence and existing Today records intact; Focus-marked activities remain read-safe or are ignored, never destructively deleted.

## Record-specific STOP / integration

STOP on unresolved R1-C baseline, second writer, wrong parent/path, source-before-RED, invalid RED, A mutation, GREEN failure, provider activity during composition, new scheduler/store, P7-04/FCS-02 activation, artifact/evidence mismatch.

No merge/Ready/status reconciliation. Independent batch auditor must verdict R2 separately. Package acceptance/merge authority are not granted by executor, prior R1 technical completion, or CI.

Canonical acceptance criteria: canonical governance docs, ADR-046, merged PR #69 owner ratification, exact accepted manifest root and this record.