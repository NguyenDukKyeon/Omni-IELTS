# Package Record — W6-ASM-00-012

Manifest: `W6-STAGE1-RECOVERY-AUTH-012`
Protocol: `BOUNDED_EXECUTION_CAPSULE_PROTOCOL_V1`
Record status: `CONDITIONAL / INACTIVE UNTIL EXACT MANIFEST ACCEPT AND R2-C2 SUCCESS`
Canonical owner: `ASM-00 Frozen Assessment`, ratified through merged PR #69.
Writer: `W6-STAGE1-EXECUTOR-012 / ONE_WRITER_EXCLUSIVE`.

## Exact predecessor selector / dependency / topology

Exact predecessor selector: the unique exact immutable R2-C2 SHA produced under AUTH-012 and read back with natural exact-head SUCCESS CI plus artifact digests before R3-A. No other SHA is eligible.

Dependencies: canonical LI-00 and QAR-00 semantics present in the technical branch predecessor. R2 technical completion grants no ASM acceptance. QAR retains question normalization/scoring authority; LI retains execution authority; ASM owns only the ratified frozen multi-item aggregate/runtime/durability boundary.

Same `codex/w6-stage1-recovery-exec-012` Draft PR. R3-A parent exact R2-C2; R3-B parent exact R3-A; R3-C parent exact R3-B. No second writer/reset/rebase/amend/squash/force/merge-from-main.

## Baseline

Baseline is exact R2-C2 natural `pull_request`/`synchronize` SUCCESS with exact run/attempt/job/head and all available verification/browser artifact digests read back before A. Missing, ambiguous or contradictory baseline => STOP.

## Commit A — exact test-only allowlist

Only:
- `tests/wave6-frozen-assessment.test.mjs`
- `tests/qar-00-question-activity-contracts.test.mjs`
- `tests/ielts-persistence.test.mjs`
- `tests/backup-registry.test.mjs`
- `tests/migration-ledger.test.mjs`
- `tests/restore-safety.test.mjs`

No production source may change. Recovery/local tests may inform authoring only after predecessor binding; blocked/rejected historical bytes are not authorized wholesale.

Eligible first RED must be an explicit Frozen Assessment capability assertion through modules already present at R2-C2: existing canonical QAR/IELTS seams do not expose the dedicated durable Frozen Assessment owner plus authenticated immutable multi-item scoring snapshot. The test must establish healthy existing QAR/IELTS prerequisites before any controlled dynamic import of the new ASM module.

Raw `ERR_MODULE_NOT_FOUND`, syntax error, IndexedDB harness failure, unrelated backup/restore failure, missing dependency or infrastructure failure is invalid RED.

Immutable A must prove after GREEN:
- immutable authenticated multi-item AssessmentBlueprint and RunSnapshot;
- exact item/order/question/key/scorer/source bindings sourced from QAR authority;
- atomic terminal completion and deterministic replay/collision handling;
- invalid/incomplete/tampered input mutates nothing;
- additive durable storage survives reopen, combined backup and restore;
- hostile accessor/cycle/symbol/sparse input fails closed without unsafe accessor execution;
- public results expose raw item/aggregate facts only;
- no EvidencePolicy/schedule/readiness/band/mastery/representativeness/AI-key authority.

RED invalidation: any source delta in A; unexpected path; raw module/syntax/infra/IndexedDB/unrelated baseline failure; wrong parent; assertion already passing predecessor; ambiguous cause. STOP before B on invalidation.

## Commit B — exact source-only allowlist / minimal GREEN

Only:
- `src/frozen-assessment-contracts.js`
- `src/frozen-assessment-runtime.js`
- `src/question-activity-contracts.js`
- `src/ielts-domain.js`
- `src/ielts-persistence.js`
- `src/backup-registry.js`

All A blobs immutable. Local recovery source may be consulted only after eligible RED and adapted minimally inside this allowlist.

Minimal GREEN: one Frozen Assessment owner consuming existing QAR authority with the minimum additive persistence/backup support required. Explicitly excluded: second assessment scheduler, second QAR registry, model-generated executable keys/scoring, EvidencePolicy writes, representative/full-mock/band/readiness/mastery claims.

Focused verification:
`node --test tests/wave6-frozen-assessment.test.mjs tests/qar-00-question-activity-contracts.test.mjs tests/ielts-persistence.test.mjs tests/backup-registry.test.mjs tests/migration-ledger.test.mjs tests/restore-safety.test.mjs`
plus every applicable shared PR GREEN gate.

Natural B exact-head workflow MUST conclude SUCCESS. Any failure, including infrastructure failure, => STOP / new authority.

## Commit C / evidence

C changes exactly:
`docs/superpowers/specs/2026-08-14-wave6-stage1-exec-012/w6-asm-00-012-evidence-v1.md`

A/B blobs remain immutable. C natural CI MUST succeed. Evidence binds AUTH-012 exact accepted head/comment; predecessor; A/B/C topology/blobs; fresh RED/GREEN; migration raw outputs; B/C artifacts/digests; immutable A/B; overlap checks; limitations; `IMPLEMENTER_EVIDENCE / NOT_ACCEPTANCE`; package and merge authority NOT_GRANTED.

## Migration obligations

Forward-only additive migration:
- IELTS IndexedDB version `3 -> 4`;
- add exactly one `frozenAssessments` store and only indexes required by immutable A tests;
- preserve every pre-existing IELTS store/record;
- IELTS backup version `3 -> 4` where required by the owner module;
- combined backup schema/registry `5 -> 6`;
- supported legacy v5/older upgrade paths produce valid v6 envelope, adding empty Frozen Assessment state only where historical schema could not contain it;
- restore/reopen preserves Frozen Assessment rows and unrelated owner data.

No destructive reset, IndexedDB downgrade, store deletion or backup-history rewrite.

## Rollback / STOP / integration

Before future integration: discard/revert R3 chain. After accepted integration: disable producers while retaining compatible readers/store/backup support; never downgrade DB versions or delete durable rows.

STOP on unresolved R2-C2 baseline, open overlap, second writer, wrong parent/path, source-before-RED, invalid RED, A mutation, migration/backup/restore ambiguity, any B/C GREEN failure, second runtime/store/registry, AI scoring authority, artifact mismatch or acceptance inference.

No Ready/merge/status reconciliation. Independent final auditor must verdict R3 separately. Package acceptance and merge authority remain NOT_GRANTED. `MECHANICAL_INTEGRATION_PREAUTHORIZATION: NOT_GRANTED`.
