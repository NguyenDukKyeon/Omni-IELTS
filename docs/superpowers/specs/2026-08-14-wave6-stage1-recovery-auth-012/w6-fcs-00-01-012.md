# Package Record — W6-FCS-00-01-012

Manifest: `W6-STAGE1-RECOVERY-AUTH-012`
Protocol: `BOUNDED_EXECUTION_CAPSULE_PROTOCOL_V1`
Record status: `CONDITIONAL / INACTIVE UNTIL EXACT MANIFEST ACCEPT`
Canonical owner: bounded `P1-07 Today Composer` Wave 6 seam, ratified through merged PR #69.
Writer: `W6-STAGE1-EXECUTOR-012 / ONE_WRITER_EXCLUSIVE`.

## Exact predecessor / baseline / topology

R2-A2 parent MUST be literal exact R1-C `c14047c74ff6499e70e06617f23b4f7161685cb2`.

Required predecessor baseline:
- natural CI #384 / run `31763393330` / attempt 1 / exact head R1-C / conclusion SUCCESS;
- `verification-output` `9205423689`, digest `sha256:a061acd340ddc3c074c7b6583a66a0ac03f341a5378b4d613e0f1a2d147e68cb`;
- Core browser `9205436300` / `sha256:03e9307adbe32b729465d8500504784556d64f57972d8e4d92f2505ab0ff3eea`;
- IELTS browser `9205442898` / `sha256:53095b8bb238018f4179de607dd4d789131d77b1516098c47bfe67f93283bc57`;
- V10 browser `9205445299` / `sha256:e63e09620d1d0bf96f41e828cc5031699de8e754db7739167d17347645537fe3`;
- hardening browser `9205446613` / `sha256:db79148e27d6f2341af1913b5327360feb918281b475c19ffa73c417381f7578`.

Branch `codex/w6-stage1-recovery-exec-012` must be created directly from R1-C only after exact AUTH-012 Independent Manifest ACCEPT and fresh overlap gate. One Draft PR to `main`. R2-B2 parent exact R2-A2; R2-C2 parent exact R2-B2. No second writer/reset/rebase/amend/squash/force/merge-from-main/cherry-pick.

Historical #71 R2-A `18027741db38ac3156ce2ce322a3ce6716111b28`, R2-B `3a8e0dd665c632c62307eb4e9c0256331b7a92cb` and CI #385/#386 remain frozen and satisfy no AUTH-012 transition.

## Historical candidate-byte reuse

Only these exact content blobs may be reused prospectively:
- A test `tests/wave6-focus-today.test.mjs` = `904a12845e5b0127c515db86ee36b16e801514e9`;
- B `src/focus-selector.js` = `fb235ce654628567fb8c19f7caed944ddd778065`;
- B `src/today-composer.js` = `0135536b3775753619d5b35d814923fac500a842`;
- B `src/today-planner-v2.js` = `af683fdb91b56ae56aac82069c2e013f0b441bc9`.

Content reuse grants no RED/GREEN/evidence/acceptance. Fresh commits and natural CI are mandatory.

## Commit A2 — exact test-only allowlist

Allowed mutation exactly:
- `tests/wave6-focus-today.test.mjs`

The reusable historical A blob above is authorized as candidate content. Existing `tests/today-composer.test.mjs`, `tests/today-containment.test.mjs`, `scripts/browser-smoke.mjs` and every other repository path are read/run-only and MUST remain byte-identical in A2.

Eligible first natural RED: a named assertion through existing Today modules showing the predecessor cannot bind exactly one authenticated Focus row with reason `observed-weakness-focus` after due work while preserving budget. Expected predecessor observation is zero such Focus rows versus required one. Due/budget/prerequisite fixtures must be healthy before this assertion.

A2 must freeze after GREEN:
- deterministic ranking from authentic R1 WeaknessProfile provenance independent of candidate ordering;
- at most one Focus slot and exactly one when eligible;
- all selected due work precedes Focus and Focus cannot displace due work;
- Focus fits the remaining Today budget;
- exact reason/target/provenance/executor/candidate-set binding is durable and reproducible;
- accepted executor registry only;
- tampered persisted binding fails before executor side effects;
- no realtime provider/AI call during Today composition;
- no second scheduler/store, Focus-side FSRS/EvidencePolicy mutation, P7-04 activation, FCS-02 advisory lane, readiness/band/mastery/representative-assessment semantics.

RED invalidation: any production-source mutation in A2; path outside exact A2 allowlist; missing-module/syntax/browser/network/provider/timeout/infrastructure/unrelated failure before the named assertion; unhealthy prerequisite fixture; wrong parent; assertion already passing predecessor; ambiguous cause. Any invalidation => STOP before B2.

## Commit B2 — exact source-only allowlist / minimal GREEN

Only:
- `src/focus-selector.js`
- `src/today-composer.js`
- `src/today-planner-v2.js`

A2 test blob remains immutable. The reusable source blobs are permitted candidate contents, but the executor may use a strict subset or minimally adapt them only inside this exact B2 allowlist to satisfy immutable A2 and current canonical seams. No unrelated Pilot-A repair may be added unless it is inherently required by the immutable Focus contract and independently auditable within this record; otherwise STOP for separate authority.

Minimal GREEN: one deterministic evidence-backed Focus contribution to the existing Today Composer; existing Today remains sole scheduler/composer; due-first; slot cap one; budget preserved; authenticated binding; no provider call; no new durable scheduling truth; no FSRS/EvidencePolicy ownership change; no P7-04/FCS-02/readiness/band/mastery authority.

Focused verification:
`node --test tests/wave6-focus-today.test.mjs tests/today-composer.test.mjs tests/today-containment.test.mjs`
plus `npm run test:browser` and every applicable shared PR gate in `.github/workflows/ci.yml`.

Natural B2 exact-head workflow MUST conclude `SUCCESS`. Any failure, including infrastructure failure, => STOP / new authority. No rerun/waiver.

## Commit C2 / evidence

C2 changes exactly:
`docs/superpowers/specs/2026-08-14-wave6-stage1-exec-012/w6-fcs-00-01-012-evidence-v1.md`

A2/B2 test/source blobs remain immutable. C2 natural exact-head workflow MUST conclude SUCCESS.

Evidence freezes AUTH-012 exact accepted head/comment; R1-C baseline; A2/B2/C2 SHAs/parents/paths/blobs; fresh RED and GREEN run/job/head/artifacts; proof old #385/#386 were not reused; immutable A proof; overlap checks; migration/rollback; C2 artifacts; limitations; `IMPLEMENTER_EVIDENCE / NOT_ACCEPTANCE`; package and merge authority NOT_GRANTED.

## Migration / rollback

Migration: `NONE`.

Rollback before future integration: discard replacement R2 chain. After future accepted integration: disable/remove Focus contribution while preserving pre-existing Today/due/FSRS durable state and all learner evidence. No destructive transform/delete.

## STOP / integration

STOP on manifest non-ACCEPT, R1-C contradiction, open overlap, second writer, wrong parent/path, source-before-eligible-RED, invalid RED, A2 mutation, provider call, second scheduler/store, forbidden authority expansion, any B2/C2 GREEN failure, artifact mismatch or acceptance inference.

No Ready/merge/status reconciliation. Independent final auditor must verdict replacement R2 separately. `MECHANICAL_INTEGRATION_PREAUTHORIZATION: NOT_GRANTED`.
