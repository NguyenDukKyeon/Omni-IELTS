# Package Record — W6-ASM-00-013

Manifest: `W6-STAGE1-RECOVERY-AUTH-013`
Protocol: `BOUNDED_EXECUTION_CAPSULE_PROTOCOL_V1`
Status: `CONDITIONAL / INACTIVE UNTIL EXACT MANIFEST ACCEPT`
Owner: `ASM-00 Frozen Assessment`.
Writer: `W6-STAGE1-EXECUTOR-013 / ONE_WRITER_EXCLUSIVE`.

## Predecessor and topology

Exact predecessor: `2dc10d86a6440efe1c4d8c3dc925923a27977248` (successful R2-C2 only).
R3-A3 parent exact predecessor; R3-B3 parent exact A3; R3-C3 parent exact B3.
Same branch `codex/w6-stage1-recovery-exec-013`; one Draft PR to main; append-only only.

Baseline: natural exact-head R2-C2 CI #391 / run `31771748853` SUCCESS with artifacts frozen in root manifest. Missing/contradictory baseline => STOP.

## R3-A3 — test-only

Allowed test paths only:
- `tests/wave6-frozen-assessment.test.mjs`
- `tests/qar-00-question-activity-contracts.test.mjs`
- `tests/ielts-persistence.test.mjs`
- `tests/backup-registry.test.mjs`
- `tests/migration-ledger.test.mjs`
- `tests/restore-safety.test.mjs`

No source path may change.

Corrected mandatory prerequisite sequence before ASM capability assertion:
- authentic owner-verified QAR questions must be created using current canonical adapters;
- registry registration uses the question's exact kind/version/capabilities;
- `registry.supports(question)` must be true;
- **canonical executor-health predicate is `registry.hasExecutor(question) === true`**;
- existing IELTS persistence/backup baseline must read successfully;
- only then controlled dynamic import may catch expected `ERR_MODULE_NOT_FOUND` for new ASM modules.

Eligible RED: after all prerequisites are healthy, explicit assertion `dedicated durable Frozen Assessment owner plus authenticated immutable multi-item scoring snapshot is absent` fails because capability is absent.

Invalid RED: any prerequisite failure, `hasExecutor(kind,version)` or other API misuse, raw module/syntax error, fixture/dependency/IndexedDB/infrastructure/unrelated failure, wrong parent/path, source delta, already-passing capability, ambiguous cause. Invalid => STOP / no B.

Immutable A after GREEN must prove:
- immutable authenticated multi-item blueprint/run snapshot;
- exact question/order/key/scorer/source bindings from QAR authority;
- atomic terminal completion and deterministic replay/conflict;
- incomplete/duplicate/unknown/tampered input mutates nothing;
- additive durable persistence plus standalone and combined backup/restore/reopen;
- hostile accessors/cycles/symbol/private-shaped input fail closed without accessor execution;
- public result contains raw item/aggregate facts only;
- `representative=false`, `bandScore=null`, `readiness=null`, `mastery=null`, `affectsSchedule=false`, `evidenceEligible=false`.

## R3-B3 — source-only

Allowed source paths only:
- `src/frozen-assessment-contracts.js`
- `src/frozen-assessment-runtime.js`
- `src/question-activity-contracts.js`
- `src/ielts-domain.js`
- `src/ielts-persistence.js`
- `src/backup-registry.js`

All A test blobs immutable.
Minimal GREEN: one Frozen Assessment owner consuming existing QAR authority with only additive persistence/backup support required by A.

Excluded: second scheduler, second QAR registry/owner, AI-generated executable key/scoring, EvidencePolicy/FSRS writes, representative/full-mock/band/readiness/mastery claims.

Focused verification:
`node --test tests/wave6-frozen-assessment.test.mjs tests/qar-00-question-activity-contracts.test.mjs tests/ielts-persistence.test.mjs tests/backup-registry.test.mjs tests/migration-ledger.test.mjs tests/restore-safety.test.mjs`
plus all shared natural PR GREEN gates. Any B failure => STOP/new authority.

## R3-C3 evidence

Only:
`docs/superpowers/specs/2026-08-14-wave6-stage1-exec-013/w6-asm-00-013-evidence-v1.md`

Evidence must bind accepted AUTH-013 head/comment, R2-C2 baseline, A/B/C SHAs/parents/blobs/paths, raw RED, exact B/C GREEN run/job/head/artifact IDs/digests, overlap preflight, migration outputs, immutable A/B and authority boundaries. C natural CI must SUCCESS.

## Migration

Forward-only additive:
- IELTS IndexedDB `3 -> 4`;
- exactly one `frozenAssessments` store with only required indexes;
- preserve all existing stores/records;
- IELTS backup `3 -> 4`;
- combined backup schema/registry `5 -> 6`;
- legacy v5 and older supported envelopes upgrade with empty Frozen state only where historical schema could not contain it;
- restore/reopen preserves Frozen rows and unrelated owner data.
No downgrade, deletion, destructive reset or history rewrite.

## STOP / rollback / acceptance

STOP on overlap, second writer, wrong predecessor/path, source-before-valid-RED, invalid RED, A mutation, migration ambiguity, second store/runtime/registry, unauthorized scoring/claims, B/C non-success or artifact mismatch.

Rollback before integration: discard/revert R3 chain. After accepted integration, disable producers but retain compatible readers/store/backup; never downgrade or delete rows.

Independent final audit required. Package acceptance and merge authority NOT_GRANTED. `MECHANICAL_INTEGRATION_PREAUTHORIZATION: NOT_GRANTED`.
