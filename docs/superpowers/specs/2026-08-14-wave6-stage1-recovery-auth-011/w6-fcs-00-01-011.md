# Package Record — W6-FCS-00-01-011

Manifest: `W6-STAGE1-RECOVERY-AUTH-011`
Protocol: `BOUNDED_EXECUTION_CAPSULE_PROTOCOL_V1`
Record status: `CONDITIONAL / INACTIVE UNTIL EXACT MANIFEST ACCEPT`
Canonical owner: bounded `P1-07 Today Composer` Wave 6 seam, independently ratified and merged through PR #69.
Writer: `W6-STAGE1-EXECUTOR-011 / ONE_WRITER_EXCLUSIVE`.

## Exact predecessor / topology

R2-A2 parent MUST be literal exact R1-C `c14047c74ff6499e70e06617f23b4f7161685cb2`. Required predecessor evidence: natural CI #384 / run `31763393330` SUCCESS, verification-output artifact `9205444163`, digest `sha256:a061acd3422245033a8311e80f4c1b46499d50464d17e22d4309a0f21fcfe549`.

Branch `codex/w6-stage1-recovery-exec-011` must be created directly from that R1-C only after exact AUTH-011 independent ACCEPT. One Draft PR to `main`. R2-B2 parent exact R2-A2; R2-C2 parent exact R2-B2. No second writer/reset/rebase/amend/squash/force/merge-from-main/cherry-pick from #71.

Historical #71 R2-A `18027741db38ac3156ce2ce322a3ce6716111b28`, R2-B `3a8e0dd665c632c62307eb4e9c0256331b7a92cb`, CI #385/#386 remain frozen and do not satisfy any AUTH-011 transition.

## Explicit candidate-byte reuse

Prospective reuse is permitted only for content of:
- A test blob `tests/wave6-focus-today.test.mjs` = `039f4c90988b8fc2326ab54a4c50e93ba3a3f50e`;
- B `src/focus-selector.js` = `fb235ce654628567fb8c19f7caed944ddd778065`;
- B `src/today-composer.js` = `0135536b3775753619d5b35d814923fac500a842`;
- B `src/today-planner-v2.js` = `af683fdb91b56ae56aac82069c2e013f0b441bc9`.

Reuse grants no RED/GREEN/evidence/acceptance. Fresh replacement commits and natural CI are mandatory.

## Commit A2 — test-only

Allowed mutation:
- `tests/wave6-focus-today.test.mjs`

The exact reusable blob above is authorized and preferred. Existing `tests/today-composer.test.mjs`, `tests/today-containment.test.mjs` and `scripts/browser-smoke.mjs` may be read and run but MUST remain byte-identical in A2 unless an independently accepted amendment expressly changes this allowlist. No production source change.

Eligible first natural RED: named assertion through existing Today path that exactly one authenticated observed-weakness Focus row is absent while due precedence and budget fixtures are healthy. Expected predecessor behavior is `focusRows.length === 0` against required `1`.

A2 must also freeze after GREEN:
- due work precedes Focus;
- at most exactly one selected Focus slot;
- budget preserved;
- Focus selection deterministic under caller ordering;
- persisted Focus binding authenticated and tampering rejected before executor side effects;
- no provider call during Today composition;
- no second scheduler, FSRS mutation, FCS-02, P7-04 activation, readiness, band, mastery or representative assessment semantics.

Invalid RED: source mutation; missing-module/syntax failure; browser/network/provider/timeout/infrastructure failure; unrelated existing test failure; wrong parent; predecessor assertion already passes; ambiguous first cause. Any invalidation => STOP before B2.

## Commit B2 — source-only minimal GREEN

Only:
- `src/focus-selector.js`
- `src/today-composer.js`
- `src/today-planner-v2.js`

All A2 test bytes immutable. The exact reusable source blobs above are permitted candidate contents; executor may use a strict subset only if immutable A2 and shared gates pass. No other source/test/harness path may change.

Minimal GREEN: deterministic Focus selector over canonical R1 WeaknessProfile; existing Today composer remains sole scheduler/composer; due-first; one Focus slot; budget preserved; authenticated binding; no provider call; no FSRS/EvidencePolicy mutation or new durable scheduler truth.

Focused verification:
`node --test tests/wave6-focus-today.test.mjs tests/today-composer.test.mjs tests/today-containment.test.mjs`
plus `npm run test:browser` and every shared PR gate from `.github/workflows/ci.yml`.

Natural B2 exact-head workflow MUST conclude `SUCCESS`. Any failure, including `INFRASTRUCTURE_FAILURE`, is STOP; do not rerun or waive.

## Commit C2 / evidence

C2 changes exactly:
`docs/superpowers/specs/2026-08-14-wave6-stage1-exec-011/w6-fcs-00-01-011-evidence-v1.md`

A2/B2 test/source blobs remain immutable. C2 natural exact-head workflow must conclude SUCCESS and its artifacts/digests must be read back.

Evidence must include predecessor R1-C identity; new A2/B2/C2 SHAs/parents/paths/blobs; fresh RED and GREEN run/job/head evidence; proof old #385/#386 were not reused; immutable A proof; no-migration statement; rollback; C2 CI/artifacts; limitations; `IMPLEMENTER_EVIDENCE / NOT_ACCEPTANCE`; package/merge authority NOT_GRANTED.

## Migration / rollback

Migration: `NONE`.
Rollback before future integration: discard replacement R2 chain. After future accepted integration: disable/remove Focus selector/contribution while preserving pre-existing Today/due/FSRS durable state. Do not transform or delete learner evidence.

## STOP / integration

STOP on any predecessor mismatch, second writer, open overlapping source writer, wrong path, source-before-RED, invalid/ambiguous RED, A mutation, provider call, second scheduler/store, FCS-02/P7-04/readiness/band/mastery authority, GREEN failure, artifact mismatch or acceptance inference.

No Ready/merge/status reconciliation. Independent final auditor must verdict replacement R2 separately. `MECHANICAL_INTEGRATION_PREAUTHORIZATION: NOT_GRANTED`.