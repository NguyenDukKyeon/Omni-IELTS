# Package Record — W6-TD-00-011

Manifest: `W6-STAGE1-RECOVERY-AUTH-011`
Protocol: `BOUNDED_EXECUTION_CAPSULE_PROTOCOL_V1`
Record status: `CONDITIONAL / INACTIVE UNTIL EXACT MANIFEST ACCEPT AND R3-C SUCCESS`
Canonical owner: `TD-00 Targeted Diagnostic` adapter, independently ratified and merged through PR #69.
Writer: `W6-STAGE1-EXECUTOR-011 / ONE_WRITER_EXCLUSIVE`.

## Exact predecessor selector / dependency / topology

Exact predecessor selector: the unique exact immutable R3-C SHA produced under AUTH-011 and read back with natural exact-head SUCCESS CI/artifacts before R4-A. No other SHA is eligible.

Dependencies: technical R1 WeaknessProfile output and technical R3 Frozen Assessment/QAR output on the same replacement branch. Neither dependency grants TD acceptance. ASM remains the only durable assessment owner.

Same `codex/w6-stage1-recovery-exec-011` Draft PR. R4-A parent exact R3-C; R4-B parent exact R4-A; R4-C parent exact R4-B. No second writer/reset/rebase/amend/squash/force/merge-from-main.

## Baseline

Baseline is exact R3-C natural `pull_request/synchronize` SUCCESS with exact run/attempt/job/head/artifact identities read back before A. Missing/ambiguous baseline => STOP.

## Commit A — exact allowlist

Only:
- `tests/wave6-targeted-diagnostic.test.mjs`

No production source change. A statically uses only dependency modules present at R3-C. For the new TD module, a controlled dynamic import may catch only expected module absence and convert it into the named product-capability assertion `Targeted Diagnostic adapter capability is absent`. Uncaught module-resolution/syntax failure is invalid RED.

Eligible first RED: explicit product capability assertion that `createTargetedDiagnosticAdapter` is unavailable while R1/R3 prerequisite fixtures are healthy.

Immutable A must prove after GREEN:
- only branded canonical WeaknessProfile v1 accepted;
- deterministic weakness-biased selection independent of caller ordering;
- at least two sufficiently observed weak skills and at least two authentic supported QAR items per selected skill;
- forged/duplicate/hostile/unsupported question input fails before Frozen-owner mutation;
- ASM-00 is sole durable assessment owner;
- blueprint/run/replay/backup compose through Frozen Assessment, never a second store/runtime;
- final diagnostic explicitly non-representative: `representative=false`, `bandScore=null`, `readiness=null`, `mastery=null`, `affectsSchedule=false`, `evidenceEligible=false`.

RED invalidation: source mutation in A; unexpected path; uncaught module/syntax/infrastructure/dependency failure; unhealthy prerequisite fixture; wrong parent; predecessor already provides capability; ambiguous cause. STOP before B on invalidation.

## Commit B — exact allowlist / minimal GREEN

Only:
- `src/targeted-diagnostic.js`

A blob unchanged. R4 may read but not modify WeaknessProfile, Frozen Assessment, QAR, Today, FSRS or EvidencePolicy modules. Local recovery TD source may be consulted only after eligible RED and adapted minimally inside this one path.

Minimal GREEN: one deterministic weakness-biased adapter over technical dependency shapes; no second store/runtime/scheduler; no schedule/evidence authority; no representative score/readiness/band/mastery claim.

Focused verification:
`node --test tests/wave6-targeted-diagnostic.test.mjs tests/progress.test.mjs tests/wave6-frozen-assessment.test.mjs`
plus every shared PR GREEN gate.

Natural B exact-head workflow MUST conclude SUCCESS. Any GREEN failure => STOP/new authority.

## Commit C / evidence

C changes exactly:
`docs/superpowers/specs/2026-08-14-wave6-stage1-exec-011/w6-td-00-011-evidence-v1.md`

A/B blobs immutable. C natural exact-head workflow must conclude SUCCESS. Evidence must bind AUTH-011 exact accepted head/comment, predecessor, A/B/C topology/blobs, fresh RED/GREEN run/job/head/artifacts, dependency identities, immutable A/B, limitations and `IMPLEMENTER_EVIDENCE / NOT_ACCEPTANCE`.

## Migration / rollback

Migration: `NONE`. TD creates no durable store/schema/version and writes only through ASM-00.

Rollback before future integration: discard/revert R4 chain. After accepted integration: remove/disable TD adapter only; Frozen Assessment rows remain valid and unrelated owner state untouched.

## STOP / integration

STOP on unresolved R3-C baseline, second writer, wrong parent/path, source-before-RED, invalid RED, A mutation, unsupported/hostile input mutating Frozen state, second store/runtime/scheduler, readiness/band/mastery/evidence/schedule authority, GREEN failure, artifact mismatch or acceptance inference.

No Ready/merge/status reconciliation. Independent final auditor must verdict R4 separately. Package acceptance and merge authority remain NOT_GRANTED. `MECHANICAL_INTEGRATION_PREAUTHORIZATION: NOT_GRANTED`.